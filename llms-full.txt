# TRDS — Türkiye Kamu Tasarım Sistemi (full reference)

> TRDS (Türkiye Kamu Tasarım Sistemi) is an open design system for Turkish public services. It has 116 HTML and CSS components with JavaScript, React and Vue wrappers, Turkish validators, DTCG tokens, and Tabler icons. Build tools use the Node.js standard library. Tests need React and Vue. Code and icons use MIT licences. State identity assets have separate terms. TRDS has no government endorsement.

This file is generated by `node tools/llms.mjs` from the registry, the component definitions, the core script, the validators and the tokens. It contains the complete markup of every component, the JavaScript API, the validator API and every design token. Version 1.0.1.

## How to integrate in 60 seconds

**Plain HTML.** Include the two files from `packages/core/dist/` and call `baslat()` once. Every behaviour is keyed by a `data-trds="<name>"` attribute and enhances working HTML.

```html
<link rel="stylesheet" href="/trds/trds.min.css">
<script type="module">
  import { baslat } from '/trds/trds.min.js';
  baslat(); // or baslat(document.getElementById('form'))
</script>
```

**React.** `npm install @tr-ds/react @tr-ds/core`. Plain JavaScript, no JSX build needed for the library. Components take Turkish prop names and render the exact HTML of the docs.

```jsx
import '@tr-ds/core/css';
import { KimlikNoGirisi, Dugme } from '@tr-ds/react';
<form><KimlikNoGirisi etiket="T.C. kimlik numaranız" name="tckn" /><Dugme>Devam et</Dugme></form>
```

**Vue 3.5 or later.** `npm install @tr-ds/vue @tr-ds/core`. Render functions, no template compiler. `app.use(Trds)` registers every component with the `Trds` prefix.

```js
import { createApp } from 'vue';
import { Trds } from '@tr-ds/vue';
import '@tr-ds/core/css';
createApp(App).use(Trds); // <TrdsKimlikNoGirisi etiket="T.C. kimlik numaranız" name="tckn" />
```

## Rules an AI must follow

- Class names are Turkish BEM: `trds-<block>__<element>--<modifier>`. Never invent a class; every class in this file exists in `trds.css`.
- Colour, spacing, type and radius come only from tokens `--trds-*`. Themes: `<html data-trds-tema="acik|koyu|yuksek">`, font size `data-trds-yazi="normal|buyuk|cok-buyuk"`. Do not write raw hex values.
- Every interactive part works without JavaScript. Script adds behaviour after `baslat()`; it never replaces markup.
- Form fields: label + optional `.trds-yardim` + optional `.trds-hata` + input inside `.trds-alan`. Errors also go to the page-top `.trds-hata-ozeti`. Touch targets are 44 px.
- Icons: inline the sprite `packages/identity/dist/trds-simgeler.svg` once, then `<svg class="trds-simge" aria-hidden="true"><use href="#trds-health"/></svg>`. 74 icons from Tabler Icons (MIT), one 24-unit grid, 2-unit stroke; do not add other icon sets.
- State identity assets have separate terms in LICENSE-IDENTITY.md. TRDS cannot grant permission for third-party logos. Code and Tabler icons use MIT licences.
- Turkish text rules: `İ/ı` case with `trBuyuk()/trKucuk()`, sorting with `trSirala()`, money with `tlBicimle()`, dates as `gg.aa.yyyy`.


## Page skeleton

A TRDS page has this order. Full-width parts carry their own `.trds-kap` container. The identifier and the footer never stand together; the corporate footer carries the institution.

```html
<!doctype html>
<html lang="tr" data-trds-tema="acik">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Hizmet adı — Kurum adı</title>
  <link rel="stylesheet" href="/trds/trds.min.css">
</head>
<body>
  <a class="trds-atla" href="#ana-icerik">Ana içeriğe geç</a>
  <!-- svg sprite: packages/identity/dist/trds-simgeler.svg, inline once, display:none -->
  <div class="trds-resmi-afis" data-trds="resmi-afis">…</div>      <!-- masthead -->
  <header class="trds-baslik-cubugu" data-trds="baslik-cubugu">…</header>
  <main id="ana-icerik" tabindex="-1">
    <div class="trds-kap">…</div>
  </main>
  <footer class="trds-alt-bilgi">…</footer>
  <script type="module">import { baslat } from '/trds/trds.min.js'; baslat();</script>
</body>
</html>
```

## JavaScript API (@tr-ds/core)

`baslat(kok = document, secenek = {})` runs every initialiser below on the descendants of `kok`. Call it once after the DOM exists, and again after you insert new markup. Each element is initialised once (idempotent). `secenek.ilceGetir(ilKodu)` may return a promise of districts for the address part.

Initialisers, each exported and each keyed by a `data-trds` attribute:

- `akordiyonBaslat(kok)`
- `sekmelerBaslat(kok)`
- `kipBaslat(kok)`
- `hataOzetiBaslat(kok)`
- `karakterSayaciBaslat(kok)`
- `dosyaBaslat(kok)`
- `parolaBaslat(kok)`
- `aranabilirBaslat(kok)`
- `aramaBaslat(kok)`
- `geciciBaslat(kok)`
- `cipBaslat(kok)`
- `acilirBilgiBaslat(kok)`
- `yanPanelBaslat(kok)`
- `eylemMenusuBaslat(kok)`
- `basaDonBaslat(kok)`
- `cerezBaslat(kok)`
- `cikisBaslat(kok)`
- `siralaBaslat(kok)`
- `aralikBaslat(kok)`
- `adetBaslat(kok)`
- `yazdirBaslat(kok)`
- `maskeBaslat(kok)`
- `takvimBaslat(kok)`
- `cokluSecimBaslat(kok)`
- `genisMenuBaslat(kok)`
- `kayanPanoBaslat(kok)`
- `yonlendirmeBaslat(kok)`
- `ipucuBaslat(kok)`
- `baslikCubuguBaslat(kok)`
- `resmiAfisBaslat(kok)`
- `erisimMenusuBaslat(kok)`
- `kimlikNoBaslat(kok)`
- `vergiNoBaslat(kok)`
- `ibanBaslat(kok)`
- `telefonBaslat(kok)`
- `plakaBaslat(kok)`
- `tarihBaslat(kok)`
- `adresBaslat(kok)`
- `kvkkBaslat(kok)`

Conventions: a live region is `[data-trds-durum]` (or `.trds-*__durum` in older parts); an error slot is `[data-trds-hata]`; a counter is `[data-trds-sayac]`; `data-zorunlu` makes an empty field an error; custom events are `trds:secildi`, `trds:kapandi`, `trds:kaldirildi`, `trds:suzgecDegisti`, `trds:cerezSecildi`.

## Validators (@tr-ds/validators)

Pure functions, no DOM. The same functions run in the browser and on the server.

- `trBuyuk(metin)` — Türkçe kuralına göre büyük harfe çevirir. `"istanbul".toUpperCase()` "ISTANBUL" verir. Doğrusu "İSTANBUL" olur. Bu fark bir plaka sorgusunda yanlış aracı bulur. @param {string} metin @returns {string}
- `trKucuk(metin)` — Türkçe kuralına göre küçük harfe çevirir. @param {string} metin @returns {string}
- `trSirala(a, b)` — Türkçe alfabe sırasına göre karşılaştırır. Liste ve tablo sıralamasında kullanın. `Array.prototype.sort` varsayılanı ç, ğ, ı, ö, ş ve ü harflerini yanlış yere koyar. @param {string} a @param {string} b @returns {number}
- `tcKimlikGecerli(deger)` — T.C. kimlik numarasının sağlama toplamını doğrular. Kural: d10 = ((d1+d3+d5+d7+d9) * 7 - (d2+d4+d6+d8)) mod 10 d11 = (d1 + ... + d10) mod 10 Ayrıca ilk hane 0 olamaz. @param {string} deger @returns {boolean}
- `yabanciKimlikGecerli(deger)` — Yabancı kimlik numarası biçimini doğrular. Bu numara 99 ile başlar ve T.C. kimlik numarasının sağlama toplamını taşımaz. Bu yüzden yalnız biçim denetlenir. Kesin doğrulama NVİ servisinden yapılır. @param {string} deger @returns {boolean}
- `kimlikNoDenetle(deger, secenek = {})` — Hem T.C. hem yabancı kimlik numarasını kabul eden tek giriş noktası. @param {string} deger @param {{ yabanciKabul?: boolean }} [secenek] @returns {{ gecerli: boolean, tur: 'tc'|'yabanci'|null, hata: string|null }}
- `vergiNoGecerli(deger)` — Vergi kimlik numarasının sağlama toplamını doğrular. 10 hanedir. @param {string} deger @returns {boolean}
- `ibanGecerli(deger)` — IBAN sağlamasını mod 97 kuralı ile doğrular. Boşlukları ve küçük harfleri kabul eder ve temizler. @param {string} deger @returns {boolean}
- `ibanKontrolHesapla(ulke, govde)` — Bir IBAN gövdesi için iki haneli kontrol numarasını hesaplar. Test ve veri üretimi için kullanılır. @param {string} ulke  iki harfli ülke kodu @param {string} govde kontrol haneleri olmadan IBAN gövdesi @returns {string} iki haneli kontrol numarası
- `ibanBicimle(deger)` — IBAN’ı dörderli gruplayarak gösterime hazırlar. Gönderilen değer boşluksuz kalır, yalnız ekranda gruplanır. @param {string} deger @returns {string}
- `ibanBankaKodu(deger)` — Türk IBAN’ından beş haneli banka kodunu çıkarır. Konum: 5. ile 9. karakter. @param {string} deger @returns {string|null}
- `telefonNormalle(deger)` — Telefon numarasını 10 haneye indirger. Başta yazılan 0, +90, 90 ve boşluk gibi ayraçlar sessizce kaldırılır. Kullanıcı numarasını istediği gibi yazar. @param {string} deger @returns {string} 10 haneli numara, veya temizlenmiş kısmi değer
- `telefonGecerli(deger, secenek = {})` — Türk telefon numarasını doğrular. İlk hane 2, 3, 4 veya 5 olur. 5 ile başlayan numara cep telefonudur. @param {string} deger @param {{ yalnizCep?: boolean }} [secenek] @returns {boolean}
- `telefonBicimle(deger)` — Telefon numarasını 5XX XXX XX XX biçiminde gösterir. @param {string} deger @returns {string}
- `plakaDenetle(deger)` — Türk araç plakasını doğrular ve parçalarına ayırır. Kabul edilen kalıplar: 1 harf ve 4 veya 5 rakam, 2 harf ve 3 veya 4 rakam, 3 harf ve 2 veya 3 rakam. @param {string} deger @returns {{ gecerli: boolean, il: string|null, harf: string|null, sayi: string|null }}
- `plakaBicimle(deger)` — Plakayı "34 ABC 123" biçiminde gösterir. @param {string} deger @returns {string}
- `postaKoduGecerli(deger)` — Posta kodunu doğrular. Beş hanedir ve ilk iki hane il kodudur. @param {string} deger @returns {boolean}
- `tarihDenetle(parcalar, secenek = {})` — Üç parçalı tarih girişini doğrular. Hangi parçanın hatalı olduğunu söyler, böylece odak doğru alana gider. @param {{ gun: string|number, ay: string|number, yil: string|number }} parcalar @param {{ gelecekYasak?: boolean, enEskiYil?: number }} [secenek] @returns {{ gecerli: boolean, hata: string|null, alan: 'gun'|'ay'|'yil'|null, tarih: Date|null }}
- `tarihBicimle(tarih)` — Tarihi Türkçe gg.aa.yyyy biçiminde yazar. @param {Date} tarih @returns {string}
- `tarihUzunBicimle(tarih)` — Tarihi "27 Mart 1997" biçiminde yazar. Ay adı Türkçe kuralına göre yazılır. @param {Date} tarih @returns {string}
- `tlBicimle(kurus)` — Tutarı Türk Lirası olarak yazar: 1.250,00 ₺ @param {number} kurus tam sayı olarak kuruş değeri @returns {string}

Also exported: `AYLAR` (Turkish month names).

## Design tokens (@tr-ds/tokens)

161 tokens. Light values below; the dark theme redefines the colour tokens under `[data-trds-tema="koyu"]` and `prefers-color-scheme: dark`. High contrast `[data-trds-tema="yuksek"]` thickens borders and removes muted tones. Never hard-code a colour: use the token.

### trds

- `--trds-renk-notr-0`: `#ffffff`
- `--trds-renk-notr-50`: `#f4f6f8`
- `--trds-renk-notr-100`: `#e8ecf0`
- `--trds-renk-notr-200`: `#d5dbe2`
- `--trds-renk-notr-300`: `#b7c0ca`
- `--trds-renk-notr-400`: `#8e99a6`
- `--trds-renk-notr-500`: `#6b7684`
- `--trds-renk-notr-600`: `#545e6b`
- `--trds-renk-notr-700`: `#3d4650`
- `--trds-renk-notr-800`: `#2a323b`
- `--trds-renk-notr-900`: `#1a2027`
- `--trds-renk-notr-950`: `#10151a`
- `--trds-renk-notr-zemin-varsayilan`: `#ffffff`
- `--trds-renk-notr-zemin-yumusak`: `#f4f6f8`
- `--trds-renk-notr-yuzey-varsayilan`: `#ffffff`
- `--trds-renk-notr-yuzey-yumusak`: `#e8ecf0`
- `--trds-renk-notr-yuzey-uzeri`: `#e8ecf0`
- `--trds-renk-notr-yuzey-etkin`: `#d5dbe2`
- `--trds-renk-notr-kenar-silik`: `#d5dbe2`
- `--trds-renk-notr-kenar-varsayilan`: `#8e99a6`
- `--trds-renk-notr-kenar-guclu`: `#1a2027`
- `--trds-renk-notr-metin-silik`: `#545e6b`
- `--trds-renk-notr-metin-varsayilan`: `#1a2027`
- `--trds-renk-notr-metin-karsit`: `#ffffff`
- `--trds-renk-notr-taban-varsayilan`: `#1a2027`
- `--trds-renk-notr-taban-uzeri`: `#2a323b`
- `--trds-renk-notr-taban-etkin`: `#10151a`
- `--trds-renk-notr-taban-karsit-varsayilan`: `#ffffff`
- `--trds-renk-notr-ortu`: `rgb(16 21 26 / 0.55)`
- `--trds-renk-birincil-50`: `#eef3fb`
- `--trds-renk-birincil-100`: `#d8e4f6`
- `--trds-renk-birincil-200`: `#b0c8ec`
- `--trds-renk-birincil-300`: `#7fa4dd`
- `--trds-renk-birincil-400`: `#4c7cc7`
- `--trds-renk-birincil-500`: `#2a5da8`
- `--trds-renk-birincil-600`: `#1e4785`
- `--trds-renk-birincil-700`: `#17376a`
- `--trds-renk-birincil-800`: `#112a51`
- `--trds-renk-birincil-900`: `#0c1e3a`
- `--trds-renk-birincil-zemin-yumusak`: `#eef3fb`
- `--trds-renk-birincil-yuzey-yumusak`: `#d8e4f6`
- `--trds-renk-birincil-yuzey-uzeri`: `#d8e4f6`
- `--trds-renk-birincil-kenar-silik`: `#b0c8ec`
- `--trds-renk-birincil-kenar-varsayilan`: `#1e4785`
- `--trds-renk-birincil-metin-varsayilan`: `#1e4785`
- `--trds-renk-birincil-metin-guclu`: `#112a51`
- `--trds-renk-birincil-metin-ziyaret`: `#4c2c92`
- `--trds-renk-birincil-taban-varsayilan`: `#1e4785`
- `--trds-renk-birincil-taban-uzeri`: `#17376a`
- `--trds-renk-birincil-taban-etkin`: `#112a51`
- `--trds-renk-birincil-taban-karsit-varsayilan`: `#ffffff`
- `--trds-renk-tehlike-50`: `#fdeeed`
- `--trds-renk-tehlike-100`: `#f9d5d2`
- `--trds-renk-tehlike-300`: `#e88a83`
- `--trds-renk-tehlike-500`: `#d32f2f`
- `--trds-renk-tehlike-600`: `#b3261e`
- `--trds-renk-tehlike-700`: `#8c1d17`
- `--trds-renk-tehlike-900`: `#4d100c`
- `--trds-renk-tehlike-zemin-yumusak`: `#fdeeed`
- `--trds-renk-tehlike-kenar-varsayilan`: `#b3261e`
- `--trds-renk-tehlike-kenar-guclu`: `#8c1d17`
- `--trds-renk-tehlike-metin-varsayilan`: `#b3261e`
- `--trds-renk-tehlike-taban-varsayilan`: `#b3261e`
- `--trds-renk-tehlike-taban-uzeri`: `#8c1d17`
- `--trds-renk-tehlike-taban-etkin`: `#4d100c`
- `--trds-renk-tehlike-taban-karsit-varsayilan`: `#ffffff`
- `--trds-renk-basari-50`: `#eaf5ef`
- `--trds-renk-basari-100`: `#cbe7d8`
- `--trds-renk-basari-500`: `#2e8b5b`
- `--trds-renk-basari-600`: `#1e6b46`
- `--trds-renk-basari-700`: `#155134`
- `--trds-renk-basari-900`: `#0b2a1b`
- `--trds-renk-basari-zemin-yumusak`: `#eaf5ef`
- `--trds-renk-basari-kenar-varsayilan`: `#1e6b46`
- `--trds-renk-basari-metin-varsayilan`: `#1e6b46`
- `--trds-renk-basari-taban-varsayilan`: `#1e6b46`
- `--trds-renk-basari-taban-karsit-varsayilan`: `#ffffff`
- `--trds-renk-uyari-50`: `#fdf3e2`
- `--trds-renk-uyari-100`: `#f9e2b8`
- `--trds-renk-uyari-500`: `#c98a00`
- `--trds-renk-uyari-600`: `#a06e00`
- `--trds-renk-uyari-700`: `#7a5400`
- `--trds-renk-uyari-900`: `#3f2b00`
- `--trds-renk-uyari-zemin-yumusak`: `#fdf3e2`
- `--trds-renk-uyari-kenar-varsayilan`: `#a06e00`
- `--trds-renk-uyari-metin-varsayilan`: `#7a5400`
- `--trds-renk-uyari-taban-varsayilan`: `#c98a00`
- `--trds-renk-uyari-taban-karsit-varsayilan`: `#10151a`
- `--trds-renk-kimlik-kirmizi`: `#e30a17`
- `--trds-renk-kimlik-kirmizi-koyu`: `#a8232b`
- `--trds-renk-kimlik-beyaz`: `#ffffff`
- `--trds-renk-kimlik-serit`: `#e30a17`
- `--trds-renk-kimlik-arma`: `#ffffff`
- `--trds-renk-odak-dolgu`: `#ffdd00`
- `--trds-renk-odak-kenar`: `#0b0c0c`
- `--trds-aralik-0`: `0`
- `--trds-aralik-1`: `0.25rem`
- `--trds-aralik-2`: `0.5rem`
- `--trds-aralik-3`: `0.75rem`
- `--trds-aralik-4`: `1rem`
- `--trds-aralik-5`: `1.25rem`
- `--trds-aralik-6`: `1.5rem`
- `--trds-aralik-7`: `2rem`
- `--trds-aralik-8`: `2.5rem`
- `--trds-aralik-9`: `3rem`
- `--trds-aralik-10`: `4rem`
- `--trds-aralik-11`: `5rem`
- `--trds-yazi-aile-govde`: `"TRDS Sans", "Public Sans", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif`
- `--trds-yazi-aile-tek`: `"IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, Consolas, monospace`
- `--trds-yazi-boyut-12`: `0.75rem`
- `--trds-yazi-boyut-14`: `0.875rem`
- `--trds-yazi-boyut-16`: `1rem`
- `--trds-yazi-boyut-18`: `1.125rem`
- `--trds-yazi-boyut-20`: `1.25rem`
- `--trds-yazi-boyut-24`: `1.5rem`
- `--trds-yazi-boyut-30`: `1.875rem`
- `--trds-yazi-boyut-36`: `2.25rem`
- `--trds-yazi-boyut-48`: `3rem`
- `--trds-yazi-kalinlik-normal`: `400`
- `--trds-yazi-kalinlik-orta`: `500`
- `--trds-yazi-kalinlik-kalin`: `700`
- `--trds-yazi-satir-sik`: `1.25`
- `--trds-yazi-satir-normal`: `1.55`
- `--trds-yazi-satir-genis`: `1.75`
- `--trds-kose-0`: `0`
- `--trds-kose-1`: `2px`
- `--trds-kose-2`: `4px`
- `--trds-kose-3`: `8px`
- `--trds-kose-tam`: `999px`
- `--trds-kenarlik-ince`: `1px`
- `--trds-kenarlik-orta`: `2px`
- `--trds-kenarlik-kalin`: `4px`
- `--trds-golge-1`: `0 1px 2px rgba(16, 21, 26, 0.08)`
- `--trds-golge-2`: `0 2px 6px rgba(16, 21, 26, 0.12)`
- `--trds-golge-3`: `0 8px 24px rgba(16, 21, 26, 0.18)`
- `--trds-golge-yok`: `none`
- `--trds-olcu-dokunma`: `44px`
- `--trds-olcu-satir-uzunluk`: `38rem`
- `--trds-olcu-kap-genislik`: `72rem`
- `--trds-olcu-odak-kalinlik`: `3px`
- `--trds-kirilma-telefon`: `30rem`
- `--trds-kirilma-tablet`: `48rem`
- `--trds-kirilma-masaustu`: `64rem`
- `--trds-metin-govde`: `1.125rem`
- `--trds-metin-kucuk`: `1rem`
- `--trds-metin-yardim`: `1rem`
- `--trds-metin-baslik-1`: `2.25rem`
- `--trds-metin-baslik-2`: `1.875rem`
- `--trds-metin-baslik-3`: `1.5rem`
- `--trds-metin-baslik-4`: `1.25rem`
- `--trds-bilesen-dugme-dolgu-y`: `0.75rem`
- `--trds-bilesen-dugme-dolgu-x`: `1.25rem`
- `--trds-bilesen-dugme-en-az-yukseklik`: `44px`
- `--trds-bilesen-dugme-kose`: `4px`
- `--trds-bilesen-girdi-dolgu-y`: `0.5rem`
- `--trds-bilesen-girdi-dolgu-x`: `0.75rem`
- `--trds-bilesen-girdi-kenarlik`: `2px`
- `--trds-bilesen-girdi-kenarlik-hata`: `4px`
- `--trds-bilesen-girdi-yukseklik`: `44px`
- `--trds-bilesen-kart-dolgu`: `1.5rem`
- `--trds-bilesen-kart-kose`: `4px`

## Themes

Three institution themes override the brand tokens from one colour: `@tr-ds/theme-vatandas` (default, #1e4785), `@tr-ds/theme-kurumsal`, `@tr-ds/theme-saglik`. Include the theme CSS after `trds.css`. A new theme is one JSON file with a brand colour; the build checks contrast.

## Identity licence

# Kimlik parçaları için kullanım koşulları

Bu belge `@tr-ds/identity` paketi için geçerlidir. Depodaki diğer paketler MIT
lisanslıdır ve bu kısıtlamalara tabi değildir.

TRDS bir kamu kurumu değildir. Bu belge, üçüncü taraf marka ve logoları için
izin vermez. Haklar ilgili kurumlara aittir. Bu dosyaların depoda bulunması
resmî onay veya kullanım izni anlamına gelmez. Kullanım ve dağıtım için
gerekli izinleri ilgili hak sahibinden alın.

## Simgeler bu belgenin dışındadır

Simge seti [Tabler Icons](https://tabler.io/icons) setinden gelir ve MIT
lisanslıdır. Her simge serbestçe kullanılır. Lisans metni
`packages/identity/src/simgeler/LICENSE-TABLER.txt` içindedir.

## Kapsam

Aşağıdaki parçalar bu belgeye tabidir:

- Türkiye Cumhuriyeti arması ve her türevi
- Resmî site afişi (`trds-resmi-afis`) ve metni
- e-Devlet giriş düğmesi (`trds-edevlet`) ve işareti
- Kurum tanıtıcısı (`trds-tanitici`) ve kurum armaları
- Favicon ve uygulama simgeleri
- `kurumlar/` altındaki kurum logoları. Her biri kurumun kendi sitesinden
  alınmıştır ve yalnız o kurumu göstermek için kullanılır:
  - `e-devlet-kapisi-beyaz.svg` — cdn.e-devlet.gov.tr, türkiye.gov.tr başlık logosu
  - `sgk.svg`, `sgk-beyaz.svg` — www.sgk.gov.tr
  - `saglik-bakanligi.svg` — www.saglik.gov.tr

## İzin verilen kullanım

Bu parçalar yalnız şu koşullarda kullanılabilir:

1. Hizmet, bir Türkiye Cumhuriyeti kamu kurumu tarafından işletilir veya o
   kurum adına yürütülür.
2. Hizmet, `gov.tr` uzantılı bir alan adında yayımlanır.
3. Kullanım, hizmetin gerçek kimliğini gösterir ve bir taklit oluşturmaz.


## Components — complete reference (116)

## Kimlik

Kullanıcıya bunun bir devlet hizmeti olduğunu söyleyen parçalar.

### Alt bilgi (Footer) — `footer`

Group: Kimlik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.5. Also in: UK, US, FR, IE, KR, DK.

Sayfanın sonunda kurumu, zorunlu bağlantıları ve iletişim bilgisini taşır. Kurum logosunu girdi olarak alır.

When to use:
- Her sayfada kullanın. Kurumsal seçenek kurum adını ve üst kurumu kendi taşır. O zaman kurum tanıtıcısı kullanılmaz. Sade seçenek tanıtıcının hemen altında durur.
- Kurumun kendi logosunu `trds-alt-bilgi__logo` ile verin. Logo yoksa kısa ad bir monogram olarak durur: SGK, ÖSYM.
- Adres, santral ve çağrı hattı iletişim bloğundadır. Beş sütuna kadar bağlantı grubu eklenir.
- Koyu seçeneği bakanlık siteleri için vardır. Kırmızı şerit iki seçenekte de kalır.
- Erişilebilirlik bildirimi, KVKK aydınlatma metni ve çerez sayfası bağlantıları burada durur. Kurum bağlantıları tanıtıcıdadır. Bir bağlantı iki yerde durmaz.
- Dil seçici sağda durur. Telif satırı en alttadır.

Accessibility:
- `<footer>` etiketini kullanın.
- Bağlantı listelerine görünür başlık verin.
- Erişilebilirlik bildirimi bağlantısını alt bilgide kolay bulunur bir yerde gösterin.

**Kurumsal, logo girdisi ile**

```html
<footer class="trds-alt-bilgi">
  <div class="trds-kap">
    <div class="trds-alt-bilgi__ust">
      <div class="trds-alt-bilgi__marka">
        <img class="trds-alt-bilgi__logo" src="/trds//e-devlet-isaret.png" alt="" width="64" height="64">
        <div><p class="trds-alt-bilgi__kurum">e-Devlet Kapısı</p><p class="trds-alt-bilgi__ust-kurum">T.C. Cumhurbaşkanlığı Siber Güvenlik Başkanlığı kurar ve yönetir. Türksat A.Ş. işletir.</p></div>
      </div>
      <address class="trds-alt-bilgi__iletisim">
        <p><span class="trds-alt-bilgi__iletisim-etiket">Çağrı merkezi</span><a href="tel:160">160</a></p>
        <p><span class="trds-alt-bilgi__iletisim-etiket">Engelsiz çağrı merkezi</span>İşaret dili ile görüntülü görüşme</p>
      </address>
    </div>
    <div class="trds-alt-bilgi__sutunlar">
      <div><p class="trds-alt-bilgi__sutun-baslik">e-Hizmetler</p><ul class="trds-alt-bilgi__sutun-liste"><li><a href="#">Sık kullanılan hizmetler</a></li><li><a href="#">Yeni eklenen hizmetler</a></li><li><a href="#">Kurum hizmetleri</a></li></ul></div>
      <div><p class="trds-alt-bilgi__sutun-baslik">Yardım</p><ul class="trds-alt-bilgi__sutun-liste"><li><a href="#">Genel yardım</a></li><li><a href="#">Sıkça sorulanlar</a></li><li><a href="#">Güvenliğiniz için</a></li></ul></div>
      <div><p class="trds-alt-bilgi__sutun-baslik">Bize ulaşın</p><ul class="trds-alt-bilgi__sutun-liste"><li><a href="#">İletişim</a></li><li><a href="#">CİMER başvurusu</a></li></ul></div>
    </div>
    <div class="trds-alt-bilgi__satir">
      <nav class="trds-alt-bilgi__baglantilar" aria-label="Alt bilgi bağlantıları"><a href="#">Erişilebilirlik bildirimi</a><a href="#">KVKK aydınlatma metni</a><a href="#">Çerez politikası</a></nav>
      <ul class="trds-alt-bilgi__sosyal" aria-label="Sosyal medya hesapları"><li><a href="#" aria-label="X"><svg class="trds-simge" aria-hidden="true"><use href="#trds-twitter"/></svg></a></li><li><a href="#" aria-label="YouTube"><svg class="trds-simge" aria-hidden="true"><use href="#trds-youtube"/></svg></a></li><li><a href="#" aria-label="Instagram"><svg class="trds-simge" aria-hidden="true"><use href="#trds-instagram"/></svg></a></li></ul>
    </div>
    <p class="trds-alt-bilgi__telif">© 2026 Türkiye Cumhuriyeti</p>
  </div>
</footer>
```

**Koyu**

```html
<footer class="trds-alt-bilgi trds-alt-bilgi--koyu">
  <div class="trds-kap">
    <div class="trds-alt-bilgi__ust">
      <div class="trds-alt-bilgi__marka">
        <span class="trds-alt-bilgi__monogram" aria-hidden="true">SGK</span>
        <div><p class="trds-alt-bilgi__kurum">Sosyal Güvenlik Kurumu</p><p class="trds-alt-bilgi__ust-kurum">Çalışma ve Sosyal Güvenlik Bakanlığı bağlı kuruluşudur.</p></div>
      </div>
      <address class="trds-alt-bilgi__iletisim">
        <p><span class="trds-alt-bilgi__iletisim-etiket">Adres</span>Ziyabey Cad. No:6 06520 Balgat/Ankara</p>
        <p><span class="trds-alt-bilgi__iletisim-etiket">Telefon</span><a href="tel:+903122078000">0 (312) 207 80 00</a> · Alo 170</p>
      </address>
    </div>
    <div class="trds-alt-bilgi__satir">
      <nav class="trds-alt-bilgi__baglantilar" aria-label="Alt bilgi bağlantıları"><a href="#">KVKK</a><a href="#">Aydınlatma metni</a><a href="#">Yasal uyarı</a><a href="#">Site haritası</a></nav>
    </div>
    <p class="trds-alt-bilgi__telif">© 2026 Sosyal Güvenlik Kurumu</p>
  </div>
</footer>
```

**Bağlantı sütunları ile**

```html
<footer class="trds-alt-bilgi">
  <div class="trds-kap">
    <div class="trds-alt-bilgi__sutunlar">
      <div><p class="trds-alt-bilgi__sutun-baslik">e-Hizmetler</p><ul class="trds-alt-bilgi__sutun-liste"><li><a href="#">Sık kullanılan hizmetler</a></li><li><a href="#">Yeni eklenen hizmetler</a></li><li><a href="#">Kurum hizmetleri</a></li></ul></div>
      <div><p class="trds-alt-bilgi__sutun-baslik">Yardım</p><ul class="trds-alt-bilgi__sutun-liste"><li><a href="#">Genel yardım</a></li><li><a href="#">Sıkça sorulanlar</a></li><li><a href="#">Güvenliğiniz için</a></li></ul></div>
      <div><p class="trds-alt-bilgi__sutun-baslik">Bize ulaşın</p><ul class="trds-alt-bilgi__sutun-liste"><li><a href="#">İletişim</a></li><li><a href="#">CİMER başvurusu</a></li></ul></div>
    </div>
    <div class="trds-alt-bilgi__satir">
      <nav class="trds-alt-bilgi__baglantilar" aria-label="Alt bilgi bağlantıları"><a href="#">Erişilebilirlik bildirimi</a><a href="#">KVKK aydınlatma metni</a><a href="#">Çerez politikası</a></nav>
    </div>
    <p class="trds-alt-bilgi__telif">© 2026 Türkiye Cumhuriyeti</p>
  </div>
</footer>
```

**Temel**

```html
<footer class="trds-alt-bilgi">
  <div class="trds-kap">
    <div class="trds-alt-bilgi__satir">
      <nav class="trds-alt-bilgi__baglantilar" aria-label="Alt bilgi bağlantıları">
        <a href="#">Erişilebilirlik bildirimi</a>
        <a href="#">KVKK aydınlatma metni</a>
        <a href="#">Çerez politikası</a>
        <a href="#">Kullanım koşulları</a>
      </nav>
      <nav class="trds-dil" aria-label="Dil seçimi">
        <ul class="trds-dil__liste">
          <li><span aria-current="true" lang="tr">Türkçe</span></li>
          <li><a href="#" lang="en" hreflang="en">English</a></li>
        </ul>
      </nav>
    </div>
    <p class="trds-alt-bilgi__telif">© 2026 Türkiye Cumhuriyeti</p>
  </div>
</footer>
```

**React** (`import { AltBilgi } from '@tr-ds/react'`):

```jsx
<AltBilgi kurum="Sosyal Güvenlik Kurumu" ustKurum="Çalışma ve Sosyal Güvenlik Bakanlığı bağlı kuruluşudur." kisaAd="SGK" baglantilar={[{"ad":"KVKK"}]} />
```

**Vue** (`import { AltBilgi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAltBilgi kurum="Sosyal Güvenlik Kurumu" ustKurum="Çalışma ve Sosyal Güvenlik Bakanlığı bağlı kuruluşudur." kisaAd="SGK" :baglantilar='[{"ad":"KVKK"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Aşama afişi (Phase banner) — `phase-banner`

Group: Kimlik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: UK, IE.

Hizmetin deneme aşamasında olduğunu söyler ve geri bildirim ister. Başlık çubuğunun hemen altında durur.

When to use:
- Alfa ve beta aşamasında her sayfada kullanın.
- Hizmet canlıya geçince kaldırın.

Accessibility:
- Etiket ve metin aynı satırdadır. Geri bildirim bağlantısı yeni sekmede açılmaz.

**Beta**

```html
<div class="trds-asama">
  <div class="trds-kap trds-asama__ic"><span class="trds-etiket trds-etiket--mavi">Beta</span><p class="trds-asama__metin">Bu yeni bir hizmettir. <a class="trds-link" href="#">Geri bildiriminiz</a> onu iyileştirir.</p></div>
</div>
```

**React** (`import { AsamaAfisi } from '@tr-ds/react'`):

```jsx
<AsamaAfisi asama="Beta">Bu yeni bir hizmettir.</AsamaAfisi>
```

**Vue** (`import { AsamaAfisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAsamaAfisi asama="Beta">Bu yeni bir hizmettir.</TrdsAsamaAfisi>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Başlık (Header) — `header`

Group: Kimlik. Status: CSS stable, JS mobil menü, React stable, Vue stable. WCAG: 1.3.1, 2.1.2, 2.4.1, 4.1.2. Also in: UK, US, FR, IE, KR, JP, DK, DE.

Her sayfanın en üstünde durur, kurumu adlandırır ve ana menüyü taşır.

When to use:
- Her sayfada aynı biçimde kullanın.
- Resmî site afişinin hemen altında durur.

Accessibility:
- `<header>` ve `<nav>` etiketlerini kullanın.
- Mobil menü düğmesi `aria-expanded` taşır.
- Menü açıkken odak menü içinde kalır.

**Temel**

```html
<header class="trds-baslik-cubugu" data-trds="baslik-cubugu">
  <div class="trds-kap trds-baslik-cubugu__ic">
    <a class="trds-baslik-cubugu__marka" href="/">
      <img class="trds-baslik-cubugu__arma" src="/trds/turk-bayragi.svg" alt="" width="48" height="32">
      <span class="trds-baslik-cubugu__ad">Çalışma ve Sosyal Güvenlik Bakanlığı</span>
    </a>
    <button class="trds-baslik-cubugu__menu-dugmesi" type="button" aria-expanded="false" aria-controls="ana-menu">Menü</button>
    <nav class="trds-baslik-cubugu__menu" id="ana-menu" aria-label="Ana menü">
      <a href="#">Hizmetler</a>
      <a href="#">Duyurular</a>
      <a href="#">İletişim</a>
    </nav>
  </div>
</header>
```

**Kurumun kendi logosu ile**

```html
<header class="trds-baslik-cubugu" data-trds="baslik-cubugu">
  <div class="trds-kap trds-baslik-cubugu__ic">
    <a class="trds-baslik-cubugu__marka" href="/" aria-label="Sosyal Güvenlik Kurumu ana sayfa">
      <img class="trds-baslik-cubugu__logo trds-baslik-cubugu__logo--beyaz" src="/trds/kurumlar/sgk-beyaz.svg" alt="Sosyal Güvenlik Kurumu" width="1303" height="640">
    </a>
    <button class="trds-baslik-cubugu__menu-dugmesi" type="button" aria-expanded="false" aria-controls="ana-menu-2">Menü</button>
    <nav class="trds-baslik-cubugu__menu" id="ana-menu-2" aria-label="Ana menü">
      <a href="#">Çalışan</a>
      <a href="#">Emekli</a>
      <a href="#">İşveren</a>
      <a href="#">İletişim</a>
    </nav>
  </div>
</header>
```

**Oturum açık**

```html
<header class="trds-baslik-cubugu" data-trds="baslik-cubugu">
  <div class="trds-kap trds-baslik-cubugu__ic">
    <a class="trds-baslik-cubugu__marka" href="/"><img class="trds-baslik-cubugu__arma" src="/trds/turk-bayragi.svg" alt="" width="48" height="32"><span class="trds-baslik-cubugu__ad">Sosyal Güvenlik Kurumu</span></a>
    <div class="trds-baslik-cubugu__oturum"><span class="trds-avatar trds-avatar--kucuk" aria-hidden="true">AY</span><span class="trds-baslik-cubugu__kullanici">Ayşe Yılmaz</span><a class="trds-baslik-cubugu__cikis" href="#"><svg class="trds-simge" aria-hidden="true"><use href="#trds-logout"/></svg> Çıkış</a></div>
    <button class="trds-baslik-cubugu__menu-dugmesi" type="button" aria-expanded="false" aria-controls="ana-menu-3">Menü</button>
    <nav class="trds-baslik-cubugu__menu" id="ana-menu-3" aria-label="Ana menü"><a href="#">Başvurularım</a><a href="#">Belgelerim</a><a href="#">Mesajlar</a></nav>
  </div>
</header>
```

**React** (`import { BaslikCubugu } from '@tr-ds/react'`):

```jsx
<BaslikCubugu kurum="Çalışma ve Sosyal Güvenlik Bakanlığı" menu={[{"ad":"Hizmetler"},{"ad":"Duyurular"}]} />
```

**Vue** (`import { BaslikCubugu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBaslikCubugu kurum="Çalışma ve Sosyal Güvenlik Bakanlığı" :menu='[{"ad":"Hizmetler"},{"ad":"Duyurular"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Çerez bildirimi (Cookie banner) — `cookie-banner`

Group: Kimlik. Status: CSS stable, JS tercih saklama, React stable, Vue stable. WCAG: 1.3.1, 2.4.3, 3.2.2. Also in: UK, FR, DK, BR, CH, IT.

İsteğe bağlı çerezler için izin ister. Kabul ve ret eşit büyüklüktedir. Tercih saklanır.

When to use:
- Zorunlu olmayan bir çerez varsa her sitede kullanın. 5651 sayılı Kanun ve KVKK gereğidir.
- Yalnız zorunlu çerez varsa kullanmayın. Çerez sayfasına bağlantı yeter.

Accessibility:
- Afiş sayfanın en başındadır ve `role="region"` ile adlandırılır. Odağı çalmaz.
- Ret, kabul kadar kolaydır. Karanlık kalıp yoktur. Seçimden sonra afiş bir onay cümlesine döner.

Data attributes used by the markup: `data-hep-goster`, `data-cerez`, `data-cerez-metin`, `data-cerez-gizle`.

**Temel**

```html
<div class="trds-cerez" role="region" aria-label="Çerez tercihi" data-trds="cerez" data-hep-goster>
  <div class="trds-kap">
    <div class="trds-cerez__soru">
      <h2 class="trds-cerez__baslik">Bu sitede çerez kullanıyoruz</h2>
      <p class="trds-govde">Zorunlu çerezler siteyi çalıştırır. İsteğe bağlı çerezler kullanımı ölçer ve hizmeti iyileştirir.</p>
      <div class="trds-button-grubu"><button class="trds-button" type="button" data-cerez="kabul">İsteğe bağlı çerezleri kabul et</button><button class="trds-button" type="button" data-cerez="ret">İsteğe bağlı çerezleri reddet</button><a class="trds-link" href="#">Çerezleri görüntüle</a></div>
    </div>
    <div class="trds-cerez__onay" hidden>
      <p class="trds-govde"><span data-cerez-metin></span> Tercihinizi <a class="trds-link" href="#">çerez sayfasından</a> değiştirebilirsiniz.</p>
      <button class="trds-button trds-button--ikincil" type="button" data-cerez-gizle>Mesajı gizle</button>
    </div>
  </div>
</div>
```

**React** (`import { CerezBildirimi } from '@tr-ds/react'`):

```jsx
<CerezBildirimi baslik="Bu sitede çerez kullanıyoruz">Zorunlu çerezler siteyi çalıştırır.</CerezBildirimi>
```

**Vue** (`import { CerezBildirimi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsCerezBildirimi baslik="Bu sitede çerez kullanıyoruz">Zorunlu çerezler siteyi çalıştırır.</TrdsCerezBildirimi>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kurum logosu (Logo lockup) — `logo`

Group: Kimlik · unique to Türkiye. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.1.1, 2.4.4. Also in: DE, FR.

Kurumun kendi logosu, tek bir bağlantıda ve sabit yükseklikte. Logosu olmayan kurum için bayrak ve ad kilidi.

When to use:
- Her sayfada, aynı yerde kullanın. Ana sayfaya bağlanır.
- Kurumun resmî logosu varsa onu kullanın. Dosya kimlik paketinde durur ve yalnız o kurumu gösterir.
- Logo yoksa bayrak ve ad kilidini kullanın. Arma ile adın oranını değiştirmeyin.
- Koyu zeminde beyaz sürüm, açık zeminde renkli sürüm durur. Logoyu yeniden boyamayın.

Accessibility:
- Logo görseli kurum adını `alt` olarak taşır. Bağlantı metni odur.
- Bayrak ve ad kilidinde bayrak boş `alt` taşır. Kurum adı metindir, ekran okuyucu onu okur.
- Üst satır "T.C." her zaman yazılır. Bu bir kimlik işaretidir.

Why it exists: Kurumun işaretini ve adını aynı düzende gösterir. Logo boyutu, boşlukları ve açık veya koyu zemin seçeneklerini ortak kurallarla belirler.

**Kurumun kendi logosu**

```html
<div class="trds-logo-dizisi">
  <a class="trds-logo" href="/"><img class="trds-logo__gorsel" src="/trds/kurumlar/saglik-bakanligi-kirmizi.svg" alt="T.C. Sağlık Bakanlığı" width="1448" height="490"></a>
  <a class="trds-logo" href="/"><img class="trds-logo__gorsel" src="/trds/kurumlar/sgk.svg" alt="Sosyal Güvenlik Kurumu" width="150" height="73"></a>
  <a class="trds-logo" href="/"><img class="trds-logo__gorsel" src="/trds/e-devlet-isaret.png" alt="e-Devlet Kapısı" width="196" height="196"></a>
</div>
```

**Koyu zeminde beyaz sürüm**

```html
<div class="trds-logo-dizisi trds-logo-dizisi--koyu">
  <a class="trds-logo" href="/"><img class="trds-logo__gorsel" src="/trds/kurumlar/e-devlet-kapisi-beyaz.svg" alt="türkiye.gov.tr" width="250" height="60"></a>
  <a class="trds-logo" href="/"><img class="trds-logo__gorsel" src="/trds/kurumlar/sgk-beyaz.svg" alt="Sosyal Güvenlik Kurumu" width="1303" height="640"></a>
  <a class="trds-logo" href="/"><img class="trds-logo__gorsel" src="/trds/kurumlar/saglik-bakanligi-beyaz.svg" alt="T.C. Sağlık Bakanlığı" width="1448" height="490"></a>
</div>
```

**Bayrak ve ad kilidi**

```html
<a class="trds-logo" href="/">
  <img class="trds-logo__arma" src="/trds/turk-bayragi.svg" alt="" width="48" height="32">
  <span class="trds-logo__metin">
    <span class="trds-logo__ust">T.C.</span>
    <span class="trds-logo__ad">Sağlık Bakanlığı</span>
  </span>
</a>
```

**Bağlı kuruluş**

```html
<a class="trds-logo" href="/">
  <img class="trds-logo__arma" src="/trds/turk-bayragi.svg" alt="" width="48" height="32">
  <span class="trds-logo__metin">
    <span class="trds-logo__ust">T.C. Çalışma ve Sosyal Güvenlik Bakanlığı</span>
    <span class="trds-logo__ad">Sosyal Güvenlik Kurumu</span>
  </span>
</a>
```

**React** (`import { KurumLogosu } from '@tr-ds/react'`):

```jsx
<KurumLogosu src="/trds/kurumlar/sgk.svg" alt="Sosyal Güvenlik Kurumu" />
```

**Vue** (`import { KurumLogosu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKurumLogosu src="/trds/kurumlar/sgk.svg" alt="Sosyal Güvenlik Kurumu" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kurum tanıtıcısı (Identifier) — `identifier`

Group: Kimlik · unique to Türkiye. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.1.1, 1.3.1, 2.4.5. Also in: US, KR.

Sayfanın sonunda hizmeti işleten kurumu ve bağlı olduğu bakanlığı adlandırır.

When to use:
- Kurumsal alt bilgi ile birlikte kullanmayın. O alt bilgi kurum adını ve üst kurumu zaten taşır. Bir metin bir sayfada bir kez durur.
- Her sayfada alt bilginin hemen üstünde kullanın.
- Bağlı kuruluşlar üst kurumu da yazar.

Accessibility:
- Kurum adı görsel değil metindir. Logo yalnız metne eşlik eder.
- İletişim bağlantıları gerçek bağlantılardır.

Why it exists: Türkiye’de bir vatandaş bir hizmeti hangi kurumun yürüttüğünü çoğu zaman bilmez. Bu bileşen sorumluyu adlandırır ve şikâyet yolunu açar.

**Temel**

```html
<section class="trds-tanitici" aria-label="Kurum bilgisi">
  <div class="trds-kap trds-tanitici__ic">
    <div>
      <p class="trds-tanitici__kurum">Sosyal Güvenlik Kurumu</p>
      <p class="trds-tanitici__ust">Çalışma ve Sosyal Güvenlik Bakanlığı bağlı kuruluşudur.</p>
    </div>
    <nav class="trds-tanitici__baglantilar" aria-label="Kurum bağlantıları">
      <a href="#">Hakkımızda</a>
      <a href="#">Bilgi edinme</a>
      <a href="#">CİMER</a>
    </nav>
  </div>
</section>
```

**React** (`import { KurumTanitici } from '@tr-ds/react'`):

```jsx
<KurumTanitici kurum="Sosyal Güvenlik Kurumu" ustKurum="Çalışma ve Sosyal Güvenlik Bakanlığı bağlı kuruluşudur." />
```

**Vue** (`import { KurumTanitici } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKurumTanitici kurum="Sosyal Güvenlik Kurumu" ustKurum="Çalışma ve Sosyal Güvenlik Bakanlığı bağlı kuruluşudur." />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Resmî site afişi (Masthead) — `masthead`

Group: Kimlik · unique to Türkiye. Status: CSS stable, JS aç ve kapat, React stable, Vue stable. WCAG: 1.3.1, 2.4.1, 4.1.2. Also in: US, KR.

Sayfanın en üstünde durur ve kullanıcıya bu sitenin resmî bir devlet sitesi olduğunu söyler.

When to use:
- Her kamu sayfasında, her zaman en üstte kullanın.
- Metni değiştirmeyin. Tek bir metin her sitede aynıdır.
- Bu bileşen `@tr-ds/identity` paketindedir ve kısıtlı lisans taşır.

Accessibility:
- Ekran okuyucu için sayfanın ilk bölgesidir.
- "Nasıl anlarım?" bölümü bir düğme ile açılır ve `aria-expanded` taşır.
- Bayrak, Türk Bayrağı Tüzüğü ölçülerine göre çizilmiştir. Boş `alt` taşır, çünkü anlamı yanındaki metin verir.

Why it exists: Resmî hizmetlerde kurum kimliğini ve alan adını kontrol etme yolunu gösterir. Afiş tek başına sitenin gerçekliğini kanıtlamaz.

**Temel**

```html
<div class="trds-resmi-afis" data-trds="resmi-afis">
  <div class="trds-kap trds-resmi-afis__ic">
    <img class="trds-resmi-afis__bayrak" src="/trds/turk-bayragi.svg" alt="">
    <p class="trds-resmi-afis__metin">Bu, Türkiye Cumhuriyeti’ne ait resmî bir devlet sitesidir.</p>
    <button class="trds-resmi-afis__dugme" type="button" aria-expanded="false" aria-controls="afis-nasil">Nasıl anlarım?</button>
  </div>
  <div class="trds-resmi-afis__panel" id="afis-nasil" hidden>
    <div class="trds-kap trds-resmi-afis__panel-ic">
      <div>
        <p class="trds-resmi-afis__panel-baslik">Adres <strong>gov.tr</strong> ile biter</p>
        <p>Resmî devlet siteleri gov.tr uzantısını kullanır. Adres çubuğunu her zaman kontrol edin.</p>
      </div>
      <div>
        <p class="trds-resmi-afis__panel-baslik">Bağlantı <strong>güvenlidir</strong></p>
        <p>Adresin başında https ve kilit simgesi bulunur. Kimlik bilgilerinizi yalnız böyle sayfalara girin.</p>
      </div>
    </div>
  </div>
</div>
```

**React** (`import { ResmiAfis } from '@tr-ds/react'`):

```jsx
<ResmiAfis />
```

**Vue** (`import { ResmiAfis } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsResmiAfis />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sekme simgesi (Favicon) — `favicon`

Group: Kimlik. Status: CSS yok, JS yok, React yok, Vue yok. WCAG: 2.4.2. Also in: KR.

Tarayıcı sekmesinde ve yer imlerinde duran küçük simge. Kurumun arması, iki boyutta.

When to use:
- Her sitede kullanın. 32×32 ve 180×180 piksel. SVG de eklenir.
- Simge kurumun kendi armasıdır. Bayrak tek başına kullanılmaz.

Accessibility:
- Sekme simgesi bir bilgi taşımaz. Sayfa başlığı taşır. `<title>` kurum adıyla biter.

**İki boyut**

```html
<div class="trds-u-satir">
  <img src="/trds/e-devlet-isaret.png" alt="" width="32" height="32">
  <img src="/trds/e-devlet-isaret.png" alt="" width="64" height="64">
  <code class="trds-kod">&lt;link rel="icon" href="/trds/e-devlet-isaret.png" type="image/png"&gt;</code>
</div>
```

No React or Vue wrapper: this part is a set of classes or a file, use the HTML directly.

## Gezinme

Kullanıcının nerede olduğunu ve nereye gidebileceğini gösteren parçalar.

### Adım göstergesi (Step indicator) — `step-indicator`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.8. Also in: US, FR, IE, KR, JP, DK.

Çok adımlı bir akışta kullanıcının kaçıncı adımda olduğunu gösterir.

When to use:
- Üç ile yedi adım arasındaki akışlarda kullanın.
- Adım sayısı belirsizse kullanmayın.

Accessibility:
- Geçerli adım `aria-current="step"` taşır.
- Metin "3 / 5" biçiminde de yazılır. Görsel gösterge tek başına yeterli değildir.

**Temel**

```html
<nav class="trds-adimlar" aria-label="Başvuru adımları">
  <p class="trds-adimlar__sayi">Adım 2 / 4</p>
  <ol class="trds-adimlar__liste">
    <li class="trds-adimlar__oge trds-adimlar__oge--bitti">Kimlik doğrulama</li>
    <li class="trds-adimlar__oge trds-adimlar__oge--etkin" aria-current="step">Başvuru bilgileri</li>
    <li class="trds-adimlar__oge">Belgeler</li>
    <li class="trds-adimlar__oge">Onay</li>
  </ol>
</nav>
```

**React** (`import { AdimGostergesi } from '@tr-ds/react'`):

```jsx
<AdimGostergesi adimlar={["Kimlik doğrulama","Başvuru bilgileri","Belgeler"]} etkin={2} />
```

**Vue** (`import { AdimGostergesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAdimGostergesi :adimlar='["Kimlik doğrulama","Başvuru bilgileri","Belgeler"]' :etkin='2' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Alt gezinme (Bottom navigation) — `bottom-navigation`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.5.8, 2.4.7. Also in: IT, JP, KR.

Telefonda ekranın altına yapışan üç ile beş ana bölüm. Uygulama benzeri hizmetler için.

When to use:
- Kullanıcının sık geçtiği üç ile beş bölüm varsa kullanın.
- Masaüstünde gizlenir, başlık çubuğu menüsü görünür.

Accessibility:
- `<nav aria-label="Ana bölümler">`. Geçerli bölüm `aria-current="page"` taşır.
- Her öge simge ve metin taşır. Yalnız simge yeterli değildir. Dokunma alanı 44 pikseldir.

**Dört bölüm**

```html
<nav class="trds-alt-gezinme" aria-label="Ana bölümler">
  <a href="#" aria-current="page"><svg class="trds-simge" aria-hidden="true"><use href="#trds-home"/></svg><span>Ana sayfa</span></a>
  <a href="#"><svg class="trds-simge" aria-hidden="true"><use href="#trds-search"/></svg><span>Ara</span></a>
  <a href="#"><svg class="trds-simge" aria-hidden="true"><use href="#trds-date"/></svg><span>Randevu</span></a>
  <a href="#"><svg class="trds-simge" aria-hidden="true"><use href="#trds-user"/></svg><span>Hesabım</span></a>
</nav>
```

**React** (`import { AltGezinme } from '@tr-ds/react'`):

```jsx
<AltGezinme ogeler={[{"ad":"Ana sayfa","simge":"home","etkin":true},{"ad":"Ara","simge":"search"}]} />
```

**Vue** (`import { AltGezinme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAltGezinme :ogeler='[{"ad":"Ana sayfa","simge":"home","etkin":true},{"ad":"Ara","simge":"search"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Arama (Search) — `search`

Group: Gezinme. Status: CSS stable, JS öneri listesi, React stable, Vue stable. WCAG: 1.3.1, 2.4.6, 4.1.2. Also in: US, FR, JP, DK, DE, NO.

Site içi arama alanı ve düğmesi. İsteğe bağlı öneri listesi: kullanıcı yazarken gruplu sonuçlar açılır.

When to use:
- Sayfa sayısı 30 üstündeki her sitede kullanın.
- Bir formun içindeki listeyi süzmek için aranabilir liste kullanın, arama değil.
- Öneri listesini `data-trds="arama"` açar. Kaynak bir sunucu adresi (`data-kaynak`) veya sayfadaki bir JSON betiğidir (`data-kaynak-id`). İki karakterden sonra, 200 ms bekleyip sorar.
- Öneriler gruplanır: hizmetler, kurumlar, belediyeler. Her gruptan en çok beş satır. Son satır tam aramaya gider.

Accessibility:
- `<form role="search">` ile ekran okuyucu bölgeyi bulur.
- Etiket görsel olarak gizlidir ama vardır. Yer tutucu etiket yerine geçmez.
- Düğme metni "Ara" olur. Yalnız büyüteç simgesi yeterli değildir.
- Öneri listesi ARIA 1.2 combobox kalıbıdır: `role="listbox"`, gruplar `role="group"`, satırlar `role="option"`. Ok tuşları gezer, Escape kapatır, dışarı tıklama kapatır.
- Canlı bölge öneri sayısını söyler: "12 öneri, 3 grupta". Eşleşen parça `<mark>` ile kalındır, yalnız renkle gösterilmez.
- Betik yüklenmezse veya sunucu geç kalırsa form düz arama olarak çalışır. Enter her zaman formu gönderir.

Data attributes used by the markup: `data-kaynak-id`, `data-trds-durum`.

**Temel**

```html
<form class="trds-arama" role="search" action="#" method="get">
  <label class="trds-etiket trds-gorsel-gizli" for="ara">Sitede ara</label>
  <input class="trds-girdi trds-arama__girdi" id="ara" name="q" type="search" placeholder="Sitede ara" autocomplete="off">
  <button class="trds-button trds-arama__dugme" type="submit">Ara</button>
</form>
```

**Öneri listesi ile**

```html
<div class="trds-arama-onerileri" data-trds="arama" data-kaynak-id="oneri-verisi">
  <form class="trds-arama" role="search" action="#" method="get">
    <label class="trds-etiket trds-gorsel-gizli" for="ara3">Hizmet, kurum veya belediye ara</label>
    <input class="trds-girdi trds-arama__girdi" id="ara3" name="q" type="search" role="combobox" aria-autocomplete="list" aria-expanded="false" aria-controls="ara3-liste" autocomplete="off" placeholder="Örnek: ikamet">
    <button class="trds-button trds-arama__dugme" type="submit">Ara</button>
  </form>
  <div class="trds-arama-onerileri__liste" id="ara3-liste" role="listbox" aria-label="Öneriler" hidden></div>
  <p class="trds-gorsel-gizli" data-trds-durum aria-live="polite"></p>
  <script type="application/json" id="oneri-verisi">{"gruplar": [{"ad": "Hizmetler", "ogeler": [{"ad": "İkametgâh Belgesi Sorgulama", "ek": "Nüfus ve Vatandaşlık İşleri Genel Müdürlüğü", "href": "#"}, {"ad": "Adli Sicil Kaydı Sorgulama", "ek": "Adalet Bakanlığı", "href": "#"}, {"ad": "Vergi Borcu Sorgulama ve Ödeme", "ek": "Gelir İdaresi Başkanlığı", "href": "#"}, {"ad": "SGK Tescil ve Hizmet Dökümü", "ek": "Sosyal Güvenlik Kurumu", "href": "#"}]}, {"ad": "Kurumlar", "ogeler": [{"ad": "Nüfus ve Vatandaşlık İşleri Genel Müdürlüğü", "href": "#"}, {"ad": "Adalet Bakanlığı", "href": "#"}]}, {"ad": "Belediyeler", "ogeler": [{"ad": "Ankara Büyükşehir Belediyesi", "href": "#"}, {"ad": "İzmir Büyükşehir Belediyesi", "href": "#"}]}]}</script>
</div>
```

**Başlık çubuğunda**

```html
<form class="trds-arama trds-arama--koyu" role="search" action="#" method="get">
  <label class="trds-etiket trds-gorsel-gizli" for="ara2">Hizmet ara</label>
  <input class="trds-girdi trds-arama__girdi" id="ara2" name="q" type="search" placeholder="Hizmet ara" autocomplete="off">
  <button class="trds-button trds-arama__dugme" type="submit">Ara</button>
</form>
```

**React** (`import { Arama } from '@tr-ds/react'`):

```jsx
<Arama etiket="Sitede ara" />
```

**Vue** (`import { Arama } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsArama etiket="Sitede ara" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Başa dön (Back to top) — `back-to-top`

Group: Gezinme. Status: CSS stable, JS görünürlük, React stable, Vue stable. WCAG: 2.4.1, 2.3.3. Also in: IT, JP, DK, FR, CH.

Uzun sayfada iki ekran aşağı inince beliren bağlantı. Başa kaydırır ve odağı ana içeriğe verir.

When to use:
- Üç ekrandan uzun sayfalarda kullanın.
- Form adımlarında kullanmayın. Adımlar kısa olur.

Accessibility:
- Bir bağlantıdır, `#ana-icerik` hedefine gider. Betik yoksa da çalışır.
- Hareket azaltma isteyen kullanıcıda kaydırma anlıktır.

Data attributes used by the markup: `data-hep-goster`.

**Temel**

```html
<a class="trds-basa-don" href="#ana-icerik" data-trds="basa-don" data-hep-goster><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-back"/></svg><span>Başa dön</span></a>
```

**React** (`import { BasaDon } from '@tr-ds/react'`):

```jsx
<BasaDon />
```

**Vue** (`import { BasaDon } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBasaDon />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Dil seçici (Language selector) — `language-selector`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 3.1.1, 3.1.2, 2.4.4. Also in: UK, US, KR, JP, DK.

Kullanıcının içerik dilini değiştirmesini sağlar. Her dil kendi adıyla ve kendi yazısıyla yazılır.

When to use:
- Hizmetin birden çok dilde sürümü varsa kullanın. Türkçe bilmeyen yerleşikler ve turistler için önemlidir.
- Bayrak kullanmayın. Bayrak bir ülkedir, dil değildir.

Accessibility:
- Her bağlantı `lang` ve `hreflang` taşır. Ekran okuyucu doğru telaffuzla okur.
- Geçerli dil `aria-current="true"` taşır ve bağlantı değildir.
- Sağdan sola diller `dir="rtl"` taşır.

**Temel**

```html
<nav class="trds-dil" aria-label="Dil seçimi">
  <ul class="trds-dil__liste">
    <li><span aria-current="true" lang="tr">Türkçe</span></li>
    <li><a href="?dil=en" lang="en" hreflang="en">English</a></li>
    <li><a href="?dil=ar" lang="ar" hreflang="ar" dir="rtl">العربية</a></li>
    <li><a href="?dil=ku" lang="ku" hreflang="ku">Kurdî</a></li>
    <li><a href="?dil=ru" lang="ru" hreflang="ru">Русский</a></li>
  </ul>
</nav>
```

**React** (`import { DilSecici } from '@tr-ds/react'`):

```jsx
<DilSecici diller={[{"kod":"tr","ad":"Türkçe","etkin":true},{"kod":"en","ad":"English","href":"?dil=en"}]} />
```

**Vue** (`import { DilSecici } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsDilSecici :diller='[{"kod":"tr","ad":"Türkçe","etkin":true},{"kod":"en","ad":"English","href":"?dil=en"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Geniş menü (Mega menu) — `mega-menu`

Group: Gezinme. Status: CSS stable, JS aç ve kapat, React stable, Vue stable. WCAG: 2.1.1, 2.4.3, 4.1.2. Also in: IT, JP, CA.

Başlık çubuğunun altında, her ana konu için tam genişlikte bir bağlantı paneli. Bir kerede bir panel açık.

When to use:
- Kurumun 30 üstü hizmeti ve dört ile altı ana konusu varsa kullanın.
- Bir hizmetin içinde kullanmayın. Orada hizmet gezinmesi vardır.

Accessibility:
- Her konu bir düğmedir: `aria-expanded` ve `aria-controls`. Escape paneli kapatır ve odağı düğmeye verir.
- Panel bağlantıları sütun başlıkları ile gruplanır. Sütun sayısı üçü geçmez, sütun başına bağlantı sekizi geçmez.
- Dar ekranda paneller alt alta açılır.

**İki konu**

```html
<nav class="trds-genis-menu" aria-label="Ana konular" data-trds="genis-menu">
  <div class="trds-kap trds-genis-menu__ic">
    <button class="trds-genis-menu__dugme" type="button" aria-expanded="false" aria-controls="gm-calisan">Çalışan</button>
    <button class="trds-genis-menu__dugme" type="button" aria-expanded="false" aria-controls="gm-emekli">Emekli</button>
    <a class="trds-genis-menu__dugme" href="#">İletişim</a>
  </div>
  <div class="trds-genis-menu__panel" id="gm-calisan" hidden>
    <div class="trds-kap trds-genis-menu__sutunlar">
      <div><p class="trds-genis-menu__baslik">Sigortalılık</p><ul class="trds-genis-menu__liste"><li><a href="#">Hizmet dökümü</a></li><li><a href="#">İşe giriş bildirgesi</a></li><li><a href="#">4A, 4B, 4C</a></li></ul></div>
      <div><p class="trds-genis-menu__baslik">Sağlık</p><ul class="trds-genis-menu__liste"><li><a href="#">Provizyon sorgulama</a></li><li><a href="#">Rapor işlemleri</a></li></ul></div>
      <div><p class="trds-genis-menu__baslik">Ödemeler</p><ul class="trds-genis-menu__liste"><li><a href="#">Geçici iş göremezlik</a></li><li><a href="#">Emzirme ödeneği</a></li></ul></div>
    </div>
  </div>
  <div class="trds-genis-menu__panel" id="gm-emekli" hidden>
    <div class="trds-kap trds-genis-menu__sutunlar">
      <div><p class="trds-genis-menu__baslik">Aylık</p><ul class="trds-genis-menu__liste"><li><a href="#">Ne zaman emekli olurum?</a></li><li><a href="#">Maaş bilgisi</a></li><li><a href="#">Bayram ikramiyesi</a></li></ul></div>
      <div><p class="trds-genis-menu__baslik">Başvuru</p><ul class="trds-genis-menu__liste"><li><a href="#">Yaşlılık aylığı</a></li><li><a href="#">Ölüm aylığı</a></li></ul></div>
    </div>
  </div>
</nav>
```

**React** (`import { GenisMenu } from '@tr-ds/react'`):

```jsx
<GenisMenu konular={[{"ad":"Çalışan","sutunlar":[{"baslik":"Sigortalılık","baglantilar":[{"ad":"Hizmet dökümü"}]}]},{"ad":"İletişim","href":"#"}]} />
```

**Vue** (`import { GenisMenu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsGenisMenu :konular='[{"ad":"Çalışan","sutunlar":[{"baslik":"Sigortalılık","baglantilar":[{"ad":"Hizmet dökümü"}]}]},{"ad":"İletişim","href":"#"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Geri bağlantısı (Back link) — `back-link`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.4, 3.2.3. Also in: UK, KR, DK.

Çok adımlı bir akışta kullanıcıyı bir önceki adıma götürür.

When to use:
- Her form adımında kullanın.
- Sayfa başlığının üstünde durur.
- Tarayıcının geri düğmesine güvenmeyin. Girilen veri kaybolabilir.

Accessibility:
- Gerçek bir bağlantıdır. JavaScript ile `history.back()` çağırmaz.
- Metin her zaman "Geri" olur.

**Temel**

```html
<a class="trds-geri" href="#">Geri</a>
```

**Geri ve ileri**

```html
<nav class="trds-geri-ileri" aria-label="Sayfa sırası">
  <a class="trds-geri" href="#">Önceki: Kişisel bilgiler</a>
  <a class="trds-ileri" href="#">Sonraki: Banka bilgileri</a>
</nav>
```

**React** (`import { GeriBaglantisi } from '@tr-ds/react'`):

```jsx
<GeriBaglantisi href="#">Geri</GeriBaglantisi>
```

**Vue** (`import { GeriBaglantisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsGeriBaglantisi href="#">Geri</TrdsGeriBaglantisi>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Hizmet gezinmesi (Service navigation) — `service-navigation`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.7, 2.4.8. Also in: UK, FI, FR.

Bir hizmetin adını ve kendi bölümlerini taşıyan ikinci çubuk. Başlık çubuğu kurumu, bu çubuk hizmeti söyler.

When to use:
- Bir hizmetin üç ile altı bölümü varsa kullanın.
- Tek sayfalık bir hizmette kullanmayın.

Accessibility:
- `<nav aria-label="Hizmet menüsü">` ile bölge olur. Geçerli sayfa `aria-current="page"` taşır.
- Hizmet adı bağlantıdır ve hizmetin ana sayfasına gider.

**Temel**

```html
<div class="trds-hizmet-gezinme">
  <div class="trds-kap trds-hizmet-gezinme__ic">
    <a class="trds-hizmet-gezinme__ad" href="#">Emeklilik başvurusu</a>
    <nav aria-label="Hizmet menüsü"><ul class="trds-hizmet-gezinme__liste"><li><a href="#" aria-current="page">Başvuru</a></li><li><a href="#">Belgeler</a></li><li><a href="#">Durum</a></li><li><a href="#">Yardım</a></li></ul></nav>
  </div>
</div>
```

**React** (`import { HizmetGezinmesi } from '@tr-ds/react'`):

```jsx
<HizmetGezinmesi hizmet="Emeklilik başvurusu" ogeler={[{"ad":"Başvuru","etkin":true},{"ad":"Belgeler"}]} />
```

**Vue** (`import { HizmetGezinmesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsHizmetGezinmesi hizmet="Emeklilik başvurusu" :ogeler='[{"ad":"Başvuru","etkin":true},{"ad":"Belgeler"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### İçeriğe atlama bağlantısı (Skip link) — `skip-link`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.1, 2.4.7. Also in: UK, FR, KR, DK, NO.

Klavye kullanıcısının menüyü atlayıp doğrudan ana içeriğe gitmesini sağlar.

When to use:
- Her sayfada kullanın. İstisnası yoktur.
- Sayfadaki ilk odaklanabilir öğe olur.

Accessibility:
- Odak almadan gizlidir. Odak alınca görünür olur.
- `display: none` kullanmayın. O zaman klavye de bulamaz.
- Hedef öğe `tabindex="-1"` taşır, yoksa odak taşınmaz.
- Ölçüm: 16 Türk bakanlık sitesinin 15 tanesinde bu bağlantı yok.

**Temel**

```html
<a class="trds-atla" href="#ana-icerik">Ana içeriğe geç</a>
<main id="ana-icerik" tabindex="-1">
  <h1 class="trds-baslik trds-baslik--1">Sayfa başlığı</h1>
</main>
```

**React** (`import { IceriyeAtla } from '@tr-ds/react'`):

```jsx
<IceriyeAtla />
```

**Vue** (`import { IceriyeAtla } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIceriyeAtla />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sayfa içi gezinme (In-page navigation) — `in-page-navigation`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.1, 2.4.5. Also in: US, KR, JP, DK.

Uzun bir sayfanın bölümlerini listeler ve her birine bağlantı verir.

When to use:
- Sayfa 4 ekrandan uzunsa ve 3 üstünde bölüm başlığı varsa kullanın.
- Mevzuat, kılavuz ve sıkça sorulan sorular sayfalarında kullanın.

Accessibility:
- Bağlantı hedefi bir başlıktır ve `id` taşır.
- Liste sıralıdır, `<ol>`. Sıra bilgi taşır.
- Sabit konumlu sürüm klavye odağını kapatmaz.

**Temel**

```html
<nav class="trds-icindekiler" aria-labelledby="ic-baslik">
  <h2 class="trds-icindekiler__baslik" id="ic-baslik">Bu sayfada</h2>
  <ol class="trds-icindekiler__liste">
    <li><a href="#kosullar">Başvuru koşulları</a></li>
    <li><a href="#belgeler">Gerekli belgeler</a></li>
    <li><a href="#sure">Süreç ve süre</a></li>
    <li><a href="#itiraz">İtiraz yolu</a></li>
  </ol>
</nav>
```

**React** (`import { SayfaIciGezinme } from '@tr-ds/react'`):

```jsx
<SayfaIciGezinme ogeler={[{"ad":"Başvuru koşulları","href":"#kosullar"},{"ad":"Gerekli belgeler","href":"#belgeler"}]} />
```

**Vue** (`import { SayfaIciGezinme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSayfaIciGezinme :ogeler='[{"ad":"Başvuru koşulları","href":"#kosullar"},{"ad":"Gerekli belgeler","href":"#belgeler"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sayfa yolu (Breadcrumb) — `breadcrumb`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.8. Also in: UK, US, FR, IE, KR, JP, DK, NO.

Kullanıcının site yapısında nerede olduğunu gösterir ve üst seviyeye dönmeyi sağlar.

When to use:
- Üç seviyeden derin site yapılarında kullanın.
- Adım adım ilerleyen bir hizmet akışında kullanmayın. Orada geri bağlantısı kullanın.

Accessibility:
- `<nav aria-label="Sayfa yolu">` içine alın.
- Son öğe bağlantı değildir ve `aria-current="page"` taşır.
- Ayraç karakteri CSS ile eklenir, metne yazılmaz.

**Temel**

```html
<nav class="trds-yol" aria-label="Sayfa yolu">
  <ol class="trds-yol__liste">
    <li><a href="/">Ana sayfa</a></li>
    <li><a href="#">Hizmetler</a></li>
    <li><span aria-current="page">Emeklilik başvurusu</span></li>
  </ol>
</nav>
```

**React** (`import { SayfaYolu } from '@tr-ds/react'`):

```jsx
<SayfaYolu ogeler={[{"ad":"Ana sayfa","href":"/"},{"ad":"Hizmetler","href":"#"},{"ad":"Emeklilik başvurusu"}]} />
```

**Vue** (`import { SayfaYolu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSayfaYolu :ogeler='[{"ad":"Ana sayfa","href":"/"},{"ad":"Hizmetler","href":"#"},{"ad":"Emeklilik başvurusu"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sayfalama (Pagination) — `pagination`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.8, 2.5.8, 4.1.2. Also in: UK, US, FR, IE, KR, JP, DK, NO.

Uzun bir listeyi sayfalara böler ve sayfalar arasında gezinmeyi sağlar.

When to use:
- Bir listede 20 satırdan çok kayıt varsa kullanın.
- Bir hizmet akışında adım göstergesi kullanın, sayfalama kullanmayın.

Accessibility:
- `<nav aria-label="Sayfalama">` içine alın.
- Geçerli sayfa `aria-current="page"` taşır.
- Her bağlantı 44 x 44 piksel dokunma alanına sahiptir.

**Temel**

```html
<nav class="trds-sayfalama" aria-label="Sayfalama">
  <a class="trds-sayfalama__yon" href="#">Önceki</a>
  <a href="#">1</a>
  <a href="#" aria-current="page">2</a>
  <a href="#">3</a>
  <a class="trds-sayfalama__yon" href="#">Sonraki</a>
</nav>
```

**React** (`import { Sayfalama } from '@tr-ds/react'`):

```jsx
<Sayfalama sayfa={2} toplam={3} />
```

**Vue** (`import { Sayfalama } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSayfalama :sayfa='2' :toplam='3' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yan menü (Side navigation) — `side-navigation`

Group: Gezinme. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.7, 2.4.8. Also in: US, FR, IE, KR, DK.

Bir bölümün alt sayfaları arasında gezinmeyi sağlar. İçeriğin solunda durur.

When to use:
- Bir bölümde 4 ile 15 arasında sayfa varsa kullanın.
- Adım adım bir akışta kullanmayın. Orada adım göstergesi kullanın.

Accessibility:
- `<nav aria-label>` ile adlandırılır. Bir sayfada birden çok nav varsa adlar farklı olur.
- Geçerli sayfa `aria-current="page"` taşır.
- Küçük ekranda menü içeriğin üstüne geçer, gizlenmez.

**İki düzey**

```html
<nav class="trds-yan-menu" aria-label="Emeklilik bölümü">
  <ul class="trds-yan-menu__liste">
    <li><a href="#">Genel bilgi</a></li>
    <li><a href="#" aria-current="page">Başvuru</a>
      <ul>
        <li><a href="#">Gerekli belgeler</a></li>
        <li><a href="#">Ücretler</a></li>
      </ul>
    </li>
    <li><a href="#">Sıkça sorulan sorular</a></li>
    <li><a href="#">İletişim</a></li>
  </ul>
</nav>
```

**React** (`import { YanMenu } from '@tr-ds/react'`):

```jsx
<YanMenu etiket="Emeklilik bölümü" ogeler={[{"ad":"Genel bilgi","href":"#"},{"ad":"Başvuru","href":"#","etkin":true,"alt":[{"ad":"Gerekli belgeler","href":"#"}]}]} />
```

**Vue** (`import { YanMenu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsYanMenu etiket="Emeklilik bölümü" :ogeler='[{"ad":"Genel bilgi","href":"#"},{"ad":"Başvuru","href":"#","etkin":true,"alt":[{"ad":"Gerekli belgeler","href":"#"}]}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

## Form

Kullanıcıdan bilgi alan parçalar.

### Açılır liste (Select) — `select`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 3.3.2, 4.1.2. Also in: UK, US, FR, IE, KR, JP, DK, DE, NO.

Kullanıcının bir listeden tek bir seçenek seçmesini sağlar.

When to use:
- Seçenek sayısı 7 ile 20 arasındaysa kullanın.
- 6 seçenekten azsa seçenek düğmesi kullanın.
- 20 seçenekten çoksa aranabilir liste kullanın.

Accessibility:
- İlk seçenek bir yönerge değil boş bir değer olur.
- Seçenekleri alfabetik değil mantıklı bir sırayla dizin.
- Türkçe sıralamada ç, ğ, ı, i, ö, ş ve ü harflerini doğru sıraya koyun.

**Temel**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="il">İl</label>
  <select class="trds-secim" id="il" name="il">
    <option value="">Seçiniz</option>
    <option value="34">İstanbul</option>
    <option value="06">Ankara</option>
    <option value="35">İzmir</option>
    <option value="16">Bursa</option>
  </select>
</div>
```

**React** (`import { AcilirListe } from '@tr-ds/react'`):

```jsx
<AcilirListe etiket="İl" secenekler={[{"deger":"06","ad":"Ankara"},{"deger":"34","ad":"İstanbul"}]} />
```

**Vue** (`import { AcilirListe } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAcilirListe etiket="İl" :secenekler='[{"deger":"06","ad":"Ankara"},{"deger":"34","ad":"İstanbul"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Adet seçici (Quantity stepper) — `quantity`

Group: Form. Status: CSS stable, JS artır, azalt, React stable, Vue stable. WCAG: 2.1.1, 4.1.2. Also in: KR, IT.

Eksi ve artı düğmeli sayı alanı. Küçük sayılar için: bilet, kişi, adet.

When to use:
- 1 ile 20 arası bir adet için kullanın.
- Büyük sayılar için sayı girişi kullanın.

Accessibility:
- Alan yazılabilir. Düğmeler `aria-label` taşır: "Bir azalt", "Bir artır".
- Sınırda düğme devre dışı kalır. Değer `aria-live` ile duyurulmaz, alan zaten odaktadır.

Data attributes used by the markup: `data-adet`.

**Temel**

```html
<div class="trds-alan" data-trds="adet">
  <label class="trds-etiket" for="kisi-sayisi">Kişi sayısı</label>
  <div class="trds-adet"><button class="trds-adet__dugme" type="button" data-adet="eksi" aria-label="Bir azalt">−</button><input class="trds-girdi trds-adet__girdi" id="kisi-sayisi" name="kisi" type="text" inputmode="numeric" value="2" min="1" max="8"><button class="trds-adet__dugme" type="button" data-adet="arti" aria-label="Bir artır">+</button></div>
</div>
```

**React** (`import { AdetSecici } from '@tr-ds/react'`):

```jsx
<AdetSecici etiket="Kişi sayısı" enAz={1} enCok={8} deger={2} />
```

**Vue** (`import { AdetSecici } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAdetSecici etiket="Kişi sayısı" :enAz='1' :enCok='8' :deger='2' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Alan grubu (Fieldset) — `fieldset`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 3.3.2. Also in: UK, DE, NO.

Birbirine bağlı form alanlarını tek bir soru altında toplar.

When to use:
- Onay kutusu veya seçenek düğmesi grubunda her zaman kullanın.
- Tarih gibi çok parçalı bir girişte kullanın.

Accessibility:
- `legend` sorunun kendisidir. Ekran okuyucu her alanla birlikte okur.
- Sayfada tek soru varsa `legend` sayfa başlığı ile aynı olur.

**Temel**

```html
<fieldset class="trds-alan-grubu">
  <legend class="trds-baslik-legend trds-baslik-legend--buyuk">Doğum tarihiniz nedir?</legend>
  <p class="trds-yardim">Örnek: 27 03 1997</p>
</fieldset>
```

**React** (`import { AlanGrubu } from '@tr-ds/react'`):

```jsx
<AlanGrubu etiket="Adres">Alanlar</AlanGrubu>
```

**Vue** (`import { AlanGrubu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAlanGrubu etiket="Adres">Alanlar</TrdsAlanGrubu>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Aralık seçici (Range slider) — `range`

Group: Form. Status: CSS stable, JS değer gösterimi, React stable, Vue stable. WCAG: 1.3.1, 4.1.2. Also in: US, FR, NL, KR.

Bir aralıktan yaklaşık bir sayı seçtirir. Seçilen değer her zaman yazıyla görünür.

When to use:
- Kesin sayı gerekmeyen yerde kullanın: yarıçap, bütçe aralığı.
- Kesin sayı için sayı girişi kullanın.

Accessibility:
- Yerel `<input type="range">`. Ok tuşları değeri değiştirir. Değer `<output>` ile bağlıdır.
- Uçlar yazıyla belirtilir. Yalnız kaydırıcı yeterli değildir.

Data attributes used by the markup: `data-birim`.

**Temel**

```html
<div class="trds-alan trds-aralik" data-trds="aralik" data-birim=" km">
  <label class="trds-etiket" for="uzaklik">En çok uzaklık</label>
  <p class="trds-yardim" id="uzaklik-yardim">1 ile 50 kilometre arası.</p>
  <div class="trds-aralik__satir"><input class="trds-aralik__girdi" id="uzaklik" name="uzaklik" type="range" min="1" max="50" step="1" value="10" aria-describedby="uzaklik-yardim"><output class="trds-aralik__deger" for="uzaklik">10 km</output></div>
  <div class="trds-aralik__uclar" aria-hidden="true"><span>1 km</span><span>50 km</span></div>
</div>
```

**React** (`import { AralikSecici } from '@tr-ds/react'`):

```jsx
<AralikSecici etiket="En çok uzaklık" enAz={1} enCok={50} deger={10} birim=" km" />
```

**Vue** (`import { AralikSecici } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAralikSecici etiket="En çok uzaklık" :enAz='1' :enCok='50' :deger='10' birim=" km" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Çok satırlı metin (Textarea) — `textarea`

Group: Form. Status: CSS stable, JS karakter sayacı, React stable, Vue stable. WCAG: 1.3.1, 3.3.1, 4.1.3. Also in: UK, IE, KR, JP, DK, DE, NO.

Kullanıcının birden çok satır metin girmesini sağlar.

When to use:
- Açıklama, gerekçe veya adres gibi uzun bir metin için kullanın.
- Bir sınır varsa karakter sayacını açın.

Accessibility:
- Karakter sayacı `aria-live="polite"` ile duyurulur.
- Sayaç yalnız son 20 karakterde duyurulur. Her tuşta duyurmayın.
- Alanı kullanıcı büyütebilir. `resize: none` kullanmayın.

Data attributes used by the markup: `data-sinir`.

**Karakter sayacı ile**

```html
<div class="trds-alan" data-trds="karakter-sayaci" data-sinir="200">
  <label class="trds-etiket" for="aciklama">Başvuru gerekçeniz</label>
  <textarea class="trds-metin-alani" id="aciklama" name="aciklama" rows="5" aria-describedby="aciklama-sayac"></textarea>
  <p class="trds-sayac" id="aciklama-sayac" aria-live="polite">200 karakter kaldı</p>
</div>
```

**React** (`import { MetinAlani } from '@tr-ds/react'`):

```jsx
<MetinAlani etiket="Başvuru gerekçeniz" enCok={200} />
```

**Vue** (`import { MetinAlani } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsMetinAlani etiket="Başvuru gerekçeniz" :enCok='200' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Dosya yükleme (File upload) — `file-upload`

Group: Form. Status: CSS stable, JS sürükle, listele, kaldır, React stable, Vue stable. WCAG: 1.3.1, 2.1.1, 3.3.2, 4.1.3. Also in: UK, US, FR, IE, KR, JP, DK, DE.

Kullanıcının cihazından bir veya daha çok dosya seçmesini sağlar. Sürükle ve bırak alanı ve dosya listesi ile.

When to use:
- Belge, fotoğraf veya form eki istediğinizde kullanın.
- Kabul edilen biçimi ve en büyük boyutu etiketin altında yazın.

Accessibility:
- Yerel girdi görünmez ama vardır ve odak alır. Klavye ve ekran okuyucu onu kullanır. Sürükle ve bırak tek yol değildir.
- Seçilen her dosya adı ve boyutu ile listelenir. Her satırda kaldırma düğmesi vardır.
- Seçim `aria-live` ile duyurulur: "2 dosya seçildi".
- Kabul edilen biçim ve en büyük boyut yardım metninde yazılır. Sunucu yine denetler.

**Temel**

```html
<div class="trds-alan" data-trds="dosya">
  <label class="trds-etiket" for="ek">İkametgâh belgesi</label>
  <p class="trds-yardim" id="ek-yardim">PDF veya JPG. En çok 5 MB.</p>
  <div class="trds-dosya-alani">
    <input class="trds-dosya" id="ek" name="ek" type="file" accept=".pdf,.jpg,.jpeg" aria-describedby="ek-yardim">
    <span class="trds-button trds-button--ikincil trds-dosya-alani__dugme" aria-hidden="true">Dosya seç</span>
    <p class="trds-dosya-alani__metin">veya dosyayı buraya sürükleyin</p>
  </div>
  <ul class="trds-dosya-liste" aria-label="Seçilen dosyalar"></ul>
  <p class="trds-dosya-durum trds-gorsel-gizli" aria-live="polite"></p>
</div>
```

**React** (`import { DosyaYukleme } from '@tr-ds/react'`):

```jsx
<DosyaYukleme etiket="İkametgâh belgesi" yardim="PDF veya JPG. En çok 5 MB." kabul=".pdf,.jpg" />
```

**Vue** (`import { DosyaYukleme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsDosyaYukleme etiket="İkametgâh belgesi" yardim="PDF veya JPG. En çok 5 MB." kabul=".pdf,.jpg" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### E-posta girişi (Email input) — `email-input`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.5, 3.3.1, 3.3.2. Also in: DE.

E-posta adresi için tek satırlık alan. Telefonda @ tuşlu klavye açılır.

When to use:
- Kullanıcıya e-posta ile döneceğiniz her formda kullanın.
- Doğrulama gerekiyorsa adresi ikinci kez yazdırmayın. Onay kodu gönderin.

Accessibility:
- `type="email"` ve `autocomplete="email"` kullanılır. Tarayıcı doğru klavyeyi ve öneriyi getirir.
- Büyük harf ve boşluk sunucuda temizlenir. Kullanıcı yazdığı için hata almaz.
- `spellcheck="false"` ile tarayıcı adresin altını çizmez.

**Temel**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="eposta2">E-posta adresiniz</label>
  <p class="trds-yardim" id="eposta2-yardim">Başvuru sonucunu bu adrese göndereceğiz.</p>
  <input class="trds-girdi" id="eposta2" name="eposta" type="email" autocomplete="email" spellcheck="false" aria-describedby="eposta2-yardim">
</div>
```

**React** (`import { EPostaGirisi } from '@tr-ds/react'`):

```jsx
<EPostaGirisi etiket="E-posta adresiniz" />
```

**Vue** (`import { EPostaGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsEPostaGirisi etiket="E-posta adresiniz" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Girdi grubu (Input group) — `input-group`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 3.3.2. Also in: US, DE.

Bir alanın başına veya sonuna birim, para işareti veya sabit metin ekler.

When to use:
- Tutar, yüzde, ağırlık veya alan gibi birimli bir değer isterken kullanın.
- Eki alan içine yazmayın. Kullanıcı silebilir ve ekran okuyucu okuyamaz.

Accessibility:
- Ek `aria-hidden="true"` taşır. Birim etiket veya yardım metninde yazılır.
- Ek, alanla aynı yükseklikte ve aynı kenarlıkta durur.

**Sonek**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="tutar">Tutar</label>
  <p class="trds-yardim" id="tutar-yardim">Türk Lirası olarak, kuruşsuz.</p>
  <div class="trds-girdi-grubu">
    <input class="trds-girdi trds-girdi--10" id="tutar" name="tutar" type="text" inputmode="numeric" aria-describedby="tutar-yardim">
    <span class="trds-girdi-grubu__ek" aria-hidden="true">₺</span>
  </div>
</div>
```

**Önek**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="oran">Katkı oranı</label>
  <div class="trds-girdi-grubu">
    <span class="trds-girdi-grubu__ek" aria-hidden="true">%</span>
    <input class="trds-girdi trds-girdi--4" id="oran" name="oran" type="text" inputmode="decimal">
  </div>
</div>
```

**React** (`import { GirdiGrubu } from '@tr-ds/react'`):

```jsx
<GirdiGrubu etiket="Tutar" sonek="₺" genislik="10" />
```

**Vue** (`import { GirdiGrubu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsGirdiGrubu etiket="Tutar" sonek="₺" genislik="10" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Girdi maskesi (Input mask) — `input-mask`

Group: Form. Status: CSS stable, JS biçimlendirme, React stable, Vue stable. WCAG: 3.3.2, 3.3.3. Also in: US.

Yazarken boşluk ve ayraçları kendisi koyar. Kart numarası, referans numarası.

When to use:
- Sabit biçimli bir kod için kullanın. Kalıp `#` ile rakam yerini gösterir.
- T.C. kimlik, IBAN, telefon ve plaka için kendi bileşenlerini kullanın. Onlar doğrular da.

Accessibility:
- Yardım metni biçimi örnekle söyler. Maske betik yoksa da alan çalışır.
- Sunucu ham rakamları alır. Kullanıcı boşluklu yazsa da hata almaz.

Data attributes used by the markup: `data-maske`.

**Referans numarası**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="ref">Başvuru referans numarası</label>
  <p class="trds-yardim" id="ref-yardim">12 hane. Örnek: 2026 0450 1234</p>
  <input class="trds-girdi trds-girdi--20" id="ref" name="ref" type="text" inputmode="numeric" maxlength="14" data-trds="maske" data-maske="#### #### ####" aria-describedby="ref-yardim">
</div>
```

**React** (`import { GirdiMaskesi } from '@tr-ds/react'`):

```jsx
<GirdiMaskesi etiket="Referans numarası" maske="#### #### ####" yardim="Örnek: 2026 0450 1234" />
```

**Vue** (`import { GirdiMaskesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsGirdiMaskesi etiket="Referans numarası" maske="#### #### ####" yardim="Örnek: 2026 0450 1234" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Metin girişi (Text input) — `text-input`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 1.3.5, 3.3.1, 3.3.2, 4.1.2. Also in: UK, US, FR, IE, KR, JP, DK, DE, NO.

Kullanıcının tek satırlık kısa bir metin girmesini sağlar.

When to use:
- Ad, e-posta veya başvuru numarası gibi kısa bir değer için kullanın.
- Beklenen uzunluğa göre alanı daraltın. Posta kodu alanı ad alanı kadar geniş olmaz.
- Uzun metin için çok satırlı metin alanını kullanın.

Accessibility:
- Her alanın görünür bir etiketi olur. Yer tutucu metni etiket yerine geçmez.
- Yardım metnini `aria-describedby` ile alana bağlayın.
- Hata durumunda kenarlık 2 pikselden kalın olur ve hata metni alanın üstünde durur.
- Türkçe adlar için alan genişliğini 60 karakterden aşağı düşürmeyin.

**Etiket ve yardım metni ile**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="ad">Adınız</label>
  <p class="trds-yardim" id="ad-yardim">Nüfus cüzdanınızda yazdığı gibi yazın.</p>
  <input class="trds-girdi" id="ad" name="ad" type="text" aria-describedby="ad-yardim" autocomplete="given-name">
</div>
```

**Hata durumu**

```html
<div class="trds-alan trds-alan--hata">
  <label class="trds-etiket" for="eposta">E-posta adresiniz</label>
  <p class="trds-hata" id="eposta-hata"><span class="trds-gorsel-gizli">Hata:</span> E-posta adresi bir @ işareti içermelidir.</p>
  <input class="trds-girdi trds-girdi--hata" id="eposta" name="eposta" type="email" aria-describedby="eposta-hata">
</div>
```

**React** (`import { MetinGirisi } from '@tr-ds/react'`):

```jsx
<MetinGirisi etiket="Adınız" yardim="Kimliğinizde yazdığı gibi." />
```

**Vue** (`import { MetinGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsMetinGirisi etiket="Adınız" yardim="Kimliğinizde yazdığı gibi." />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Parola girişi (Password input) — `password-input`

Group: Form. Status: CSS stable, JS göster ve gizle, React stable, Vue stable. WCAG: 1.3.5, 3.3.2, 4.1.2. Also in: UK, FR, IE, DE.

Parola alanı. Kullanıcı yazdığını görmek için "Göster" düğmesine basar.

When to use:
- Parola veya şifre isterken kullanın.
- T.C. kimlik numarası için kullanmayın. O bir parola değildir.

Accessibility:
- "Göster" düğmesi `aria-controls` ile alana bağlıdır ve durumunu `aria-pressed` ile söyler.
- Alan gösterilirken de `autocomplete` çalışır. Parola yöneticisi bozulmaz.
- Caps Lock açıkken uyarı yalnız ekranda gösterilir, ekran okuyucu tuş durumunu zaten bildirir.

**Temel**

```html
<div class="trds-alan" data-trds="parola">
  <label class="trds-etiket" for="parola">Parolanız</label>
  <div class="trds-parola">
    <input class="trds-girdi" id="parola" name="parola" type="password" autocomplete="current-password" spellcheck="false">
    <button class="trds-button trds-button--ikincil trds-parola__dugme" type="button" aria-controls="parola" aria-pressed="false">Göster</button>
  </div>
</div>
```

**React** (`import { ParolaGirisi } from '@tr-ds/react'`):

```jsx
<ParolaGirisi etiket="Parolanız" />
```

**Vue** (`import { ParolaGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsParolaGirisi etiket="Parolanız" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Saat girişi (Time input) — `time-input`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 3.3.2. Also in: FI, IT, US.

Saat ve dakika için iki kısa alan. Tarih girişiyle aynı kalıp.

When to use:
- Randevu saati veya olay saati isterken kullanın.
- Saat seçici açılır liste kullanmayın. Yazmak daha hızlıdır.

Accessibility:
- Alan grubu `<fieldset>` ve `<legend>` ile etiketlenir.
- 24 saat biçimi kullanılır. Yardım metni "Örnek: 14 30" der.

**Temel**

```html
<fieldset class="trds-alan-grubu">
  <legend class="trds-baslik-legend">Randevu saati</legend>
  <p class="trds-yardim" id="saat-yardim">24 saat biçimi. Örnek: 14 30</p>
  <div class="trds-saat" aria-describedby="saat-yardim">
    <div class="trds-saat__parca"><label class="trds-etiket trds-etiket--kucuk" for="saat">Saat</label><input class="trds-girdi trds-girdi--2" id="saat" name="saat" type="text" inputmode="numeric" maxlength="2"></div>
    <span class="trds-saat__ayrac" aria-hidden="true">:</span>
    <div class="trds-saat__parca"><label class="trds-etiket trds-etiket--kucuk" for="dakika">Dakika</label><input class="trds-girdi trds-girdi--2" id="dakika" name="dakika" type="text" inputmode="numeric" maxlength="2"></div>
  </div>
</fieldset>
```

**React** (`import { SaatGirisi } from '@tr-ds/react'`):

```jsx
<SaatGirisi etiket="Randevu saati" />
```

**Vue** (`import { SaatGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSaatGirisi etiket="Randevu saati" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sayı girişi (Number input) — `number-input`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.5, 3.3.2. Also in: DE.

Sayı için tek satırlık alan. Metin türü ve sayısal klavye kullanır, sayı türü kullanmaz.

When to use:
- Adet, yıl veya tutar gibi bir sayı isterken kullanın.
- Kimlik numarası, plaka veya posta kodu için kullanmayın. Onlar sayı değil koddur.

Accessibility:
- `type="text"` ve `inputmode="numeric"` kullanılır. `type="number"` fare tekerleğiyle değeri değiştirir ve ekran okuyucuda sorun çıkarır. GOV.UK aynı kararı verdi.
- Ondalık için `inputmode="decimal"` kullanın. Türkçede ondalık ayracı virgüldür, sunucu ikisini de kabul eder.
- Alan genişliği beklenen hane sayısına göre daraltılır.

**Tam sayı**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="kisi">Hanede yaşayan kişi sayısı</label>
  <input class="trds-girdi trds-girdi--2" id="kisi" name="kisi" type="text" inputmode="numeric" pattern="[0-9]*">
</div>
```

**Ondalık**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="alan">Arsa alanı</label>
  <p class="trds-yardim" id="alan-yardim">Metrekare. Ondalık için virgül kullanın: 245,5</p>
  <div class="trds-girdi-grubu">
    <input class="trds-girdi trds-girdi--10" id="alan" name="alan" type="text" inputmode="decimal" aria-describedby="alan-yardim">
    <span class="trds-girdi-grubu__ek" aria-hidden="true">m²</span>
  </div>
</div>
```

**React** (`import { SayiGirisi } from '@tr-ds/react'`):

```jsx
<SayiGirisi etiket="Kişi sayısı" genislik="2" />
```

**Vue** (`import { SayiGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSayiGirisi etiket="Kişi sayısı" genislik="2" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Takvim (Date picker) — `date-picker`

Group: Form. Status: CSS stable, JS ay ızgarası ve klavye, React stable, Vue stable. WCAG: 1.3.1, 2.1.1, 4.1.2. Also in: NL, IT, JP, DK, NO, FI, US, BR, KR, CH.

Tarih alanının yanında bir takvim düğmesi. Alan yazılabilir kalır, takvim yardımcıdır.

When to use:
- Kullanıcının bilmediği bir tarihi seçtirirken kullanın: randevu, gelecek bir gün.
- Doğum tarihi gibi bilinen tarih için tarih girişi kullanın. Yazmak daha hızlıdır.

Accessibility:
- Alan tek başına çalışır. Takvim betik ile gelir ve `aria-expanded` taşıyan bir düğme ile açılır.
- Izgara `role="grid"`. Ok tuşları gün, Page Up ve Page Down ay, Shift ile yıl değiştirir. Enter seçer, Escape kapatır.
- Ay ve yıl listesi başlıkta durur. Uzun geçmişe giden bir tarih için ok düğmesi yetmez.
- Her gün düğmesi tam tarihi söyler: "14 Eylül 2026 Pazartesi". Bugün ve seçili gün yalnız renkle değil, kalın ve çerçeve ile belli olur.

Data attributes used by the markup: `data-en-az`, `data-en-cok`.

**Randevu tarihi**

```html
<div class="trds-alan trds-takvim" data-trds="takvim" data-en-az="2026-09-07" data-en-cok="2026-12-31">
  <label class="trds-etiket" for="randevu">Randevu tarihi</label>
  <p class="trds-yardim" id="randevu-yardim">GG.AA.YYYY. Örnek: 14.09.2026</p>
  <div class="trds-takvim__satir">
    <input class="trds-girdi trds-girdi--10" id="randevu" name="randevu" type="text" inputmode="numeric" autocomplete="off" aria-describedby="randevu-yardim">
    <button class="trds-button trds-button--ikincil trds-takvim__ac" type="button" aria-expanded="false" aria-label="Takvimi aç"><svg class="trds-simge" aria-hidden="true"><use href="#trds-date"/></svg></button>
  </div>
  <div class="trds-takvim__panel" role="dialog" aria-label="Takvim" hidden></div>
</div>
```

**Geçmiş bir tarih**

```html
<div class="trds-alan trds-takvim" data-trds="takvim" data-en-cok="2026-09-07">
  <label class="trds-etiket" for="belge-tarih">Belge tarihi</label>
  <p class="trds-yardim" id="belge-tarih-yardim">GG.AA.YYYY. Örnek: 12.03.1998</p>
  <div class="trds-takvim__satir">
    <input class="trds-girdi trds-girdi--10" id="belge-tarih" name="belge_tarih" type="text" inputmode="numeric" autocomplete="off" aria-describedby="belge-tarih-yardim">
    <button class="trds-button trds-button--ikincil trds-takvim__ac" type="button" aria-expanded="false" aria-label="Takvimi aç"><svg class="trds-simge" aria-hidden="true"><use href="#trds-date"/></svg></button>
  </div>
  <div class="trds-takvim__panel" role="dialog" aria-label="Takvim" hidden></div>
</div>
```

**React** (`import { Takvim } from '@tr-ds/react'`):

```jsx
<Takvim etiket="Randevu tarihi" enAz="2026-09-07" />
```

**Vue** (`import { Takvim } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsTakvim etiket="Randevu tarihi" enAz="2026-09-07" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Web adresi girişi (URL input) — `url-input`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.5, 3.3.2. Also in: DE.

Web adresi için tek satırlık alan. Telefonda nokta ve eğik çizgi tuşlu klavye açılır.

When to use:
- Kurumsal başvurularda web sitesi isterken kullanın.

Accessibility:
- `type="url"` ve `inputmode="url"` kullanılır.
- Başta https:// olmadan yazılan adres reddedilmez. Sunucu ekler.

**Temel**

```html
<div class="trds-alan">
  <label class="trds-etiket" for="site">Kurumunuzun web sitesi</label>
  <p class="trds-yardim" id="site-yardim">Örnek: ornek.com.tr</p>
  <input class="trds-girdi" id="site" name="site" type="url" inputmode="url" autocomplete="url" spellcheck="false" aria-describedby="site-yardim">
</div>
```

**React** (`import { WebAdresiGirisi } from '@tr-ds/react'`):

```jsx
<WebAdresiGirisi etiket="Web sitesi" />
```

**Vue** (`import { WebAdresiGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsWebAdresiGirisi etiket="Web sitesi" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yardım metni (Hint) — `hint`

Group: Form. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.3, 3.3.2. Also in: UK, IE, DK, NO.

Bir alanın nasıl doldurulacağını kısaca anlatır.

When to use:
- Beklenen biçim açık değilse kullanın.
- Yardım metnini alanın üstüne koyun. Kullanıcı yazmadan önce okur.

Accessibility:
- Alana `aria-describedby` ile bağlanır.
- Metin gri olsa da kontrast oranı 4.5 üzerinde kalır.

**Temel**

```html
<p class="trds-yardim">Nüfus cüzdanınızın ön yüzünde yazan 11 haneli numara.</p>
```

**React** (`import { YardimMetni } from '@tr-ds/react'`):

```jsx
<YardimMetni id="ad-yardim">Kimliğinizde yazdığı gibi.</YardimMetni>
```

**Vue** (`import { YardimMetni } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsYardimMetni id="ad-yardim">Kimliğinizde yazdığı gibi.</TrdsYardimMetni>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

## Seçim

Kullanıcının seçenekler arasından seçim yapmasını sağlayan parçalar.

### Anahtar (Switch) — `switch`

Group: Seçim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.5.8, 4.1.2. Also in: FR, KR, JP, DK, NO.

Bir ayarı açar veya kapatır. Etkisi hemen görülür, form gönderimi beklemez.

When to use:
- Bildirim ayarı gibi anında etkili bir açık ve kapalı seçimi için kullanın.
- Bir formun parçası olan seçim için onay kutusu kullanın. Anahtar gönderim beklemez.

Accessibility:
- Yerel bir onay kutusu `role="switch"` taşır. JavaScript gerekmez, klavye ve ekran okuyucu tarayıcıdan gelir.
- Etiket durumu yazmaz. "Bildirim gönder" der, "açık" veya "kapalı" demez. Durumu tarayıcı okur.
- Anahtar en az 44 piksel dokunma alanına sahiptir.

**Temel**

```html
<div class="trds-anahtar">
  <input class="trds-anahtar__girdi" id="bildirim" name="bildirim" type="checkbox" role="switch">
  <label class="trds-anahtar__etiket" for="bildirim">
    <span class="trds-anahtar__yol" aria-hidden="true"></span>
    <span>Başvuru durumu değişince SMS gönder</span>
  </label>
</div>
```

**React** (`import { Anahtar } from '@tr-ds/react'`):

```jsx
<Anahtar etiket="Başvuru durumu değişince SMS gönder" ad="bildirim" />
```

**Vue** (`import { Anahtar } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAnahtar etiket="Başvuru durumu değişince SMS gönder" ad="bildirim" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Aranabilir liste (Combobox) — `combobox`

Group: Seçim. Status: CSS stable, JS süzme ve klavye, React stable, Vue stable. WCAG: 1.3.1, 2.1.1, 4.1.2, 4.1.3. Also in: US, FR, IE, JP, NO.

Uzun bir listeden yazarak seçim yaptırır. 81 il ve 973 ilçe için açılır liste yerine bunu kullanın.

When to use:
- Seçenek sayısı 20 üstündeyse kullanın.
- Kullanıcı seçeneği bilmiyorsa kullanmayın. O zaman seçenek düğmesi veya açılır liste daha iyidir.
- JavaScript yoksa alan düz bir metin girişi olarak kalır ve sunucu değeri doğrular.

Accessibility:
- ARIA 1.2 combobox kalıbı: `role="combobox"`, `aria-expanded`, `aria-controls`, `aria-activedescendant`.
- Yukarı ve aşağı ok seçenekler arasında gezinir. Enter seçer. Escape kapatır.
- Eşleşen seçenek sayısı `aria-live` ile duyurulur: "5 sonuç".
- Türkçe harf duyarsız süzer: "i" ile "İ", "ı" ile "I" eşleşir.

Data attributes used by the markup: `data-deger`.

**İl seçimi**

```html
<div class="trds-alan" data-trds="aranabilir">
  <label class="trds-etiket" for="il-ara">İl</label>
  <p class="trds-yardim" id="il-ara-yardim">Yazmaya başlayın ve listeden seçin.</p>
  <div class="trds-aranabilir">
    <input class="trds-girdi" id="il-ara" name="il" type="text" role="combobox" aria-expanded="false" aria-controls="il-liste" aria-autocomplete="list" autocomplete="off" aria-describedby="il-ara-yardim">
    <ul class="trds-aranabilir__liste" id="il-liste" role="listbox" aria-label="İller" hidden>
      <li role="option" id="il-01" data-deger="01">Adana</li>
      <li role="option" id="il-06" data-deger="06">Ankara</li>
      <li role="option" id="il-07" data-deger="07">Antalya</li>
      <li role="option" id="il-16" data-deger="16">Bursa</li>
      <li role="option" id="il-21" data-deger="21">Diyarbakır</li>
      <li role="option" id="il-27" data-deger="27">Gaziantep</li>
      <li role="option" id="il-32" data-deger="32">Isparta</li>
      <li role="option" id="il-34" data-deger="34">İstanbul</li>
      <li role="option" id="il-35" data-deger="35">İzmir</li>
      <li role="option" id="il-42" data-deger="42">Konya</li>
      <li role="option" id="il-63" data-deger="63">Şanlıurfa</li>
    </ul>
  </div>
  <p class="trds-aranabilir__durum trds-gorsel-gizli" aria-live="polite"></p>
</div>
```

**React** (`import { AranabilirListe } from '@tr-ds/react'`):

```jsx
<AranabilirListe etiket="İl" secenekler={[{"deger":"06","ad":"Ankara"},{"deger":"34","ad":"İstanbul"}]} />
```

**Vue** (`import { AranabilirListe } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAranabilirListe etiket="İl" :secenekler='[{"deger":"06","ad":"Ankara"},{"deger":"34","ad":"İstanbul"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Bölümlü seçim (Segmented control) — `segmented-control`

Group: Seçim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1, 4.1.2. Also in: NO, FR.

İki ile dört seçenek, yan yana ve tek satırda. Görünüm veya sıralama seçmek için.

When to use:
- Liste ve harita gibi iki görünüm arasında geçiş için kullanın.
- Beş üstü seçenek için seçenek düğmesi veya açılır liste kullanın.

Accessibility:
- Seçenek düğmesi kalıbıdır: `radio` ve `label`. Ok tuşları gezer.
- Seçili olan yalnız renkle değil, kalın metinle de belli olur.

**Üç seçenek**

```html
<fieldset class="trds-bolumlu">
  <legend class="trds-gorsel-gizli">Görünüm</legend>
  <input class="trds-gorsel-gizli" type="radio" name="gorunum" id="gorunum-1" value="liste" checked><label for="gorunum-1">Liste</label>
  <input class="trds-gorsel-gizli" type="radio" name="gorunum" id="gorunum-2" value="harita"><label for="gorunum-2">Harita</label>
  <input class="trds-gorsel-gizli" type="radio" name="gorunum" id="gorunum-3" value="takvim"><label for="gorunum-3">Takvim</label>
</fieldset>
```

**React** (`import { BolumluSecim } from '@tr-ds/react'`):

```jsx
<BolumluSecim etiket="Görünüm" ad="gorunum" secenekler={[{"deger":"liste","ad":"Liste"},{"deger":"harita","ad":"Harita"}]} deger="liste" />
```

**Vue** (`import { BolumluSecim } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBolumluSecim etiket="Görünüm" ad="gorunum" :secenekler='[{"deger":"liste","ad":"Liste"},{"deger":"harita","ad":"Harita"}]' deger="liste" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Çoklu seçim (Multi-select) — `multi-select`

Group: Seçim. Status: CSS stable, JS süzme, seçim etiketi, React stable, Vue stable. WCAG: 1.3.1, 4.1.2, 4.1.3. Also in: FI, IE, FR, CH.

Uzun bir listeden birden çok seçim. Yazarak süzülür, seçilenler etiket olur.

When to use:
- Seçenek sayısı 10 üstündeyse ve birden çok seçim gerekiyorsa kullanın.
- Seçenek 10 altındaysa onay kutusu kullanın.

Accessibility:
- ARIA 1.2 combobox ve `aria-multiselectable` liste. Enter seçer ve kaldırır, Backspace son etiketi kaldırır.
- Yerel bir `<select multiple>` formu taşır. Betik yoksa yalnız o görünür ve tek başına çalışır. Betik onu gizler ve zenginleştirilmiş alanı açar.
- Seçilenler canlı bölgeden duyurulur.

Data attributes used by the markup: `data-deger`, `data-trds-durum`.

**İller**

```html
<div class="trds-alan trds-coklu" data-trds="coklu-secim">
  <label class="trds-etiket" for="iller" id="iller-etiket">Hizmet verilen iller</label>
  <p class="trds-yardim" id="iller-yardim">Yazın ve listeden seçin. Birden çok il seçebilirsiniz.</p>
  <div class="trds-coklu__secilenler" aria-label="Seçilen iller" hidden></div>
  <input class="trds-girdi" id="iller" type="text" role="combobox" hidden aria-autocomplete="list" aria-expanded="false" aria-controls="iller-liste" aria-describedby="iller-yardim" autocomplete="off">
  <div class="trds-coklu__liste" id="iller-liste" role="listbox" aria-multiselectable="true" aria-label="İller" hidden>
    <div class="trds-coklu__oge" role="option" id="il-06" data-deger="06" aria-selected="true">Ankara</div><div class="trds-coklu__oge" role="option" id="il-34" data-deger="34" aria-selected="false">İstanbul</div><div class="trds-coklu__oge" role="option" id="il-35" data-deger="35" aria-selected="false">İzmir</div><div class="trds-coklu__oge" role="option" id="il-16" data-deger="16" aria-selected="false">Bursa</div><div class="trds-coklu__oge" role="option" id="il-07" data-deger="07" aria-selected="false">Antalya</div><div class="trds-coklu__oge" role="option" id="il-42" data-deger="42" aria-selected="false">Konya</div><div class="trds-coklu__oge" role="option" id="il-01" data-deger="01" aria-selected="false">Adana</div><div class="trds-coklu__oge" role="option" id="il-27" data-deger="27" aria-selected="false">Gaziantep</div>
  </div>
  <p class="trds-gorsel-gizli" data-trds-durum aria-live="polite"></p>
  <select class="trds-secim trds-coklu__yerel" id="iller-yerel" name="iller" multiple aria-labelledby="iller-etiket"><option value="06" selected>Ankara</option><option value="34">İstanbul</option><option value="35">İzmir</option><option value="16">Bursa</option><option value="07">Antalya</option><option value="42">Konya</option><option value="01">Adana</option><option value="27">Gaziantep</option></select>
</div>
```

**React** (`import { CokluSecim } from '@tr-ds/react'`):

```jsx
<CokluSecim etiket="İller" ad="iller" secenekler={[{"deger":"06","ad":"Ankara","secili":true},{"deger":"34","ad":"İstanbul"}]} />
```

**Vue** (`import { CokluSecim } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsCokluSecim etiket="İller" ad="iller" :secenekler='[{"deger":"06","ad":"Ankara","secili":true},{"deger":"34","ad":"İstanbul"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Onay kutusu (Checkbox) — `checkbox`

Group: Seçim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.5.8, 4.1.2. Also in: UK, US, FR, IE, KR, JP, DK, DE, NO.

Kullanıcının bir listeden birden çok seçenek seçmesini sağlar.

When to use:
- Birden çok seçim mümkünse kullanın.
- Tek bir evet ve hayır sorusu için tek onay kutusu kullanın.
- Seçenekler birbirini dışlıyorsa seçenek düğmesi kullanın.

Accessibility:
- Etiketin tamamı tıklanabilir alandır.
- Kutu 24 x 24 piksel, dokunma alanı 44 x 44 pikseldir.
- Grubu `fieldset` ve `legend` içine alın.

**Grup**

```html
<fieldset class="trds-alan-grubu">
  <legend class="trds-baslik-legend">Hangi belgeleri yükleyeceksiniz?</legend>
  <div class="trds-secenekler">
    <div class="trds-secenek">
      <input class="trds-onay" id="b1" name="belge" type="checkbox" value="kimlik">
      <label class="trds-secenek-etiket" for="b1">Kimlik fotokopisi</label>
    </div>
    <div class="trds-secenek">
      <input class="trds-onay" id="b2" name="belge" type="checkbox" value="ikametgah">
      <label class="trds-secenek-etiket" for="b2">İkametgâh belgesi</label>
    </div>
  </div>
</fieldset>
```

**React** (`import { OnayKutusu } from '@tr-ds/react'`):

```jsx
<OnayKutusu etiket="Hangi belgeleri yükleyeceksiniz?" ad="belge" secenekler={[{"deger":"kimlik","ad":"Kimlik fotokopisi"},{"deger":"ikametgah","ad":"İkametgâh belgesi"}]} />
```

**Vue** (`import { OnayKutusu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsOnayKutusu etiket="Hangi belgeleri yükleyeceksiniz?" ad="belge" :secenekler='[{"deger":"kimlik","ad":"Kimlik fotokopisi"},{"deger":"ikametgah","ad":"İkametgâh belgesi"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Puanlama (Rating) — `rating`

Group: Seçim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.1.1, 4.1.2. Also in: IT, IE.

Bir hizmeti 1 ile 5 arasında puanlar. Beş seçenek düğmesi, yıldız görünümünde.

When to use:
- Hizmet sonunda memnuniyet sorusu için kullanın.
- Puanı zorunlu yapmayın. Kullanıcı atlayabilir.

Accessibility:
- Beş `radio` ve bir `legend`. Ekran okuyucu "5 üzerinden 3" okur.
- Klavye ile ok tuşları puanı değiştirir. Odak halkası yıldızda görünür.

**Beş yıldız**

```html
<fieldset class="trds-puan">
  <legend class="trds-baslik-legend">Bu hizmetten ne kadar memnun kaldınız?</legend>
  <div class="trds-puan__yildizlar">
    <input class="trds-gorsel-gizli" type="radio" name="puan" id="puan-5" value="5"><label for="puan-5"><svg class="trds-simge trds-puan__bos" aria-hidden="true"><use href="#trds-star"/></svg><svg class="trds-simge trds-puan__dolu" aria-hidden="true"><use href="#trds-star2"/></svg><span class="trds-gorsel-gizli">5 üzerinden 5</span></label>
    <input class="trds-gorsel-gizli" type="radio" name="puan" id="puan-4" value="4" checked><label for="puan-4"><svg class="trds-simge trds-puan__bos" aria-hidden="true"><use href="#trds-star"/></svg><svg class="trds-simge trds-puan__dolu" aria-hidden="true"><use href="#trds-star2"/></svg><span class="trds-gorsel-gizli">5 üzerinden 4</span></label>
    <input class="trds-gorsel-gizli" type="radio" name="puan" id="puan-3" value="3"><label for="puan-3"><svg class="trds-simge trds-puan__bos" aria-hidden="true"><use href="#trds-star"/></svg><svg class="trds-simge trds-puan__dolu" aria-hidden="true"><use href="#trds-star2"/></svg><span class="trds-gorsel-gizli">5 üzerinden 3</span></label>
    <input class="trds-gorsel-gizli" type="radio" name="puan" id="puan-2" value="2"><label for="puan-2"><svg class="trds-simge trds-puan__bos" aria-hidden="true"><use href="#trds-star"/></svg><svg class="trds-simge trds-puan__dolu" aria-hidden="true"><use href="#trds-star2"/></svg><span class="trds-gorsel-gizli">5 üzerinden 2</span></label>
    <input class="trds-gorsel-gizli" type="radio" name="puan" id="puan-1" value="1"><label for="puan-1"><svg class="trds-simge trds-puan__bos" aria-hidden="true"><use href="#trds-star"/></svg><svg class="trds-simge trds-puan__dolu" aria-hidden="true"><use href="#trds-star2"/></svg><span class="trds-gorsel-gizli">5 üzerinden 1</span></label>
  </div>
</fieldset>
```

**React** (`import { Puanlama } from '@tr-ds/react'`):

```jsx
<Puanlama etiket="Bu hizmetten ne kadar memnun kaldınız?" ad="puan" deger={4} />
```

**Vue** (`import { Puanlama } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsPuanlama etiket="Bu hizmetten ne kadar memnun kaldınız?" ad="puan" :deger='4' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Seçenek düğmesi (Radio) — `radio`

Group: Seçim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.1.1, 4.1.2. Also in: UK, US, FR, IE, KR, JP, DK, DE, NO.

Kullanıcının birbirini dışlayan seçeneklerden birini seçmesini sağlar.

When to use:
- Seçenekler birbirini dışlıyorsa kullanın.
- 6 seçeneğe kadar kullanın. Daha fazlası için açılır liste kullanın.
- Hiçbir seçenek varsayılan olarak seçili gelmez.

Accessibility:
- Grubu `fieldset` ve `legend` içine alın.
- Ok tuşları grup içinde gezinir. Bu tarayıcının kendi davranışıdır. Bozmayın.
- Koşullu alan açan bir seçenek `aria-expanded` taşır.

**Grup**

```html
<fieldset class="trds-alan-grubu">
  <legend class="trds-baslik-legend">Başvuru türü</legend>
  <div class="trds-secenekler">
    <div class="trds-secenek">
      <input class="trds-radyo" id="t1" name="tur" type="radio" value="bireysel">
      <label class="trds-secenek-etiket" for="t1">Bireysel</label>
    </div>
    <div class="trds-secenek">
      <input class="trds-radyo" id="t2" name="tur" type="radio" value="kurumsal">
      <label class="trds-secenek-etiket" for="t2">Kurumsal</label>
    </div>
  </div>
</fieldset>
```

**React** (`import { SecenekDugmesi } from '@tr-ds/react'`):

```jsx
<SecenekDugmesi etiket="Başvuru türü" ad="tur" secenekler={[{"deger":"bireysel","ad":"Bireysel"},{"deger":"kurumsal","ad":"Kurumsal"}]} />
```

**Vue** (`import { SecenekDugmesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSecenekDugmesi etiket="Başvuru türü" ad="tur" :secenekler='[{"deger":"bireysel","ad":"Bireysel"},{"deger":"kurumsal","ad":"Kurumsal"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Seçim etiketi (Chip) — `chip`

Group: Seçim. Status: CSS stable, JS kaldırma, React stable, Vue stable. WCAG: 4.1.2, 2.5.8. Also in: NO, IE, FI, IT, JP, CH.

Basılabilen küçük etiket. Süzgeç seçmek veya seçileni kaldırmak için.

When to use:
- Arama sonucunu süzerken ve çoklu seçimde seçilenleri göstermek için kullanın.
- Durum göstermek için etiket kullanın. Etiket basılmaz.

Accessibility:
- Süzgeç etiketi bir `button` ve `aria-pressed` taşır.
- Kaldırma düğmesi görsel gizli metinle neyi kaldırdığını söyler: "Ankara’yı kaldır".

**Süzgeç ve seçili**

```html
<div class="trds-cip-grubu" data-trds="cip">
  <button class="trds-cip" type="button" aria-pressed="true">Emekli</button>
  <button class="trds-cip" type="button" aria-pressed="false">Çalışan</button>
  <button class="trds-cip" type="button" aria-pressed="false">İşveren</button>
  <span class="trds-cip trds-cip--secili">Ankara<button class="trds-cip__kaldir" type="button" aria-label="Ankara’yı kaldır"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></span>
</div>
```

**React** (`import { SecimEtiketi } from '@tr-ds/react'`):

```jsx
<SecimEtiketi ogeler={[{"ad":"Emekli","basili":true},{"ad":"Çalışan"},{"ad":"Ankara","secili":true}]} />
```

**Vue** (`import { SecimEtiketi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSecimEtiketi :ogeler='[{"ad":"Emekli","basili":true},{"ad":"Çalışan"},{"ad":"Ankara","secili":true}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

## Eylem

Kullanıcının bir işlem başlatmasını sağlayan parçalar.

### Bağlantı (Link) — `link`

Group: Eylem. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1, 2.4.4, 2.4.7. Also in: US, FR, IE, KR, DK, DE, NO.

Kullanıcıyı başka bir sayfaya veya aynı sayfadaki bir bölüme götürür.

When to use:
- Gezinme için bağlantı kullanın. İşlem için düğme kullanın.
- Bağlantı metni hedefi anlatır. "Buraya tıklayın" yazmayın.
- Yeni sekmede açılan bir bağlantıyı metinde belirtin.

Accessibility:
- Alt çizgi her zaman görünür. Rengi tek ayırt edici işaret olarak kullanmayın.
- Ziyaret edilmiş bağlantı ayrı bir renk alır.
- Dış bağlantı için hedefi metinde adlandırın.

**Metin içi**

```html
<p class="trds-govde">Ayrıntılar için <a class="trds-link" href="#">başvuru koşullarını okuyun</a>.</p>
```

**Yeni sekme**

```html
<a class="trds-link" href="#" target="_blank" rel="noopener">Kılavuzu indirin (PDF, yeni sekmede açılır)</a>
```

**React** (`import { Baglanti } from '@tr-ds/react'`):

```jsx
<Baglanti href="#">Başvuru koşulları</Baglanti>
```

**Vue** (`import { Baglanti } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBaglanti href="#">Başvuru koşulları</TrdsBaglanti>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Düğme (Button) — `button`

Group: Eylem. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.11, 2.4.7, 2.5.5, 2.5.8. Also in: UK, US, FR, IE, KR, JP, DK, DE, NO.

Kullanıcının bir işlemi başlatmasını sağlar. Her ekranda tek bir birincil düğme bulunur.

When to use:
- Bir formu gönderirken birincil düğmeyi kullanın.
- Geri dönüş gibi ikincil işlemler için ikincil düğmeyi kullanın.
- Silme gibi geri alınamaz işlemler için tehlike düğmesini kullanın.
- Başka bir sayfaya götüren bir eylem için düğme değil bağlantı kullanın.

Accessibility:
- Dokunma alanı en az 44 x 44 piksel olur.
- Odak halkası 3 piksel kalınlığında ve sarı renktedir. Odak halkasını kaldırmayın.
- Etiket metni eylemi anlatır. "Tıkla" veya "Gönder" yerine "Başvuruyu gönder" yazın.
- Devre dışı düğme yerine düğmeyi görünür tutun ve hata mesajı gösterin.

**Birincil**

```html
<button class="trds-button" type="button">Başvuruyu gönder</button>
```

**İkincil**

```html
<button class="trds-button trds-button--ikincil" type="button">Taslağı kaydet</button>
```

**Tehlike**

```html
<button class="trds-button trds-button--tehlike" type="button">Başvuruyu sil</button>
```

**Düğme grubu**

```html
<div class="trds-button-grubu">
  <button class="trds-button" type="button">Devam et</button>
  <button class="trds-button trds-button--ikincil" type="button">İptal</button>
</div>
```

**Yalnız simge**

```html
<div class="trds-button-grubu">
  <button class="trds-button trds-button--ikincil trds-button--simge" type="button" aria-label="Yazdır"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-print"/></svg></button>
  <button class="trds-button trds-button--ikincil trds-button--simge" type="button" aria-label="Düzenle"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-edit"/></svg></button>
  <button class="trds-button trds-button--tehlike trds-button--simge" type="button" aria-label="Sil"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button>
</div>
```

**React** (`import { Dugme } from '@tr-ds/react'`):

```jsx
<Dugme>Devam et</Dugme>
```

**Vue** (`import { Dugme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsDugme>Devam et</TrdsDugme>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Eylem menüsü (Action menu) — `action-menu`

Group: Eylem. Status: CSS stable, JS klavye desteği, React stable, Vue stable. WCAG: 2.1.1, 4.1.2. Also in: NO, DK, FI, IT, FR, JP.

Bir düğmenin altında açılan eylem listesi. Düzenle, indir, sil.

When to use:
- Bir kayıt için üç ile altı eylem varsa kullanın.
- Sayfa gezinmesi için kullanmayın. O menü başlık çubuğundadır.

Accessibility:
- `aria-haspopup="menu"`, `role="menu"` ve `role="menuitem"`. Ok tuşları gezer, Home ve End uçlara gider, Escape kapatır.
- Yıkıcı eylem en altta ve kırmızı durur.

**Kayıt eylemleri**

```html
<div class="trds-eylem-menusu" data-trds="eylem-menusu">
  <button class="trds-button trds-button--ikincil" type="button" id="em-1-dugme" aria-haspopup="menu" aria-expanded="false" aria-controls="em-1">Eylemler <svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-down"/></svg></button>
  <div class="trds-eylem-menusu__liste" id="em-1" role="menu" aria-labelledby="em-1-dugme" hidden>
    <button class="trds-eylem-menusu__oge" role="menuitem" type="button"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-edit"/></svg> Düzenle</button>
    <button class="trds-eylem-menusu__oge" role="menuitem" type="button"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-down"/></svg> İndir</button>
    <button class="trds-eylem-menusu__oge" role="menuitem" type="button"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-print"/></svg> Yazdır</button>
    <button class="trds-eylem-menusu__oge trds-eylem-menusu__oge--tehlike" role="menuitem" type="button"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg> Sil</button>
  </div>
</div>
```

**React** (`import { EylemMenusu } from '@tr-ds/react'`):

```jsx
<EylemMenusu etiket="Eylemler" eylemler={[{"ad":"Düzenle","simge":"tool-edit"},{"ad":"Sil","simge":"close","tehlike":true}]} />
```

**Vue** (`import { EylemMenusu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsEylemMenusu etiket="Eylemler" :eylemler='[{"ad":"Düzenle","simge":"tool-edit"},{"ad":"Sil","simge":"close","tehlike":true}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### İndirme bağlantısı (Download link) — `download-link`

Group: Eylem. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.4, 3.2.5. Also in: FR, CH.

Bir dosyaya giden bağlantı. Dosya türünü ve boyutunu bağlantı metninin içinde söyler.

When to use:
- PDF, XLSX veya ZIP verdiğiniz her yerde kullanın.
- Tür ve boyut yazılmayan bir dosya bağlantısı yayımlanmaz.

Accessibility:
- Tür ve boyut bağlantı metninin parçasıdır. Ekran okuyucu "Başvuru formu, PDF, 240 kilobayt" okur.
- `download` özniteliği dosyayı sayfa yerine indirir.

**Liste**

```html
<ul class="trds-liste trds-liste--sade trds-liste--aralikli">
  <li><a class="trds-indir" href="#" download><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-down"/></svg><span class="trds-indir__ad">Emeklilik başvuru formu</span><span class="trds-indir__bilgi">PDF, 240 KB</span></a></li>
  <li><a class="trds-indir" href="#" download><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-down"/></svg><span class="trds-indir__ad">2026 prim tablosu</span><span class="trds-indir__bilgi">XLSX, 1,2 MB</span></a></li>
</ul>
```

**React** (`import { IndirmeBaglantisi } from '@tr-ds/react'`):

```jsx
<IndirmeBaglantisi href="#" ad="Başvuru formu" bilgi="PDF, 240 KB" />
```

**Vue** (`import { IndirmeBaglantisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIndirmeBaglantisi href="#" ad="Başvuru formu" bilgi="PDF, 240 KB" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Paylaş (Share) — `share`

Group: Eylem. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.4, 3.2.5. Also in: FR, CH.

Sayfayı sosyal medyada veya e-posta ile paylaşan bağlantılar. Haber ve duyuru sayfasının sonunda durur.

When to use:
- Haber, duyuru ve kılavuz sayfalarında kullanın.
- Form adımlarında ve kişisel sayfalarda kullanmayın.

Accessibility:
- Her bağlantı `aria-label` ile platform adını söyler. Simge süstür.
- Bağlantılar yeni sekmede açılıyorsa görsel gizli metin bunu söyler.

**Temel**

```html
<div class="trds-paylas">
  <p class="trds-paylas__baslik">Paylaş</p>
  <ul class="trds-paylas__liste"><li><a href="#" aria-label="X’te paylaş"><svg class="trds-simge" aria-hidden="true"><use href="#trds-twitter"/></svg></a></li><li><a href="#" aria-label="Facebook’ta paylaş"><svg class="trds-simge" aria-hidden="true"><use href="#trds-facebook"/></svg></a></li><li><a href="#" aria-label="E-posta ile gönder"><svg class="trds-simge" aria-hidden="true"><use href="#trds-mail"/></svg></a></li><li><a href="#" aria-label="Yazdır"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-print"/></svg></a></li></ul>
</div>
```

**React** (`import { Paylas } from '@tr-ds/react'`):

```jsx
<Paylas kanallar={[{"ad":"X’te paylaş","simge":"twitter","href":"#"}]} />
```

**Vue** (`import { Paylas } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsPaylas :kanallar='[{"ad":"X’te paylaş","simge":"twitter","href":"#"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sayfadan çık (Exit this page) — `exit-this-page`

Group: Eylem. Status: CSS stable, JS hızlı çıkış, React stable, Vue stable. WCAG: 2.1.1, 2.4.3. Also in: UK.

Şiddet ve istismar konulu sayfalarda kullanıcının sayfayı bir dokunuşla terk etmesini sağlar.

When to use:
- Aile içi şiddet, KADES ve benzeri hizmet sayfalarında kullanın.
- Başka hiçbir sayfada kullanmayın.

Accessibility:
- Düğme sayfanın sağ üstünde, kaydırınca da yerinde kalır. Shift tuşuna üç kez basmak da çıkarır.
- Hedef zararsız bir sayfadır. Yeni sekme açılır ve bu sayfa geçmişte değiştirilir.

Data attributes used by the markup: `data-trds-durum`.

**Temel**

```html
<div class="trds-cikis" data-trds="cikis">
  <a class="trds-cikis__dugme" href="https://www.mgm.gov.tr" rel="nofollow noreferrer"><span class="trds-cikis__ust">Acil</span> Sayfadan çık</a>
  <p class="trds-gorsel-gizli" data-trds-durum aria-live="polite"></p>
</div>
<p class="trds-yardim trds-u-ust-2">Shift tuşuna üç kez basmak da sayfadan çıkarır.</p>
```

**React** (`import { SayfadanCik } from '@tr-ds/react'`):

```jsx
<SayfadanCik />
```

**Vue** (`import { SayfadanCik } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSayfadanCik />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yazdır (Print button) — `print`

Group: Eylem. Status: CSS stable, JS yazdırma, React stable, Vue stable. WCAG: 4.1.2. Also in: CH.

Tarayıcının yazdırma penceresini açan düğme. Onay sayfasında ve belgelerde.

When to use:
- Onay sayfasında ve resmî belge görüntüleyen sayfada kullanın.

Accessibility:
- Bir düğmedir, bağlantı değil. Betik yoksa görünmez.
- Yazdırma stili başlık çubuğunu ve menüyü gizler, içeriği tam genişlikte basar.

**Temel**

```html
<button class="trds-button trds-button--ikincil" type="button" data-trds="yazdir"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-print"/></svg> Bu sayfayı yazdır</button>
```

**React** (`import { Yazdir } from '@tr-ds/react'`):

```jsx
<Yazdir />
```

**Vue** (`import { Yazdir } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsYazdir />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yüzen düğme (Floating action button) — `fab`

Group: Eylem. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.5.8, 1.4.10. Also in: KR, BR.

Ekranın köşesinde her zaman görünen tek eylem. Telefonda "Randevu al" gibi.

When to use:
- Sayfanın tek ana eylemi varsa ve sayfa uzunsa kullanın.
- Masaüstünde kullanmayın. Orada eylem sayfanın başında durur.

Accessibility:
- Metin taşır. Yalnız simge yeterli değildir.
- İçeriği örtmez. Sayfanın altında düğme kadar boşluk bırakılır.

**Temel**

```html
<a class="trds-yuzen" href="#"><svg class="trds-simge" aria-hidden="true"><use href="#trds-date"/></svg><span>Randevu al</span></a>
```

**React** (`import { YuzenDugme } from '@tr-ds/react'`):

```jsx
<YuzenDugme href="#" simge="date">Randevu al</YuzenDugme>
```

**Vue** (`import { YuzenDugme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsYuzenDugme href="#" simge="date">Randevu al</TrdsYuzenDugme>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

## Geri bildirim

Sistemin kullanıcıya durum bildiren parçaları.

### Açılır bilgi (Popover) — `popover`

Group: Geri bildirim. Status: CSS stable, JS aç ve kapat, React stable, Vue stable. WCAG: 1.4.13, 4.1.2. Also in: NO, IE, IT, NL, CH.

Bir düğmeye basınca açılan küçük panel. İpucundan farkı: dokunmayla çalışır ve bağlantı taşıyabilir.

When to use:
- Bir alanın yanında "Bu nedir?" sorusu için kullanın.
- Uzun metin için kullanmayın. Ayrıntılar bileşeni vardır.

Accessibility:
- Düğme `aria-expanded` ve `aria-controls` taşır. Panel açılınca odak düğmede kalır.
- Escape, dışarı tıklama ve kapat düğmesi kapatır. Panel `aria-live="polite"` ile duyurulur.

Data attributes used by the markup: `data-trds-kapat`.

**Alan yanında**

```html
<div class="trds-acilir-bilgi" data-trds="acilir-bilgi">
  <button class="trds-acilir-bilgi__dugme" type="button" aria-expanded="false" aria-controls="ab-1"><svg class="trds-simge" aria-hidden="true"><use href="#trds-question23"/></svg><span class="trds-gorsel-gizli">Vergi kimlik numarası nedir?</span></button>
  <div class="trds-acilir-bilgi__panel" id="ab-1" role="region" aria-live="polite" hidden>
    <p class="trds-govde">10 haneli numara. Vergi levhanızda ve <a class="trds-link" href="#">Dijital Vergi Dairesi</a>’nde yazar.</p>
    <button class="trds-button trds-button--ikincil trds-button--kucuk" type="button" data-trds-kapat>Kapat</button>
  </div>
</div>
```

**React** (`import { AcilirBilgi } from '@tr-ds/react'`):

```jsx
<AcilirBilgi etiket="Vergi kimlik numarası nedir?">10 haneli numara.</AcilirBilgi>
```

**Vue** (`import { AcilirBilgi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAcilirBilgi etiket="Vergi kimlik numarası nedir?">10 haneli numara.</TrdsAcilirBilgi>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Bildirim afişi (Notification banner) — `notification-banner`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.3, 1.3.1, 4.1.3. Also in: UK, FR, JP, DE.

Sayfanın en üstünde önemli veya acil bir duyuru gösterir.

When to use:
- Hizmet kesintisi veya acil durum duyurusu için kullanın.
- Sayfada en çok bir tane bulunur.

Accessibility:
- Başlık bir `<h2>` öğesidir.
- Acil durum afişi kırmızı zemin ve beyaz metin kullanır. Kontrast oranı 7 üzerindedir.

**Acil**

```html
<div class="trds-afis trds-afis--acil" role="region" aria-labelledby="afis-baslik">
  <h2 class="trds-afis__baslik" id="afis-baslik">Önemli</h2>
  <p>Sistem bakımı nedeniyle 14 Mart 02:00 ile 05:00 arasında hizmet verilmeyecektir.</p>
</div>
```

**React** (`import { BildirimAfisi } from '@tr-ds/react'`):

```jsx
<BildirimAfisi baslik="Önemli" acil={true}>Sistem bakımı 14 Mart 02:00 ile 05:00 arasında.</BildirimAfisi>
```

**Vue** (`import { BildirimAfisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBildirimAfisi baslik="Önemli" :acil='true'>Sistem bakımı 14 Mart 02:00 ile 05:00 arasında.</TrdsBildirimAfisi>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Geçici bildirim (Toast) — `toast`

Group: Geri bildirim. Status: CSS stable, JS kapatma ve süre, React stable, Vue stable. WCAG: 2.2.1, 4.1.3. Also in: DK, NO, IE, FI, IT, KR.

Bir işlemin sonucunu köşede, kısa süre gösterir. Kaydedildi, gönderildi, silindi.

When to use:
- Kullanıcının sayfada kalmasını gerektirmeyen kısa bir sonuç için kullanın.
- Hata için kullanmayın. Hata sayfada kalır, uyarı bileşeni ile gösterilir.
- En çok bir tane açık durur. Yenisi eskisini kapatır.

Accessibility:
- Alan `role="status"` ve `aria-live="polite"` taşır. Ekran okuyucu metni okur, odağı almaz.
- Kapat düğmesi vardır. Süre 8 saniyeden kısa olmaz. Fare üstündeyken süre durur.
- Metin bir eylem içermez. Bir eylem gerekiyorsa bağlantı metnin içinde durur ve süre kapanır.

**Üç tür**

```html
<div class="trds-gecici-alan" role="status" aria-live="polite" data-trds="gecici">
  <div class="trds-gecici trds-gecici--basari"><p class="trds-gecici__metin">Başvurunuz kaydedildi.</p><button class="trds-gecici__kapat" type="button" aria-label="Bildirimi kapat"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></div>
  <div class="trds-gecici"><p class="trds-gecici__metin">Belge indiriliyor.</p><button class="trds-gecici__kapat" type="button" aria-label="Bildirimi kapat"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></div>
  <div class="trds-gecici trds-gecici--uyari"><p class="trds-gecici__metin">Oturumunuz 2 dakika içinde kapanacak.</p><button class="trds-gecici__kapat" type="button" aria-label="Bildirimi kapat"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></div>
</div>
```

**Eylemli (snackbar)**

```html
<div class="trds-gecici-alan" role="status" aria-live="polite" data-trds="gecici">
  <div class="trds-gecici"><p class="trds-gecici__metin">Belge silindi. <a class="trds-link" href="#">Geri al</a></p><button class="trds-gecici__kapat" type="button" aria-label="Bildirimi kapat"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></div>
</div>
```

**React** (`import { GeciciBildirim } from '@tr-ds/react'`):

```jsx
<GeciciBildirim bildirimler={[{"metin":"Başvurunuz kaydedildi.","tur":"basari"}]} />
```

**Vue** (`import { GeciciBildirim } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsGeciciBildirim :bildirimler='[{"metin":"Başvurunuz kaydedildi.","tur":"basari"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Hata mesajı (Error message) — `error-message`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1, 3.3.1, 3.3.3. Also in: UK, US, DK, NO.

Tek bir alandaki hatayı alanın yanında anlatır.

When to use:
- Her hatalı alanın üstünde gösterin.
- Hatayı ve çözümü birlikte yazın. Yalnız "Geçersiz" yazmayın.

Accessibility:
- Metin "Hata:" ile başlar. Bu kısım ekran okuyucu için gizli metindir.
- Rengi tek işaret olarak kullanmayın. Kalın kenarlık de ekleyin.
- Alan `aria-describedby` ile hata metnine bağlanır.

**Temel**

```html
<p class="trds-hata"><span class="trds-gorsel-gizli">Hata:</span> T.C. kimlik numarası 11 hane olmalıdır.</p>
```

**React** (`import { HataMesaji } from '@tr-ds/react'`):

```jsx
<HataMesaji id="ad-hata">Adınızı yazın.</HataMesaji>
```

**Vue** (`import { HataMesaji } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsHataMesaji id="ad-hata">Adınızı yazın.</TrdsHataMesaji>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Hata özeti (Error summary) — `error-summary`

Group: Geri bildirim. Status: CSS stable, JS odak yönetimi, React stable, Vue stable. WCAG: 2.4.3, 3.3.1, 4.1.3. Also in: UK, DK, NO.

Sayfadaki tüm hataları en üstte toplar ve her birine bağlantı verir.

When to use:
- Sunucu doğrulaması hata döndüğünde sayfanın en üstüne koyun.
- Hataları sayfadaki alan sırasına göre listeleyin.

Accessibility:
- Sayfa yüklendiğinde odak hata özetine gider.
- `role="alert"` ile duyurulur.
- Her madde hatalı alana giden bir bağlantıdır.
- Sayfa başlığı da "Hata: " ile başlar.

**Temel**

```html
<div class="trds-hata-ozeti" data-trds="hata-ozeti" role="alert" tabindex="-1">
  <h2 class="trds-hata-ozeti__baslik">Bir sorun var</h2>
  <ul class="trds-hata-ozeti__liste">
    <li><a href="#kimlik">T.C. kimlik numarası 11 hane olmalıdır</a></li>
    <li><a href="#dogum">Doğum tarihi bugünden sonra olamaz</a></li>
  </ul>
</div>
```

**React** (`import { HataOzeti } from '@tr-ds/react'`):

```jsx
<HataOzeti hatalar={[{"hedef":"#kimlik","metin":"T.C. kimlik numarası 11 hane olmalıdır"}]} />
```

**Vue** (`import { HataOzeti } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsHataOzeti :hatalar='[{"hedef":"#kimlik","metin":"T.C. kimlik numarası 11 hane olmalıdır"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### İlerleme çubuğu (Progress) — `progress`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 4.1.2. Also in: IE, JP, DE, NO.

Süresi bilinen bir işlemin ne kadarının bittiğini gösterir.

When to use:
- Dosya yükleme gibi yüzdesi bilinen işlemlerde kullanın.
- Çok adımlı bir formda kullanmayın. Orada adım göstergesi kullanın.

Accessibility:
- Yerel `<progress>` öğesi kullanılır. Değeri ekran okuyucu yüzde olarak okur.
- Yüzde metin olarak da yazılır. Çubuk tek başına yeterli değildir.
- Etiket `aria-labelledby` ile bağlanır.

**Dosya yükleme**

```html
<div class="trds-ilerleme">
  <div class="trds-ilerleme__ust">
    <span id="ilerleme-etiket">ikametgah.pdf yükleniyor</span>
    <span class="trds-ilerleme__deger">%60</span>
  </div>
  <progress class="trds-ilerleme__cubuk" value="60" max="100" aria-labelledby="ilerleme-etiket">%60</progress>
</div>
```

**Daire**

```html
<div class="trds-ilerleme-daire" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="70" aria-label="Yükleme" style="--trds-ilerleme: 70"><span class="trds-ilerleme-daire__deger">%70</span></div>
```

**React** (`import { Ilerleme } from '@tr-ds/react'`):

```jsx
<Ilerleme deger={60} etiket="ikametgah.pdf yükleniyor" />
```

**Vue** (`import { Ilerleme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIlerleme :deger='60' etiket="ikametgah.pdf yükleniyor" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### İpucu (Tooltip) — `tooltip`

Group: Geri bildirim. Status: CSS stable, JS göster ve gizle, React stable, Vue stable. WCAG: 1.4.13, 2.1.1, 4.1.2. Also in: US, FR, IE, KR, DK, NO.

Bir düğme veya simge için kısa bir açıklama gösterir. Odak veya işaretçi ile açılır.

When to use:
- Yalnız simgeden oluşan bir düğmeye ad vermek için kullanın.
- Zorunlu bilgi için kullanmayın. Dokunmatik ekranda ipucu güvenilir değildir. Yardım metni kullanın.
- İçinde bağlantı veya düğme olmaz.

Accessibility:
- Tetikleyici `aria-describedby` ile ipucuna bağlıdır. Ekran okuyucu düğmeyle birlikte okur.
- Klavye odağı ile açılır, Escape ile kapanır. Yalnız işaretçiye bağlı değildir.
- İşaretçi ipucunun üstüne geçince ipucu kapanmaz. WCAG 1.4.13.

**Simge düğmesi**

```html
<span class="trds-ipucu-kap" data-trds="ipucu">
  <button class="trds-button trds-button--ikincil" type="button" aria-describedby="ipucu-yazdir">Yazdır</button>
  <span class="trds-ipucu" id="ipucu-yazdir" role="tooltip" hidden>Bu sayfayı yazıcıya gönderir</span>
</span>
```

**React** (`import { Ipucu } from '@tr-ds/react'`):

```jsx
<Ipucu metin="Bu sayfayı yazıcıya gönderir">Yazdır</Ipucu>
```

**Vue** (`import { Ipucu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIpucu metin="Bu sayfayı yazıcıya gönderir">Yazdır</TrdsIpucu>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### İskelet (Skeleton) — `skeleton`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.3.3, 4.1.3. Also in: NO.

İçerik yüklenirken yerini tutan gri şekiller. Sayfa zıplamaz.

When to use:
- Bir liste veya kart 1 saniyeden geç geliyorsa kullanın.
- Bir düğmenin sonucu için kullanmayın. Orada yükleniyor halkası durur.

Accessibility:
- Kap `aria-busy="true"` taşır. İçerik gelince kalkar.
- Hareket azaltma isteyen kullanıcıda parıltı durur.

**Kart yerine**

```html
<div class="trds-kart" aria-busy="true">
  <span class="trds-iskelet trds-iskelet--baslik"></span>
  <span class="trds-iskelet trds-iskelet--satir"></span>
  <span class="trds-iskelet trds-iskelet--satir"></span>
  <span class="trds-iskelet trds-iskelet--satir trds-iskelet--kisa"></span>
</div>
```

**React** (`import { Iskelet } from '@tr-ds/react'`):

```jsx
<Iskelet tur="satir" />
```

**Vue** (`import { Iskelet } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIskelet tur="satir" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sonuç paneli (Panel) — `panel`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.6. Also in: UK.

Bir işlemin bittiğini büyük ve yeşil söyler. Başvuru numarası bu kutuda durur.

When to use:
- Onay sayfasının en üstünde kullanın. Bir kere.
- Bir hata için kullanmayın. Yeşil zemin başarı demektir.

Accessibility:
- Başlık `h1` olur. Sayfanın başlığı budur.
- Referans numarası büyük yazılır ve kopyalanabilir. Görsel olarak gizli bir "Başvuru numaranız" etiketi taşır.

**Onay**

```html
<div class="trds-panel">
  <h1 class="trds-panel__baslik">Başvurunuz alındı</h1>
  <div class="trds-panel__govde"><span class="trds-gorsel-gizli">Başvuru numaranız</span><strong>2026-EM-004512</strong></div>
</div>
```

**React** (`import { SonucPaneli } from '@tr-ds/react'`):

```jsx
<SonucPaneli baslik="Başvurunuz alındı" referans="2026-EM-004512" />
```

**Vue** (`import { SonucPaneli } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSonucPaneli baslik="Başvurunuz alındı" referans="2026-EM-004512" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Uyarı (Alert) — `alert`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1, 1.4.3, 4.1.3. Also in: US, FR, IE, DK, DE, NO.

Sayfa içinde bilgi, başarı, uyarı veya hata mesajı gösterir.

When to use:
- İşlem sonucunu bildirmek için kullanın.
- Form alanı hatası için hata mesajı bileşenini kullanın.

Accessibility:
- Dört tür ikonla ve metinle ayırt edilir. Renk tek işaret değildir.
- Sayfa yüklendikten sonra eklenen uyarı `role="status"` taşır.
- Acil bir durum için `role="alert"` kullanın.

**Bilgi**

```html
<div class="trds-uyari trds-uyari--bilgi" role="status">
  <p><strong>Başvurunuz alındı.</strong> Başvuru numaranız: 2026-004512.</p>
</div>
```

**Hata**

```html
<div class="trds-uyari trds-uyari--hata" role="alert">
  <p><strong>Ödeme tamamlanmadı.</strong> Kart bilgilerinizi kontrol edin ve yeniden deneyin.</p>
</div>
```

**React** (`import { Uyari } from '@tr-ds/react'`):

```jsx
<Uyari tur="bilgi">Başvurunuz alındı.</Uyari>
```

**Vue** (`import { Uyari } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsUyari tur="bilgi">Başvurunuz alındı.</TrdsUyari>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Uyarı metni (Warning text) — `warning-text`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.3, 1.4.1. Also in: UK.

Kullanıcının atlamaması gereken bir sonucu söyler. Para cezası, hak kaybı, süre.

When to use:
- Bir eylemin ciddi sonucu varsa, eylemden hemen önce kullanın.
- Sayfada en çok bir tane durur. İkisi birden dikkati böler.

Accessibility:
- Simge süstür. "Uyarı" kelimesi görsel gizli metin olarak durur, ekran okuyucu onu okur.
- Metin kalındır ama yalnız kalınlığa güvenmez. Simge ve kelime birlikte anlatır.

**Temel**

```html
<div class="trds-uyari-metni">
  <span class="trds-uyari-metni__simge" aria-hidden="true">!</span>
  <p class="trds-uyari-metni__metin"><span class="trds-gorsel-gizli">Uyarı:</span> Başvurunuzu 30 gün içinde tamamlamazsanız yeniden başlamanız gerekir.</p>
</div>
```

**React** (`import { UyariMetni } from '@tr-ds/react'`):

```jsx
<UyariMetni>Başvurunuzu 30 gün içinde tamamlayın.</UyariMetni>
```

**Vue** (`import { UyariMetni } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsUyariMetni>Başvurunuzu 30 gün içinde tamamlayın.</TrdsUyariMetni>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yönlendirme (Coach mark) — `coach-mark`

Group: Geri bildirim. Status: CSS stable, JS adımlar, React stable, Vue stable. WCAG: 2.1.1, 2.4.3, 4.1.2. Also in: KR.

İlk kullanımda sayfanın parçalarını sırayla tanıtır. Her adım bir hedefi halkalar ve yanında bir kutu açar.

When to use:
- Yeni ve karmaşık bir ekranın ilk açılışında kullanın. Bir kere gösterin, atlanabilsin.
- Basit bir formda kullanmayın. Yardım metni yeter.

Accessibility:
- Kutu `role="dialog"` taşır, odak içine geçer, Escape bitirir ve odak başlat düğmesine döner.
- Her adım "Adım 2 / 3" der. Hedef halkası yalnız renk değildir, kalın bir çerçevedir.
- Betik yoksa hedefler sıradan içerik olarak kalır.

Data attributes used by the markup: `data-yonlendirme-baslat`, `data-yonlendirme-adim`, `data-yonlendirme-baslik`, `data-yonlendirme-metin`.

**Üç adım**

```html
<div data-trds="yonlendirme">
  <button class="trds-button trds-button--ikincil" type="button" data-yonlendirme-baslat>Sayfayı tanıt</button>
  <div class="trds-kart-izgara trds-kart-izgara--3">
    <div class="trds-kart" data-yonlendirme-adim="1" data-yonlendirme-baslik="Başvurularınız" data-yonlendirme-metin="Açık ve biten başvurular burada durur."><h3 class="trds-kart__baslik">Başvurularım</h3><p class="trds-kart__ozet">2 açık başvuru</p></div>
    <div class="trds-kart" data-yonlendirme-adim="2" data-yonlendirme-baslik="Belgeleriniz" data-yonlendirme-metin="Yüklediğiniz her belge burada saklanır."><h3 class="trds-kart__baslik">Belgelerim</h3><p class="trds-kart__ozet">7 belge</p></div>
    <div class="trds-kart" data-yonlendirme-adim="3" data-yonlendirme-baslik="Mesajlar" data-yonlendirme-metin="Kurumdan gelen her yazı burada. E-posta ile de bildiririz."><h3 class="trds-kart__baslik">Mesajlar</h3><p class="trds-kart__ozet">1 yeni mesaj</p></div>
  </div>
</div>
```

**React** (`import { Yonlendirme } from '@tr-ds/react'`):

```jsx
<Yonlendirme baslat="Sayfayı tanıt">Adımlar</Yonlendirme>
```

**Vue** (`import { Yonlendirme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsYonlendirme baslat="Sayfayı tanıt">Adımlar</TrdsYonlendirme>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yükleniyor (Spinner) — `spinner`

Group: Geri bildirim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.3.3, 4.1.3. Also in: IE, KR, DK, DE, NO.

Bir işlemin sürdüğünü ve süresinin bilinmediğini gösterir.

When to use:
- Sorgu 1 saniyeden uzun sürüyorsa gösterin.
- Süre biliniyorsa ilerleme çubuğu kullanın.
- 10 saniyeden uzun sürerse metin ekleyin: "Bu işlem bir dakika sürebilir."

Accessibility:
- `role="status"` ile durum bir kez duyurulur. `aria-live="assertive"` kullanılmaz.
- Dönme hareketi `prefers-reduced-motion` ile durur, halka yine görünür.
- Görsel yanında her zaman metin bulunur.

**Temel**

```html
<div class="trds-yukleniyor" role="status">
  <span class="trds-yukleniyor__halka" aria-hidden="true"></span>
  <span>Başvurunuz sorgulanıyor</span>
</div>
```

**React** (`import { Yukleniyor } from '@tr-ds/react'`):

```jsx
<Yukleniyor>Başvurunuz sorgulanıyor</Yukleniyor>
```

**Vue** (`import { Yukleniyor } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsYukleniyor>Başvurunuz sorgulanıyor</TrdsYukleniyor>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

## Yerleşim

İçeriği düzenleyen ve gruplayan parçalar.

### Akordiyon (Accordion) — `accordion`

Group: Yerleşim. Status: CSS stable, JS aç ve kapat, React stable, Vue stable. WCAG: 1.3.1, 2.1.1, 4.1.2. Also in: UK, US, FR, IE, KR, JP, DK, DE, NO.

Uzun içeriği başlıklara böler ve her bölümü istek üzerine açar.

When to use:
- Sık sorulan sorular gibi tarama yapılan içerikte kullanın.
- Kullanıcının tümünü okuması gereken içeriği gizlemeyin.

Accessibility:
- Her başlık bir `<button>` içinde durur ve `aria-expanded` taşır.
- JavaScript çalışmazsa tüm bölümler açık kalır.
- "Tümünü göster" düğmesi durumu duyurur.

**Temel**

```html
<div class="trds-akordiyon" data-trds="akordiyon">
  <div class="trds-akordiyon__bolum">
    <h3 class="trds-akordiyon__baslik">
      <button class="trds-akordiyon__dugme" type="button" aria-expanded="false" aria-controls="a1">Başvurum ne kadar sürede sonuçlanır?</button>
    </h3>
    <div class="trds-akordiyon__icerik" id="a1" hidden>
      <p class="trds-govde">Başvurular 15 iş günü içinde sonuçlanır.</p>
    </div>
  </div>
</div>
```

**React** (`import { Akordiyon } from '@tr-ds/react'`):

```jsx
<Akordiyon bolumler={[{"baslik":"Başvurum ne kadar sürede sonuçlanır?","icerik":"Başvurular 15 iş günü içinde sonuçlanır."}]} />
```

**Vue** (`import { Akordiyon } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAkordiyon :bolumler='[{"baslik":"Başvurum ne kadar sürede sonuçlanır?","icerik":"Başvurular 15 iş günü içinde sonuçlanır."}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Ayraç (Divider) — `divider`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: IE, JP, DE, NO.

İki içerik bölümünü ince bir çizgiyle ayırır.

When to use:
- Başlık aralığı yeterli olmadığında kullanın.
- Her paragraf arasına koymayın. Boşluk yeterlidir.

Accessibility:
- Anlamsal ayrım için `<hr>` kullanılır. Süs için `role="presentation"` eklenir.

**İki kalınlık**

```html
<p class="trds-govde">Birinci bölüm.</p>
<hr class="trds-ayrac">
<p class="trds-govde">İkinci bölüm.</p>
<hr class="trds-ayrac trds-ayrac--kalin">
<p class="trds-govde">Üçüncü bölüm.</p>
```

**React** (`import { Ayrac } from '@tr-ds/react'`):

```jsx
<Ayrac />
```

**Vue** (`import { Ayrac } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAyrac />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Ayrıntılar (Details) — `details`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.1.1. Also in: UK, IE, KR, JP, DK, NO.

Tek bir yardımcı açıklamayı istek üzerine gösterir.

When to use:
- Kullanıcıların çoğunun ihtiyaç duymadığı bir açıklama için kullanın.
- Birden çok bölüm için akordiyon kullanın.

Accessibility:
- Yerel `<details>` ve `<summary>` etiketlerini kullanın. JavaScript gerekmez.
- Özet metni bir soru gibi yazılır.

**Temel**

```html
<details class="trds-ayrinti">
  <summary>T.C. kimlik numaramı nerede bulurum?</summary>
  <p class="trds-govde">Nüfus cüzdanınızın ön yüzünde, ad ve soyadınızın üstünde yazar.</p>
</details>
```

**React** (`import { Ayrintilar } from '@tr-ds/react'`):

```jsx
<Ayrintilar ozet="T.C. kimlik numaramı nerede bulurum?">Nüfus cüzdanınızın ön yüzünde yazar.</Ayrintilar>
```

**Vue** (`import { Ayrintilar } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAyrintilar ozet="T.C. kimlik numaramı nerede bulurum?">Nüfus cüzdanınızın ön yüzünde yazar.</TrdsAyrintilar>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Bölüm (Section) — `section`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.4. Also in: IT, CH.

Ana sayfayı yatay şeritlere böler. Her şeridin bir başlığı ve isteğe bağlı "Tümü" bağlantısı vardır.

When to use:
- Ana sayfada her konu bir bölümdür: duyurular, hizmetler, haberler.
- Ardışık iki bölüm aynı zemini almaz. Açık ve yumuşak sırayla gelir.

Accessibility:
- `<section>` başlık ile etiketlenir: `aria-labelledby`.
- "Tümü" bağlantısı görsel gizli metinle neyin tümü olduğunu söyler.

**Başlık ve bağlantı**

```html
<section class="trds-bolum trds-bolum--yumusak" aria-labelledby="b-duyuru">
  <div class="trds-kap">
    <div class="trds-bolum__ust"><h2 class="trds-bolum__baslik" id="b-duyuru">Duyurular</h2><a class="trds-link" href="#">Tümü<span class="trds-gorsel-gizli">: duyurular</span></a></div>
    <p class="trds-govde trds-u-alt-0">Bölümün içeriği burada durur.</p>
  </div>
</section>
```

**React** (`import { Bolum } from '@tr-ds/react'`):

```jsx
<Bolum baslik="Duyurular">İçerik</Bolum>
```

**Vue** (`import { Bolum } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBolum baslik="Duyurular">İçerik</TrdsBolum>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Izgara (Grid) — `grid`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.2, 1.4.10. Also in: IE, DE.

On iki sütunlu yerleşim. Küçük ekranda sütunlar alt alta iner.

When to use:
- İçerik ve yan menü gibi iki bölgeli sayfalarda kullanın.
- Form alanlarını yan yana dizmek için kullanmayın. Alanlar alt alta durur.
- Okuma sütunu 8 sütunu geçmez. Uzun satır okumayı yavaşlatır.

Accessibility:
- Görsel sıra ile DOM sırası aynıdır. Sütunlar CSS ile yer değiştirmez.
- Küçük ekranda içerik hiçbir zaman yatay kaymaz.

**Sekiz ve dört**

```html
<div class="trds-izgara">
  <div class="trds-sutun trds-sutun--8"><div class="trds-izgara-ornek">Ana içerik · 8 sütun</div></div>
  <div class="trds-sutun trds-sutun--4"><div class="trds-izgara-ornek">Yan bölge · 4 sütun</div></div>
</div>
<div class="trds-izgara">
  <div class="trds-sutun trds-sutun--4"><div class="trds-izgara-ornek">4</div></div>
  <div class="trds-sutun trds-sutun--4"><div class="trds-izgara-ornek">4</div></div>
  <div class="trds-sutun trds-sutun--4"><div class="trds-izgara-ornek">4</div></div>
</div>
```

**React** (`import { Izgara } from '@tr-ds/react'`):

```jsx
<Izgara>Sütunlar</Izgara>
```

**Vue** (`import { Izgara } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIzgara>Sütunlar</TrdsIzgara>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kap (Container) — `container`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.8, 1.4.10. Also in: CA, IE, FI, DE.

İçeriği ortalar ve genişliğini sınırlar. Her sayfa bloğu bir kap içinde durur.

When to use:
- Başlık çubuğu, gövde ve alt bilgi aynı kapı kullanır. Kenarlar hizalanır.
- Uzun metin için dar kap kullanın. Satır 75 karakteri geçmez.

Accessibility:
- Kap bir anlam taşımaz, yalnız genişlik verir. Bölge rolleri içindeki ögelere aittir.

**Geniş ve dar**

```html
<div class="trds-kap trds-izgara-ornek">trds-kap · en çok 75rem</div>
<div class="trds-kap trds-kap--dar trds-izgara-ornek trds-u-ust-2">trds-kap--dar · en çok 48rem</div>
```

**React** (`import { Kap } from '@tr-ds/react'`):

```jsx
<Kap>İçerik</Kap>
```

**Vue** (`import { Kap } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKap>İçerik</TrdsKap>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Karo (Tile) — `tile`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.4, 2.5.8. Also in: FR.

Bir konuya giden büyük bağlantı kutusu. Simge, başlık ve bir cümle. Kartın bağlantı olan hali.

When to use:
- Ana sayfada üç ile altı ana konu için kullanın.
- Bir kutuda birden çok bağlantı gerekiyorsa kart kullanın.

Accessibility:
- Karonun tamamı tek bir bağlantıdır. Bağlantı metni başlıktır.
- Simge süstür.

**Üç karo**

```html
<div class="trds-kart-izgara trds-kart-izgara--3 trds-u-ust-0 trds-u-alt-0">
  <a class="trds-karo" href="#"><span class="trds-karo__simge"><svg class="trds-simge" aria-hidden="true"><use href="#trds-socialsecurity"/></svg></span><span class="trds-karo__baslik">Emeklilik</span><span class="trds-karo__aciklama">Ne zaman emekli olurum, başvuru, maaş.</span></a>
  <a class="trds-karo" href="#"><span class="trds-karo__simge"><svg class="trds-simge" aria-hidden="true"><use href="#trds-health"/></svg></span><span class="trds-karo__baslik">Sağlık</span><span class="trds-karo__aciklama">Provizyon, rapor, ilaç.</span></a>
  <a class="trds-karo" href="#"><span class="trds-karo__simge"><svg class="trds-simge" aria-hidden="true"><use href="#trds-business"/></svg></span><span class="trds-karo__baslik">İşveren</span><span class="trds-karo__aciklama">Bildirge, prim, teşvik.</span></a>
</div>
```

**React** (`import { Karo } from '@tr-ds/react'`):

```jsx
<Karo baslik="Emeklilik" simge="socialsecurity" aciklama="Başvuru, maaş." href="#" />
```

**Vue** (`import { Karo } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKaro baslik="Emeklilik" simge="socialsecurity" aciklama="Başvuru, maaş." href="#" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Karşılama bloğu (Hero) — `hero`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.3, 2.4.6. Also in: IT, CH, FR.

Ana sayfanın ilk bloğu. Bir başlık, bir cümle ve bir arama veya düğme. Fotoğraf yok.

When to use:
- Kurum ana sayfasında ve hizmet giriş sayfasında kullanın.
- Kayan pano yerine kullanın. Tek bir mesaj, tek bir eylem.

Accessibility:
- Başlık `h1` olur.
- Zemin düz renktir. Metin fotoğraf üstüne binmez.

**Aramalı**

```html
<section class="trds-kahraman">
  <div class="trds-kap">
    <p class="trds-kahraman__ust">Sosyal Güvenlik Kurumu</p>
    <h1 class="trds-kahraman__baslik">Daima Yanınızda</h1>
    <p class="trds-kahraman__metin">Çalışan, işveren, emekli ve hak sahibi için sosyal güvenlik işlemleri tek adreste.</p>
    <div class="trds-kahraman__eylemler"><a class="trds-button" href="#">e-Devlet ile giriş yap</a><a class="trds-button trds-button--ikincil" href="#">Hizmetleri gör</a></div>
  </div>
</section>
```

**React** (`import { KarsilamaBlogu } from '@tr-ds/react'`):

```jsx
<KarsilamaBlogu baslik="Daima Yanınızda" ust="Sosyal Güvenlik Kurumu" metin="Sosyal güvenlik işlemleri tek adreste." />
```

**Vue** (`import { KarsilamaBlogu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKarsilamaBlogu baslik="Daima Yanınızda" ust="Sosyal Güvenlik Kurumu" metin="Sosyal güvenlik işlemleri tek adreste." />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kart (Card) — `card`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.4. Also in: US, FR, IE, JP, DK, DE, NO.

Bir hizmeti veya duyuruyu başlık, açıklama ve bağlantı ile tanıtır.

When to use:
- Hizmet listeleri ve duyuru listeleri için kullanın.
- Form alanlarını kart içine koymayın.

Accessibility:
- Tüm kartı tıklanabilir yapmayın. Başlıktaki bağlantı tıklanır.
- Kart başlıkları sayfadaki başlık düzenine uyar.

**Temel**

```html
<div class="trds-kart">
  <h3 class="trds-kart__baslik"><a href="#">Emeklilik başvurusu</a></h3>
  <p class="trds-kart__ozet">Yaş ve prim gün sayısı koşullarını sağlıyorsanız çevrim içi başvurun.</p>
</div>
```

**React** (`import { Kart } from '@tr-ds/react'`):

```jsx
<Kart baslik="Emeklilik başvurusu" href="#" ozet="Çevrim içi başvurun." />
```

**Vue** (`import { Kart } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKart baslik="Emeklilik başvurusu" href="#" ozet="Çevrim içi başvurun." />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kayan pano (Carousel) — `carousel`

Group: Yerleşim. Status: CSS stable, JS düğmeler ve noktalar, React stable, Vue stable. WCAG: 2.2.2, 2.3.3, 4.1.2. Also in: IT, JP, BR, KR, CH.

Yan yana kartlardan bir şerit. Kendiliğinden dönmez. Düğmeler bir kart kaydırır.

When to use:
- Eşit önemde üç ile altı tanıtım için kullanın. Ana mesaj için karşılama bloğu vardır.
- Kendiliğinden döndürmeyin. Zorunluysa 5 saniye altına inmeyin ve bir durdur düğmesi koyun.
- Kullanıcıların çoğu ikinci slaytı görmez. Önemli içeriği kaydırmaya saklamayın.

Accessibility:
- Her slayt `role="group"` ve "1 / 5" etiketi taşır. Şerit klavye ile de kayar.
- Hareket azaltma isteyen kullanıcıda kayma anlıktır ve kendiliğinden dönme kapalıdır.
- İşaretçi veya odak şeritteyken dönme durur.

Data attributes used by the markup: `data-trds-durum`, `data-yon`, `data-durdur`.

**Üç kart**

```html
<div class="trds-kayan-pano" data-trds="kayan-pano">
  <div class="trds-kayan-pano__ust"><p class="trds-gorsel-gizli" data-trds-durum aria-live="polite"></p><div class="trds-kayan-pano__dugmeler"><button class="trds-button trds-button--ikincil trds-button--simge" type="button" data-yon="geri" aria-label="Önceki"><svg class="trds-simge" aria-hidden="true"><use href="#trds-arrow-left"/></svg></button><button class="trds-button trds-button--ikincil trds-button--simge" type="button" data-yon="ileri" aria-label="Sonraki"><svg class="trds-simge" aria-hidden="true"><use href="#trds-arrow-right"/></svg></button><button class="trds-button trds-button--ikincil trds-button--kucuk" type="button" data-durdur aria-pressed="false">Durdur</button></div></div>
  <ul class="trds-kayan-pano__serit">
    <li class="trds-kayan-pano__slayt" tabindex="-1"><div class="trds-kart"><h3 class="trds-kart__baslik">Annelik Yolculuğu</h3><p class="trds-kart__ozet">Gebelikten doğuma kadar her adımda rehber.</p><p class="trds-kart__alt"><a class="trds-link" href="#">Detaylı bilgi</a></p></div></li>
    <li class="trds-kayan-pano__slayt" tabindex="-1"><div class="trds-kart"><h3 class="trds-kart__baslik">Şehir Hastanelerimiz</h3><p class="trds-kart__ozet">23 şehir hastanesi, 34 bin yatak.</p><p class="trds-kart__alt"><a class="trds-link" href="#">Detaylı bilgi</a></p></div></li>
    <li class="trds-kayan-pano__slayt" tabindex="-1"><div class="trds-kart"><h3 class="trds-kart__baslik">e-Nabız</h3><p class="trds-kart__ozet">Kişisel sağlık sistemi.</p><p class="trds-kart__alt"><a class="trds-link" href="#">Detaylı bilgi</a></p></div></li>
    <li class="trds-kayan-pano__slayt" tabindex="-1"><div class="trds-kart"><h3 class="trds-kart__baslik">Alo 171</h3><p class="trds-kart__ozet">Arayanların 135 bini sigarayı bıraktı.</p><p class="trds-kart__alt"><a class="trds-link" href="#">Detaylı bilgi</a></p></div></li>
  </ul>
  <div class="trds-kayan-pano__noktalar" aria-label="Slaytlar"></div>
</div>
```

**React** (`import { KayanPano } from '@tr-ds/react'`):

```jsx
<KayanPano ad="Tanıtımlar" slaytlar={["Bir","İki","Üç"]} />
```

**Vue** (`import { KayanPano } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKayanPano ad="Tanıtımlar" :slaytlar='["Bir","İki","Üç"]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kip pencere (Modal) — `modal`

Group: Yerleşim. Status: CSS stable, JS odak tuzağı, React stable, Vue stable. WCAG: 2.1.2, 2.4.3, 4.1.2. Also in: US, FR, IE, KR, JP, DK, DE, NO.

Kullanıcıdan tek bir onay veya küçük bir karar ister.

When to use:
- Geri alınamaz bir işlemi onaylatmak için kullanın.
- Uzun bir form için kullanmayın. Ayrı bir sayfa kullanın.

Accessibility:
- Yerel `<dialog>` öğesini kullanın. Odak tuzağı tarayıcıdan gelir.
- Escape tuşu pencereyi kapatır.
- Kapatınca odak pencereyi açan düğmeye döner.

Data attributes used by the markup: `data-trds-ac`, `data-trds-kapat`.

**Onay**

```html
<button class="trds-button trds-button--tehlike" type="button" data-trds-ac="silme-onay">Başvuruyu sil</button>
<dialog class="trds-kip" id="silme-onay" data-trds="kip">
  <h2 class="trds-kip__baslik">Başvuruyu silmek istiyor musunuz?</h2>
  <p class="trds-govde">Bu işlem geri alınamaz.</p>
  <div class="trds-button-grubu">
    <button class="trds-button trds-button--tehlike" type="button">Evet, sil</button>
    <button class="trds-button trds-button--ikincil" type="button" data-trds-kapat>Vazgeç</button>
  </div>
</dialog>
```

**Onay isteyen (alertdialog)**

```html
<dialog class="trds-kip" role="alertdialog" aria-labelledby="kip-sil-baslik" aria-describedby="kip-sil-metin" open>
  <h2 class="trds-kip__baslik" id="kip-sil-baslik">Başvuruyu silmek istiyor musunuz?</h2>
  <p class="trds-govde" id="kip-sil-metin">Bu işlem geri alınamaz. Yüklediğiniz 3 belge de silinir.</p>
  <div class="trds-button-grubu"><button class="trds-button trds-button--tehlike" type="button">Sil</button><button class="trds-button trds-button--ikincil" type="button">Vazgeç</button></div>
</dialog>
```

**React** (`import { KipPencere } from '@tr-ds/react'`):

```jsx
<KipPencere id="silme-onay" baslik="Başvuruyu silmek istiyor musunuz?" acici="Başvuruyu sil">Bu işlem geri alınamaz.</KipPencere>
```

**Vue** (`import { KipPencere } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKipPencere id="silme-onay" baslik="Başvuruyu silmek istiyor musunuz?" acici="Başvuruyu sil">Bu işlem geri alınamaz.</TrdsKipPencere>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sekmeler (Tabs) — `tabs`

Group: Yerleşim. Status: CSS stable, JS klavye desteği, React stable, Vue stable. WCAG: 2.1.1, 2.4.3, 4.1.2. Also in: UK, FR, IE, KR, JP, DK, DE, NO.

Aynı alanda birbirini dışlayan içerik bölümleri arasında geçiş yapar.

When to use:
- İçerik bölümleri eş değerdeyse ve karşılaştırma gerekmiyorsa kullanın.
- Bir formun adımları için kullanmayın. Adım göstergesi kullanın.
- Mobil ekranda sekmeler akordiyona dönüşür.

Accessibility:
- Ok tuşları sekmeler arasında gezinir. Home ve End uçlara gider.
- Etkin sekme `aria-selected="true"` taşır.
- Panel `role="tabpanel"` ve `tabindex="0"` taşır.

**Temel**

```html
<div class="trds-sekmeler" data-trds="sekmeler">
  <div class="trds-sekmeler__liste" role="tablist" aria-label="Başvuru bilgileri">
    <button class="trds-sekmeler__dugme" role="tab" aria-selected="true" aria-controls="p1" id="s1">Genel</button>
    <button class="trds-sekmeler__dugme" role="tab" aria-selected="false" aria-controls="p2" id="s2" tabindex="-1">Belgeler</button>
  </div>
  <div class="trds-sekmeler__panel" role="tabpanel" id="p1" aria-labelledby="s1" tabindex="0">
    <p class="trds-govde">Başvurunuz 12 Mart 2026 tarihinde alındı.</p>
  </div>
  <div class="trds-sekmeler__panel" role="tabpanel" id="p2" aria-labelledby="s2" tabindex="0" hidden>
    <p class="trds-govde">İki belge eksik.</p>
  </div>
</div>
```

**React** (`import { Sekmeler } from '@tr-ds/react'`):

```jsx
<Sekmeler etiket="Başvuru bilgileri" sekmeler={[{"ad":"Genel","icerik":"Başvurunuz alındı."},{"ad":"Belgeler","icerik":"İki belge eksik."}]} />
```

**Vue** (`import { Sekmeler } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSekmeler etiket="Başvuru bilgileri" :sekmeler='[{"ad":"Genel","icerik":"Başvurunuz alındı."},{"ad":"Belgeler","icerik":"İki belge eksik."}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Simge kartı (Icon card) — `icon-card`

Group: Yerleşim. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.1.1, 2.4.4.

Bir portalın ana bölümlerine giden büyük simgeli bağlantı. e-Devlet’in beş temel işlevi bu kalıptadır.

When to use:
- Ana sayfada üç ile altı ana bölüm için kullanın.
- Simge e-Devlet resmî setinden seçilir. Yeni simge çizilmez.

Accessibility:
- Kartın tamamı tek bir bağlantıdır. Simge süstür ve `aria-hidden` taşır.
- Ad bağlantı metnidir. Açıklama kısa bir cümledir.

**Beş bölüm**

```html
<div class="trds-izgara">
  <a class="trds-sutun trds-sutun--4 trds-simge-kart" href="#"><span class="trds-simge-kart__simge"><svg class="trds-simge" aria-hidden="true"><use href="#trds-eservice"/></svg></span><span class="trds-simge-kart__ad">e-Hizmetler</span><p class="trds-simge-kart__aciklama">Sorgulama, başvuru ve ödeme hizmetleri.</p></a>
  <a class="trds-sutun trds-sutun--4 trds-simge-kart" href="#"><span class="trds-simge-kart__simge"><svg class="trds-simge" aria-hidden="true"><use href="#trds-agency"/></svg></span><span class="trds-simge-kart__ad">Kurumlar</span><p class="trds-simge-kart__aciklama">Resmî kurumların hizmetleri ve iletişim bilgileri.</p></a>
  <a class="trds-sutun trds-sutun--4 trds-simge-kart" href="#"><span class="trds-simge-kart__simge"><svg class="trds-simge" aria-hidden="true"><use href="#trds-municipality"/></svg></span><span class="trds-simge-kart__ad">Belediyeler</span><p class="trds-simge-kart__aciklama">Belediyelerin iletişim bilgileri ve hizmetleri.</p></a>
</div>
```

**React** (`import { SimgeKarti } from '@tr-ds/react'`):

```jsx
<SimgeKarti ad="e-Hizmetler" simge="eservice" aciklama="Sorgulama, başvuru ve ödeme." />
```

**Vue** (`import { SimgeKarti } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSimgeKarti ad="e-Hizmetler" simge="eservice" aciklama="Sorgulama, başvuru ve ödeme." />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yan panel (Drawer) — `drawer`

Group: Yerleşim. Status: CSS stable, JS aç ve kapat, React stable, Vue stable. WCAG: 2.1.2, 2.4.3, 4.1.2. Also in: NL, JP, IE, KR.

Kenardan kayan panel. Yardım metni, süzgeçler veya telefonda alt sayfa için. Altta açılan hali alt sayfadır.

When to use:
- Süzgeç formu ve yardım paneli için kullanın.
- Onay istemek için kip pencere kullanın.

Accessibility:
- Yerel `<dialog>` ile açılır. Odak panele geçer, Escape kapatır, arka plan kilitlenir.
- Başlık `aria-labelledby` ile panele bağlanır. Kapat düğmesi ilk odaktır.

Data attributes used by the markup: `data-trds-panel-ac`, `data-trds-kapat`.

**Sağdan ve alttan**

```html
<div data-trds="yan-panel">
  <button class="trds-button trds-button--ikincil" type="button" data-trds-panel-ac>Süzgeçleri aç</button>
  <dialog class="trds-yan-panel" aria-labelledby="yp-baslik">
    <div class="trds-yan-panel__ust"><h2 class="trds-yan-panel__baslik" id="yp-baslik">Süzgeçler</h2><button class="trds-button trds-button--ikincil trds-button--simge" type="button" aria-label="Paneli kapat" data-trds-kapat><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></div>
    <div class="trds-yan-panel__govde"><div class="trds-alan"><label class="trds-etiket" for="yp-il">İl</label><select class="trds-secim" id="yp-il"><option>Ankara</option><option>İstanbul</option></select></div></div>
    <div class="trds-yan-panel__alt"><button class="trds-button" type="button" data-trds-kapat>Uygula</button></div>
  </dialog>
</div>
<div data-trds="yan-panel" class="trds-u-ust-4">
  <button class="trds-button trds-button--ikincil" type="button" data-trds-panel-ac>Alt sayfayı aç</button>
  <dialog class="trds-yan-panel trds-yan-panel--alt" aria-labelledby="as-baslik">
    <div class="trds-yan-panel__ust"><h2 class="trds-yan-panel__baslik" id="as-baslik">Paylaş</h2><button class="trds-button trds-button--ikincil trds-button--simge" type="button" aria-label="Kapat" data-trds-kapat><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></div>
    <div class="trds-yan-panel__govde"><ul class="trds-liste trds-liste--sade"><li><a class="trds-link" href="#">Bağlantıyı kopyala</a></li><li><a class="trds-link" href="#">E-posta ile gönder</a></li></ul></div>
  </dialog>
</div>
```

**Yardım paneli**

```html
<div data-trds="yan-panel">
  <button class="trds-button trds-button--ikincil" type="button" data-trds-panel-ac><svg class="trds-simge" aria-hidden="true"><use href="#trds-question23"/></svg> Yardım</button>
  <dialog class="trds-yan-panel trds-yan-panel--sol" aria-labelledby="yardim-baslik">
    <div class="trds-yan-panel__ust"><h2 class="trds-yan-panel__baslik" id="yardim-baslik">Bu sayfa hakkında</h2><button class="trds-button trds-button--ikincil trds-button--simge" type="button" aria-label="Yardımı kapat" data-trds-kapat><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button></div>
    <div class="trds-yan-panel__govde"><dl class="trds-tanim"><div class="trds-tanim__satir"><dt>Prim günü</dt><dd>Sigortalı çalıştığınız her gün için bir prim günü işlenir.</dd></div><div class="trds-tanim__satir"><dt>Hizmet dökümü</dt><dd>Bütün işverenleriniz ve prim günleriniz.</dd></div></dl><p class="trds-govde">Daha fazlası için <a class="trds-link" href="#">Alo 170</a>.</p></div>
  </dialog>
</div>
```

**React** (`import { YanPanel } from '@tr-ds/react'`):

```jsx
<YanPanel baslik="Süzgeçler" acici="Süzgeçleri aç">İçerik</YanPanel>
```

**Vue** (`import { YanPanel } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsYanPanel baslik="Süzgeçler" acici="Süzgeçleri aç">İçerik</TrdsYanPanel>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Yardımcı sınıflar (Utility classes) — `utilities`

Group: Yerleşim. Status: CSS stable, JS yok, React yok, Vue yok. WCAG: 1.3.2. Also in: US, DE.

Boşluk, hizalama ve görünürlük için tek işli sınıflar. Bileşen olmayan yerde kullanılır.

When to use:
- Bir bileşenin dış boşluğunu bir kez değiştirmek için kullanın.
- Yeni bir bileşen kurmak için kullanmayın. Beş yardımcı sınıf bir araya geliyorsa bir bileşen eksiktir.

Accessibility:
- `trds-u-gizle-telefon` yalnız görsel olarak gizler. İçerik ekran okuyucuda kalır.
- Tam gizlemek için `hidden` özniteliği kullanılır.

**Boşluk ve hizalama**

```html
<p class="trds-govde trds-u-alt-0">Bu paragrafın altında boşluk yok.</p>
<p class="trds-govde trds-u-ust-6 trds-u-metin-sag">Bu paragraf 6 birim üst boşluk taşır ve sağa yaslıdır.</p>
<p class="trds-govde trds-u-gizle-telefon">Bu paragraf telefonda görünmez.</p>
```

**Yapışkan**

```html
<div class="trds-u-yapiskan trds-izgara-ornek">trds-u-yapiskan · kaydırırken üstte kalır</div>
```

No React or Vue wrapper: this part is a set of classes or a file, use the HTML directly.

## İçerik

Veriyi ve metni sunan parçalar.

### Alıntı (Blockquote) — `blockquote`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: NL, JP, FR, CH.

Başka bir kaynaktan alınan bir cümleyi metinden ayırır. Kaynağı altında yazar.

When to use:
- Mevzuat maddesi, karar metni veya bir açıklamayı alıntılarken kullanın.
- Vurgu için kullanmayın. Vurgu için vurgulu metin vardır.

Accessibility:
- `<blockquote>` ve `<cite>` kullanılır. Kaynak bir bağlantı olabilir.
- Tırnak işareti CSS ile gelir, metne yazılmaz.

**Kaynaklı**

```html
<figure class="trds-alinti">
  <blockquote class="trds-alinti__metin"><p>Herkes, dilekçe hakkına sahiptir. Vatandaşlar ve karşılıklılık esası gözetilmek kaydıyla Türkiye’de ikamet eden yabancılar kendileriyle veya kamu ile ilgili dilek ve şikâyetleri hakkında yetkili makamlara başvurma hakkına sahiptir.</p></blockquote>
  <figcaption class="trds-alinti__kaynak">Anayasa, <cite>Madde 74</cite></figcaption>
</figure>
```

**React** (`import { Alinti } from '@tr-ds/react'`):

```jsx
<Alinti kaynak="Anayasa" eser="Madde 74">Herkes, dilekçe hakkına sahiptir.</Alinti>
```

**Vue** (`import { Alinti } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAlinti kaynak="Anayasa" eser="Madde 74">Herkes, dilekçe hakkına sahiptir.</TrdsAlinti>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Avatar (Avatar) — `avatar`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.1.1. Also in: NL, NO, IT, BR, CH.

Bir kişiyi veya kurumu fotoğraf ya da baş harflerle gösterir.

When to use:
- Yorum, mesaj ve görev listesinde kişiyi göstermek için kullanın.
- Fotoğraf yoksa baş harfler durur. Rastgele renk kullanmayın, tek renk vardır.

Accessibility:
- Fotoğraf `alt` olarak kişinin adını taşır. Baş harfler `aria-hidden` taşır, ad yanında metin olarak durur.
- Yığın en çok beş avatar gösterir. Fazlası "+12" olarak yazılır.

**Baş harf ve yığın**

```html
<div class="trds-u-satir">
  <span class="trds-avatar" aria-hidden="true">AY</span>
  <span class="trds-avatar trds-avatar--buyuk" aria-hidden="true">SGK</span>
  <div class="trds-avatar-yigini" aria-label="Görevli 7 kişi">
    <span class="trds-avatar" aria-hidden="true">AY</span><span class="trds-avatar" aria-hidden="true">MK</span><span class="trds-avatar" aria-hidden="true">ZD</span><span class="trds-avatar trds-avatar--sayi">+4</span>
  </div>
</div>
```

**React** (`import { Avatar } from '@tr-ds/react'`):

```jsx
<Avatar basHarf="AY" ad="Ayşe Yılmaz" />
```

**Vue** (`import { Avatar } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAvatar basHarf="AY" ad="Ayşe Yılmaz" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Bilgi kutusu (Callout) — `callout`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: FR, US, IT, NL, JP.

Başlıklı bir bilgi kutusu. Bir durum bildirmez, bir bilgiyi öne çıkarır.

When to use:
- Bir sayfanın en önemli bilgisini, örneğin son günü veya ücreti, ayrı bir kutuda vermek için kullanın.
- Hata veya başarı için uyarı bileşeni kullanın.

Accessibility:
- Başlık bir başlık ögesidir ve sayfa sırasına uyar.
- Simge süstür.

**Temel**

```html
<div class="trds-bilgi-kutusu">
  <h3 class="trds-bilgi-kutusu__baslik"><svg class="trds-simge" aria-hidden="true"><use href="#trds-information"/></svg> Son gün 31 Mart</h3>
  <p class="trds-govde trds-u-alt-0">Yıllık gelir vergisi beyannamesini 31 Mart 2026 Salı 23:59’a kadar verin. Ücret yoktur.</p>
</div>
```

**React** (`import { BilgiKutusu } from '@tr-ds/react'`):

```jsx
<BilgiKutusu baslik="Son gün 31 Mart">Beyannameyi 31 Mart’a kadar verin.</BilgiKutusu>
```

**Vue** (`import { BilgiKutusu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBilgiKutusu baslik="Son gün 31 Mart">Beyannameyi 31 Mart’a kadar verin.</TrdsBilgiKutusu>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Etiket (Tag) — `tag`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1, 1.4.3. Also in: UK, US, FR, IE, KR, JP, DK, NO.

Bir kaydın durumunu kısa bir sözcükle gösterir.

When to use:
- Başvuru durumu gibi bir durumu göstermek için kullanın.
- Bağlantı veya düğme olarak kullanmayın.

Accessibility:
- Metin her zaman bulunur. Yalnız renk kullanmayın.
- Beş renk seçeneğinin hepsi 4.5 üzerinde kontrast sağlar.

**Durumlar**

```html
<span class="trds-etiket trds-etiket--mavi">İnceleniyor</span>
<span class="trds-etiket trds-etiket--yesil">Onaylandı</span>
<span class="trds-etiket trds-etiket--kirmizi">Reddedildi</span>
<span class="trds-etiket trds-etiket--sari">Belge bekleniyor</span>
<span class="trds-etiket trds-etiket--gri">Başlanmadı</span>
```

**React** (`import { Etiket } from '@tr-ds/react'`):

```jsx
<Etiket renk="mavi">İnceleniyor</Etiket>
```

**Vue** (`import { Etiket } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsEtiket renk="mavi">İnceleniyor</TrdsEtiket>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Görev listesi (Task list) — `task-list`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1, 1.3.1. Also in: UK, DE.

Çok parçalı bir başvuruda hangi bölümün tamamlandığını gösterir.

When to use:
- Kullanıcı başvuruyu birden çok oturumda tamamlıyorsa kullanın.
- Kısa ve doğrusal bir formda kullanmayın.

Accessibility:
- Durum etiketi `aria-describedby` ile göreve bağlanır.
- Durumu yalnız renkle göstermeyin. Metin de yazın.

**Temel**

```html
<ol class="trds-gorev-listesi">
  <li class="trds-gorev-listesi__oge">
    <a href="#" aria-describedby="g1">Kişisel bilgiler</a>
    <span class="trds-etiket trds-etiket--yesil" id="g1">Tamamlandı</span>
  </li>
  <li class="trds-gorev-listesi__oge">
    <a href="#" aria-describedby="g2">Belgeleri yükleyin</a>
    <span class="trds-etiket trds-etiket--gri" id="g2">Başlanmadı</span>
  </li>
</ol>
```

**React** (`import { GorevListesi } from '@tr-ds/react'`):

```jsx
<GorevListesi gorevler={[{"ad":"Kişisel bilgiler","href":"#","durum":"Tamamlandı","renk":"yesil"}]} />
```

**Vue** (`import { GorevListesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsGorevListesi :gorevler='[{"ad":"Kişisel bilgiler","href":"#","durum":"Tamamlandı","renk":"yesil"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Görsel (Figure) — `figure`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.1.1, 1.4.5. Also in: NL, JP, KR, FR.

Bir görsel ve altında bir açıklama. Görsel içeriğin genişliğini aşmaz.

When to use:
- Bir haber, duyuru veya kılavuz görseli için kullanın.
- Süs görseli için kullanmayın. Süs görselinin açıklaması olmaz.

Accessibility:
- `alt` görseli anlatır. Açıklama görseli yorumlar. İkisi aynı cümle olmaz.
- Görsel `width` ve `height` taşır, sayfa yüklenirken zıplamaz.

**Açıklamalı**

```html
<figure class="trds-gorsel">
  <img src="/trds/turk-bayragi.svg" alt="Türk bayrağı: kırmızı zemin üzerinde beyaz ay ve yıldız" width="1200" height="800">
  <figcaption class="trds-gorsel__altyazi">Bayrağın oranları 2893 sayılı Türk Bayrağı Kanunu ile belirlenir.</figcaption>
</figure>
```

**React** (`import { Gorsel } from '@tr-ds/react'`):

```jsx
<Gorsel src="/trds/turk-bayragi.svg" alt="Türk bayrağı" altyazi="Bayrağın oranları kanunla belirlenir." />
```

**Vue** (`import { Gorsel } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsGorsel src="/trds/turk-bayragi.svg" alt="Türk bayrağı" altyazi="Bayrağın oranları kanunla belirlenir." />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### İstatistik kutusu (Stat tile) — `stat`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 1.3.2.

Bir kurumun büyüklüğünü dört rakamla anlatır. Kayıtlı kullanıcı, hizmet sayısı gibi.

When to use:
- Ana sayfada, en çok dört sayı ile kullanın.
- Sayı ne anlama geliyorsa altına yazın. Tek başına 9.409 anlamsızdır.
- Ondalık ve binlik ayracı Türkçe kuralına göre yazılır: 69.596.229.

Accessibility:
- Sayı ve adı tek bir okuma sırasında gelir. Sayı önce, ad sonra.
- Rakamlar eş genişliktedir, sütunlar hizalanır.

**Dört sayı**

```html
<div class="trds-istatistik">
  <div class="trds-istatistik__kutu"><span class="trds-istatistik__deger">69.596.229</span><span class="trds-istatistik__ad">Kayıtlı kullanıcı</span></div>
  <div class="trds-istatistik__kutu"><span class="trds-istatistik__deger">9.409</span><span class="trds-istatistik__ad">Hizmet</span></div>
  <div class="trds-istatistik__kutu"><span class="trds-istatistik__deger">6.426</span><span class="trds-istatistik__ad">Mobil hizmet</span></div>
  <div class="trds-istatistik__kutu"><span class="trds-istatistik__deger">1.129</span><span class="trds-istatistik__ad">Kurum</span></div>
</div>
```

**React** (`import { Istatistik } from '@tr-ds/react'`):

```jsx
<Istatistik kutular={[{"deger":"69.596.229","ad":"Kayıtlı kullanıcı"}]} />
```

**Vue** (`import { Istatistik } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIstatistik :kutular='[{"deger":"69.596.229","ad":"Kayıtlı kullanıcı"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kayıt listesi (Collection) — `collection`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.4. Also in: US, KR, JP, BR.

Haber, duyuru veya belge kayıtlarının listesi. Her kayıt başlık, tarih, özet ve etiket taşır.

When to use:
- Arama sonucu, duyuru listesi ve haber listesi için kullanın.
- Üç kayıttan az ise kart kullanın.

Accessibility:
- Liste `<ul>` ve `<li>` ile kurulur. Başlık bağlantıdır.
- Tarih ve kurum, başlıktan önce küçük satırda durur.

**Duyurular**

```html
<ul class="trds-kayitlar">
  <li class="trds-kayit"><p class="trds-kayit__ust"><time datetime="2026-09-02">2 Eylül 2026</time> · Genel Sağlık Sigortası Genel Müdürlüğü</p><h3 class="trds-kayit__baslik"><a href="#">Bedeli Ödenecek İlaçlar Listesinde Yapılan Düzenlemeler Hakkında Duyuru 2026/34</a></h3><p class="trds-kayit__ozet">Liste 5 Eylül 2026 tarihinden itibaren geçerlidir.</p><p class="trds-kayit__etiketler"><span class="trds-etiket trds-etiket--mavi">Duyuru</span></p></li>
  <li class="trds-kayit"><p class="trds-kayit__ust"><time datetime="2026-08-31">31 Ağustos 2026</time> · İnşaat ve Emlak Daire Başkanlığı</p><h3 class="trds-kayit__baslik"><a href="#">Gayrimenkul Satış İlanı</a></h3><p class="trds-kayit__etiketler"><span class="trds-etiket trds-etiket--sari">İhale</span></p></li>
</ul>
```

**React** (`import { KayitListesi } from '@tr-ds/react'`):

```jsx
<KayitListesi kayitlar={[{"ust":"2 Eylül 2026 · GSS","baslik":"Duyuru 2026/34","href":"#"}]} />
```

**Vue** (`import { KayitListesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKayitListesi :kayitlar='[{"ust":"2 Eylül 2026 · GSS","baslik":"Duyuru 2026/34","href":"#"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Kod (Code) — `code`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.3, 1.4.10. Also in: NL.

Satır içi kod ve kod bloğu. API belgeleri ve teknik kılavuzlar için.

When to use:
- Bir komut, dosya adı veya parametre yazarken kullanın.
- Vatandaşa dönük sayfalarda kullanmayın.

Accessibility:
- `<code>` ve `<pre>` kullanılır. Blok yatay kayar, sayfa kaymaz.
- Karşıtlık koyu zeminde de 4,5:1 üstündedir.

**İkisi**

```html
<p class="trds-govde">Belirteçleri <code class="trds-kod">node packages/tokens/build.mjs</code> ile üretin.</p>
<pre class="trds-kod-blok"><code>&lt;input class="trds-girdi" id="tc" inputmode="numeric" maxlength="11"&gt;</code></pre>
```

**React** (`import { Kod } from '@tr-ds/react'`):

```jsx
<Kod>node build.mjs</Kod>
```

**Vue** (`import { Kod } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKod>node build.mjs</TrdsKod>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Liste (List) — `list`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: US, IE, KR, JP, NO, DE.

Madde işaretli, numaralı ve aralıklı liste biçimleri.

When to use:
- Sıra önemliyse numaralı liste kullanın. Adımlar sıralıdır.
- Her madde bir cümleden uzunsa aralıklı biçimi kullanın.
- Tek maddelik liste yapmayın. Bir cümle yazın.

Accessibility:
- `<ul>` ve `<ol>` kullanılır. Ekran okuyucu madde sayısını duyurur.
- Madde işareti CSS ile kaldırılırsa `role="list"` eklenir. Safari aksi hâlde listeyi duyurmaz.

**Üç biçim**

```html
<ul class="trds-liste trds-liste--madde">
  <li>Nüfus cüzdanı</li>
  <li>İkametgâh belgesi</li>
  <li>Son üç aylık maaş bordrosu</li>
</ul>
<ol class="trds-liste trds-liste--sira">
  <li>e-Devlet ile giriş yapın.</li>
  <li>Başvuru formunu doldurun.</li>
  <li>Belgeleri yükleyin.</li>
</ol>
<ul class="trds-liste trds-liste--madde trds-liste--aralikli">
  <li>Başvurunuz alındıktan sonra 15 iş günü içinde değerlendirilir.</li>
  <li>Eksik belge varsa SMS ile bilgilendirilirsiniz ve 10 gün ek süre verilir.</li>
</ul>
```

**React** (`import { Liste } from '@tr-ds/react'`):

```jsx
<Liste ogeler={["Nüfus cüzdanı","İkametgâh belgesi"]} />
```

**Vue** (`import { Liste } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsListe :ogeler='["Nüfus cüzdanı","İkametgâh belgesi"]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Özet listesi (Summary list) — `summary-list`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.4, 3.3.4. Also in: UK, FR, IE, JP, DE.

Kullanıcının girdiği bilgileri gönderim öncesinde tek sayfada gösterir.

When to use:
- Her form akışının sonunda "Cevaplarınızı kontrol edin" sayfasında kullanın.
- Her satır düzenleme bağlantısı taşır.

Accessibility:
- `<dl>`, `<dt>` ve `<dd>` etiketlerini kullanın.
- Düzenleme bağlantısı hangi alanı düzenlediğini gizli metinle söyler.

**Cevaplarınızı kontrol edin**

```html
<dl class="trds-ozet">
  <div class="trds-ozet__satir">
    <dt class="trds-ozet__anahtar">Ad soyad</dt>
    <dd class="trds-ozet__deger">Ayşe Yılmaz</dd>
    <dd class="trds-ozet__eylem"><a href="#">Değiştir<span class="trds-gorsel-gizli"> ad soyad</span></a></dd>
  </div>
  <div class="trds-ozet__satir">
    <dt class="trds-ozet__anahtar">Doğum tarihi</dt>
    <dd class="trds-ozet__deger">27.03.1997</dd>
    <dd class="trds-ozet__eylem"><a href="#">Değiştir<span class="trds-gorsel-gizli"> doğum tarihi</span></a></dd>
  </div>
</dl>
```

**React** (`import { OzetListesi } from '@tr-ds/react'`):

```jsx
<OzetListesi satirlar={[{"anahtar":"Ad soyad","deger":"Ayşe Yılmaz","eylem":{"href":"#","ad":"Değiştir"}}]} />
```

**Vue** (`import { OzetListesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsOzetListesi :satirlar='[{"anahtar":"Ad soyad","deger":"Ayşe Yılmaz","eylem":{"href":"#","ad":"Değiştir"}}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Rozet (Badge) — `badge`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1, 1.4.3. Also in: FR, KR, DK, DE, NO.

Bir öğenin yanında küçük bir sayı veya kısa bir kelime gösterir. Etiketten küçüktür.

When to use:
- Okunmamış bildirim sayısı gibi bir sayı için kullanın.
- "Yeni" gibi tek kelimelik bir işaret için kullanın.
- Başvuru durumu için etiket kullanın. Rozet cümle taşımaz.

Accessibility:
- Sayı rozeti bağlamını metinden alır: "Bildirimler 12". Tek başına 12 anlamsızdır.
- Yalnız renkle anlam vermez. Kırmızı rozet "Yeni" yazar.

**Sayı ve kelime**

```html
<a class="trds-link" href="#">Bildirimler <span class="trds-rozet trds-rozet--sayi">12</span></a>
<span class="trds-rozet">Taslak</span>
<span class="trds-rozet trds-rozet--kirmizi">Yeni</span>
<span class="trds-rozet trds-rozet--mavi">Beta</span>
```

**React** (`import { Rozet } from '@tr-ds/react'`):

```jsx
<Rozet renk="kirmizi">Yeni</Rozet>
```

**Vue** (`import { Rozet } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsRozet renk="kirmizi">Yeni</TrdsRozet>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Ses (Audio) — `audio`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.2.1, 1.4.2. Also in: CH, KR.

Ses kaydı ve altında dökümü. Tarayıcının kendi oynatıcısı kullanılır.

When to use:
- Basın açıklaması veya sesli kılavuz için kullanın.

Accessibility:
- Döküm her zaman vardır. Ayrıntılar bileşeninin içinde durur.
- Oynatıcı kendiliğinden başlamaz.

**Dökümlü**

```html
<div class="trds-ses">
  <p class="trds-ses__baslik">Sağlık Bakanı’nın açıklaması, 6 Eylül 2026, 4 dakika</p>
  <audio class="trds-ses__oynatici" controls preload="none" src="#"></audio>
  <details class="trds-ayrinti"><summary>Döküm</summary><p class="trds-govde">Değerli basın mensupları, bugün Dikili Devlet Hastanesi’ni hizmete açıyoruz.</p></details>
</div>
```

**React** (`import { Ses } from '@tr-ds/react'`):

```jsx
<Ses baslik="Açıklama, 4 dakika" src="#" dokum="Döküm metni" />
```

**Vue** (`import { Ses } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSes baslik="Açıklama, 4 dakika" src="#" dokum="Döküm metni" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Sıralanabilir tablo (Sortable table) — `sortable-table`

Group: İçerik. Status: CSS stable, JS sütun sıralama, React stable, Vue stable. WCAG: 1.3.1, 4.1.3. Also in: IE, JP.

Başlığa basınca sütuna göre sıralanan tablo. Sayı sütunları sayı olarak sıralanır.

When to use:
- 10 satırdan uzun veri tablosunda kullanın.
- Sunucudan sayfalanan veride sıralamayı sunucu yapar. Bu bileşen sayfadaki satırları sıralar.

Accessibility:
- Başlıklar `aria-sort` taşır ve düğmedir. Sıralama sonucu canlı bölgeden duyurulur.
- Sıralama yönü ok simgesiyle ve metinle belli olur.

**Üç sütun**

```html
<div class="trds-tablo-kap">
  <table class="trds-tablo trds-tablo--siralanabilir" data-trds="sirala">
    <caption>Sosyal güvenlik il müdürlükleri</caption>
    <thead><tr><th scope="col" aria-sort="none"><button type="button">İl <svg class="trds-simge" aria-hidden="true"><use href="#trds-sort-up-down"/></svg></button></th><th scope="col" aria-sort="none"><button type="button">Merkez sayısı <svg class="trds-simge" aria-hidden="true"><use href="#trds-sort-up-down"/></svg></button></th><th scope="col" aria-sort="none"><button type="button">Çalışan <svg class="trds-simge" aria-hidden="true"><use href="#trds-sort-up-down"/></svg></button></th></tr></thead>
    <tbody><tr><td>İzmir</td><td>28</td><td>1.940</td></tr><tr><td>Ankara</td><td>31</td><td>2.310</td></tr><tr><td>Çanakkale</td><td>7</td><td>312</td></tr><tr><td>İstanbul</td><td>52</td><td>4.120</td></tr></tbody>
  </table>
</div>
```

**React** (`import { SiralanabilirTablo } from '@tr-ds/react'`):

```jsx
<SiralanabilirTablo baslik="İl müdürlükleri" sutunlar={["İl","Çalışan"]} satirlar={[["İzmir","1.940"],["Ankara","2.310"]]} />
```

**Vue** (`import { SiralanabilirTablo } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSiralanabilirTablo baslik="İl müdürlükleri" :sutunlar='["İl","Çalışan"]' :satirlar='[["İzmir","1.940"],["Ankara","2.310"]]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Simge (Icon) — `icon`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.1.1, 1.4.11. Also in: US, IE, DE, KR.

TRDS simge setinden bir simgeyi üç boyda gösterir. Yeni simge çizilmez.

When to use:
- Bir eylemi veya bölümü tanımak için metnin yanında kullanın.
- Simgeyi tek başına anlam taşıyacak biçimde kullanmayın. Yanında metin bulunur.
- Set dışından simge kullanmayın. Vatandaş aynı simgeyi her sitede tanır.

Accessibility:
- Süs simgesi `aria-hidden="true"` taşır.
- Anlam taşıyan simge `role="img"` ve `aria-label` taşır.
- Simge `currentColor` ile metnin rengini alır. Karşıtlık metinle aynıdır.

**Üç boy**

```html
<p class="trds-govde">
  <svg class="trds-simge trds-simge--kucuk" aria-hidden="true"><use href="#trds-health"/></svg>
  <svg class="trds-simge" aria-hidden="true"><use href="#trds-health"/></svg>
  <svg class="trds-simge trds-simge--buyuk" aria-hidden="true"><use href="#trds-health"/></svg>
  Sağlık hizmetleri
</p>
```

**Metinle**

```html
<a class="trds-link trds-simgeli" href="#"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-print"/></svg> Belgeyi yazdır</a>
<button class="trds-button trds-button--ikincil trds-simgeli" type="button"><svg class="trds-simge" aria-hidden="true"><use href="#trds-tool-refresh"/></svg> Yenile</button>
```

**React** (`import { Simge } from '@tr-ds/react'`):

```jsx
<Simge ad="health" />
```

**Vue** (`import { Simge } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSimge ad="health" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Simgeli liste (Icon list) — `icon-list`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 1.4.1. Also in: US.

Her maddesi bir simge ile başlayan liste. Gerekli belgeler, koşullar.

When to use:
- Koşul listesi ve belge listesi için kullanın.
- Simge maddeyi anlatır, süslemez. Her maddede aynı simge de olabilir.

Accessibility:
- Simge süstür. Madde metni tek başına anlaşılır.
- Onay işareti "tamam" anlamına gelir. Anlam yalnız simgeyle verilmez, metin de söyler.

**Gerekli belgeler**

```html
<ul class="trds-simgeli-liste">
  <li><svg class="trds-simge" aria-hidden="true"><use href="#trds-identity"/></svg><span>T.C. kimlik kartı</span></li>
  <li><svg class="trds-simge" aria-hidden="true"><use href="#trds-home"/></svg><span>İkametgâh belgesi, son 30 gün içinde alınmış</span></li>
  <li><svg class="trds-simge" aria-hidden="true"><use href="#trds-mail"/></svg><span>Bir e-posta adresi</span></li>
</ul>
```

**React** (`import { SimgeliListe } from '@tr-ds/react'`):

```jsx
<SimgeliListe ogeler={[{"simge":"identity","ad":"T.C. kimlik kartı"}]} />
```

**Vue** (`import { SimgeliListe } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSimgeliListe :ogeler='[{"simge":"identity","ad":"T.C. kimlik kartı"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Son güncelleme (Date modified) — `date-modified`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: CA, CH.

Sayfanın en son ne zaman değiştiğini söyler. Her içerik sayfasının sonunda durur.

When to use:
- Mevzuat, kılavuz ve duyuru sayfalarında kullanın.
- Form adımlarında kullanmayın.

Accessibility:
- Tarih `<time datetime>` taşır.
- Metin "Son güncelleme" ile başlar. Yalnız tarih yazılmaz.

**Temel**

```html
<p class="trds-guncelleme">Son güncelleme: <time datetime="2026-09-06">6 Eylül 2026</time></p>
```

**React** (`import { SonGuncelleme } from '@tr-ds/react'`):

```jsx
<SonGuncelleme tarih="6 Eylül 2026" iso="2026-09-06" />
```

**Vue** (`import { SonGuncelleme } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSonGuncelleme tarih="6 Eylül 2026" iso="2026-09-06" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Süreç listesi (Process list) — `process-list`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: US, FR.

Bir işin adımlarını numaralı ve açıklamalı anlatır. Başvurudan önce okunur.

When to use:
- "Nasıl başvururum?" sayfalarında kullanın.
- Form içinde ilerleme için adım göstergesi kullanın.

Accessibility:
- Sıralı liste kullanılır. Numara CSS ile gelir, ekran okuyucu listeden sayar.
- Her adımın başlığı bir başlık ögesidir.

**Üç adım**

```html
<ol class="trds-surec">
  <li class="trds-surec__adim"><h3 class="trds-surec__baslik">Belgeleri hazırlayın</h3><p class="trds-surec__metin">Kimlik kartı ve ikametgâh belgesi. İkisi de e-Devlet’ten alınır.</p></li>
  <li class="trds-surec__adim"><h3 class="trds-surec__baslik">Başvuruyu doldurun</h3><p class="trds-surec__metin">Form 10 dakika sürer. Kaydedip sonra devam edebilirsiniz.</p></li>
  <li class="trds-surec__adim"><h3 class="trds-surec__baslik">Sonucu bekleyin</h3><p class="trds-surec__metin">Sonuç 15 iş günü içinde e-Devlet’e düşer.</p></li>
</ol>
```

**React** (`import { SurecListesi } from '@tr-ds/react'`):

```jsx
<SurecListesi adimlar={[{"baslik":"Belgeleri hazırlayın","metin":"Kimlik ve ikametgâh."}]} />
```

**Vue** (`import { SurecListesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsSurecListesi :adimlar='[{"baslik":"Belgeleri hazırlayın","metin":"Kimlik ve ikametgâh."}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Tablo (Table) — `table`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 1.4.10. Also in: UK, US, FR, IE, KR, JP, DK, DE, NO.

Veriyi satır ve sütun olarak düzenler.

When to use:
- İki boyutlu veriyi göstermek için kullanın.
- Sayfa düzeni için kullanmayın.
- Küçük ekranda tablo yatay kayar. Sayfanın kendisi yatay kaymaz.

Accessibility:
- Her tabloda `<caption>` bulunur.
- Başlık hücreleri `<th scope="col">` veya `<th scope="row">` olur.
- Sayı sütunları sağa yaslanır ve `font-variant-numeric: tabular-nums` alır.

**Temel**

```html
<div class="trds-tablo-kap">
  <table class="trds-tablo">
    <caption>Başvuru geçmişi</caption>
    <thead>
      <tr><th scope="col">Tarih</th><th scope="col">İşlem</th><th scope="col" class="trds-sayi">Tutar</th></tr>
    </thead>
    <tbody>
      <tr><td>12.03.2026</td><td>Başvuru alındı</td><td class="trds-sayi">0,00 ₺</td></tr>
      <tr><td>18.03.2026</td><td>Harç ödendi</td><td class="trds-sayi">1.250,00 ₺</td></tr>
    </tbody>
  </table>
</div>
```

**React** (`import { Tablo } from '@tr-ds/react'`):

```jsx
<Tablo baslik="Başvuru geçmişi" sutunlar={["Tarih","İşlem"]} satirlar={[["12.03.2026","Başvuru alındı"]]} />
```

**Vue** (`import { Tablo } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsTablo baslik="Başvuru geçmişi" :sutunlar='["Tarih","İşlem"]' :satirlar='[["12.03.2026","Başvuru alındı"]]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Takip et (Follow) — `follow`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 2.4.4. Also in: FR, CH.

Bülten kaydı ve sosyal medya hesapları tek blokta. Alt bilginin hemen üstünde durur.

When to use:
- Kurumun bir bülteni varsa kullanın.
- Sosyal hesaplar alt bilgide zaten varsa burada yinelemeyin.

Accessibility:
- E-posta alanı etiket taşır. Düğme metni "Abone ol" olur.
- Sosyal bağlantılar `aria-label` ile platform adını söyler.

**Temel**

```html
<div class="trds-takip">
  <form class="trds-takip__bulten" action="#" method="post">
    <h3 class="trds-takip__baslik">Bültene abone olun</h3>
    <p class="trds-govde">Ayda bir e-posta. İstediğiniz zaman ayrılın.</p>
    <div class="trds-alan"><label class="trds-etiket" for="bulten-eposta">E-posta adresiniz</label><input class="trds-girdi" id="bulten-eposta" name="eposta" type="email" autocomplete="email"></div>
    <button class="trds-button" type="submit">Abone ol</button>
  </form>
  <div class="trds-takip__sosyal">
    <h3 class="trds-takip__baslik">Bizi takip edin</h3>
    <ul class="trds-paylas__liste"><li><a href="#" aria-label="X"><svg class="trds-simge" aria-hidden="true"><use href="#trds-twitter"/></svg></a></li><li><a href="#" aria-label="YouTube"><svg class="trds-simge" aria-hidden="true"><use href="#trds-youtube"/></svg></a></li><li><a href="#" aria-label="Instagram"><svg class="trds-simge" aria-hidden="true"><use href="#trds-instagram"/></svg></a></li><li><a href="#" aria-label="Facebook"><svg class="trds-simge" aria-hidden="true"><use href="#trds-facebook"/></svg></a></li></ul>
  </div>
</div>
```

**React** (`import { TakipEt } from '@tr-ds/react'`):

```jsx
<TakipEt sosyal={[{"ad":"X","simge":"twitter"}]} />
```

**Vue** (`import { TakipEt } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsTakipEt :sosyal='[{"ad":"X","simge":"twitter"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Tanım listesi (Description list) — `description-list`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1. Also in: DE, JP, IE.

Terim ve karşılık çiftlerini gösterir. Düzenleme bağlantısı yoktur, o özet listesindedir.

When to use:
- Başvuru numarası ve tarihi gibi salt okunur bilgi çiftleri için kullanın.
- Kullanıcının düzenleyeceği bilgi için özet listesi kullanın.

Accessibility:
- `<dl>`, `<dt>` ve `<dd>` kullanılır.
- Bir terim birden çok karşılık taşıyabilir. Her biri ayrı `<dd>` olur.

**Temel**

```html
<dl class="trds-tanim">
  <div class="trds-tanim__satir"><dt>Başvuru numarası</dt><dd>2026-004512</dd></div>
  <div class="trds-tanim__satir"><dt>Başvuru tarihi</dt><dd>12.03.2026</dd></div>
  <div class="trds-tanim__satir"><dt>Durum</dt><dd><span class="trds-etiket trds-etiket--mavi">İnceleniyor</span></dd></div>
</dl>
```

**React** (`import { TanimListesi } from '@tr-ds/react'`):

```jsx
<TanimListesi satirlar={[{"ad":"Başvuru numarası","deger":"2026-004512"}]} />
```

**Vue** (`import { TanimListesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsTanimListesi :satirlar='[{"ad":"Başvuru numarası","deger":"2026-004512"}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Tipografi (Typography) — `typography`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 1.4.8, 1.4.12, 2.4.6. Also in: IE, JP, DE, NO.

Başlık, üst başlık, alt başlık, gövde ve etiket sınıfları. Dokuz kademeli ölçek.

When to use:
- Her sayfada tek bir `h1` bulunur. Başlık düzeyi atlanmaz.
- Görsel boyut ile anlamsal düzey ayrıdır. `h2` öğesine `trds-baslik--3` sınıfı verilebilir.
- Üst başlık kısa bir bağlam verir: kurum adı, bölüm adı veya belge türü.

Accessibility:
- Satır uzunluğu 38rem ile sınırlıdır. Uzun satır okumayı yavaşlatır.
- Satır yüksekliği 1,5 altına düşmez. Türkçe uzun sözcük taşır.
- Yazı tipi her ağırlıkta ş, ğ, ı, İ, ö, ç, ü taşır.

**Başlık düzeyleri**

```html
<p class="trds-ust-baslik">Sosyal Güvenlik Kurumu</p>
<h1 class="trds-baslik trds-baslik--1">Emeklilik başvurusu</h1>
<p class="trds-alt-baslik">Yaş ve prim gün sayısı koşullarını sağlıyorsanız çevrim içi başvurun.</p>
<h2 class="trds-baslik trds-baslik--2">Başvuru koşulları</h2>
<h3 class="trds-baslik trds-baslik--3">Yaş koşulu</h3>
<h4 class="trds-baslik trds-baslik--4">İstisnalar</h4>
```

**Gövde metni**

```html
<p class="trds-govde trds-govde--buyuk">Başvurunuz 15 iş günü içinde sonuçlanır. Sonuç e-Devlet üzerinden bildirilir.</p>
<p class="trds-govde">Eksik belge varsa size SMS gönderilir. Belgeyi 10 gün içinde yüklemeniz gerekir.</p>
<p class="trds-govde trds-kucuk">Son güncelleme: 12 Mart 2026</p>
```

**React** (`import { Baslik } from '@tr-ds/react'`):

```jsx
<Baslik seviye={1}>Emeklilik başvurusu</Baslik>
```

**Vue** (`import { Baslik } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsBaslik :seviye='1'>Emeklilik başvurusu</TrdsBaslik>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Video (Video) — `video`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.2.2, 1.2.3, 1.4.2. Also in: NL, IT, FR, CH, KR.

Altyazılı video ve altında dökümü. 16:9 kutu, sayfa zıplamaz.

When to use:
- Kılavuz videosu ve tören kaydı için kullanın.
- Sayfanın tek içeriği video olmaz. Metin de olur.

Accessibility:
- `<track kind="captions">` ile altyazı gelir. Döküm ayrıntılar içinde durur.
- Kendiliğinden başlamaz, ses açık gelmez.

**Altyazılı**

```html
<div class="trds-video">
  <div class="trds-video__kap"><video class="trds-video__oynatici" controls preload="metadata" poster="/trds/turk-bayragi.svg"><track kind="captions" srclang="tr" label="Türkçe" src="#"></video></div>
  <p class="trds-video__altyazi">Dikili Devlet Hastanesi açılış töreni, 6 Eylül 2026, 12 dakika</p>
  <details class="trds-ayrinti"><summary>Döküm</summary><p class="trds-govde">Törenin tam dökümü burada durur.</p></details>
</div>
```

**React** (`import { Video } from '@tr-ds/react'`):

```jsx
<Video altyazi="Tören, 12 dakika" src="#" dokum="Döküm" />
```

**Vue** (`import { Video } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsVideo altyazi="Tören, 12 dakika" src="#" dokum="Döküm" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Vurgulu metin (Inset text) — `inset-text`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.4.1. Also in: UK, US, FR, IE.

Metnin geri kalanından ayrılması gereken bir paragrafı sol çizgiyle öne çıkarır.

When to use:
- Bir istisna, koşul veya önemli bir not için kullanın.
- Uyarı için uyarı bileşenini kullanın. Bu bileşen renk taşımaz.
- Sayfada birden çok kez kullanmayın. Vurgu çoğalınca kaybolur.

Accessibility:
- Anlam sol çizgiye bağlı değildir. Metin kendi başına anlaşılır.
- Çizgi rengi metin rengiyle aynı kontrastı taşır.

**Temel**

```html
<div class="trds-vurgu">
  <p>Yurt dışında yaşıyorsanız başvurunuzu en yakın konsolosluk üzerinden yapın. Çevrim içi başvuru yurt dışı adresini kabul etmez.</p>
</div>
```

**React** (`import { VurguluMetin } from '@tr-ds/react'`):

```jsx
<VurguluMetin>Yurt dışında yaşıyorsanız konsolosluk üzerinden başvurun.</VurguluMetin>
```

**Vue** (`import { VurguluMetin } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsVurguluMetin>Yurt dışında yaşıyorsanız konsolosluk üzerinden başvurun.</TrdsVurguluMetin>
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Zaman çizelgesi (Timeline) — `timeline`

Group: İçerik. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 1.3.1, 1.3.2. Also in: IT, NL.

Bir başvurunun geçtiği adımları tarih sırasıyla gösterir. En yenisi en üstte.

When to use:
- Başvuru geçmişi, dava süreci veya kargo takibi için kullanın.
- Gelecek adımlar için adım göstergesi kullanın. Zaman çizelgesi geçmişi anlatır.

Accessibility:
- Sıralı liste kullanılır. Ekran okuyucu adım sayısını söyler.
- Tarih `<time datetime>` taşır. Çizgi ve nokta süstür.

**Başvuru geçmişi**

```html
<ol class="trds-zaman">
  <li class="trds-zaman__oge trds-zaman__oge--etkin"><time class="trds-zaman__tarih" datetime="2026-09-04">4 Eylül 2026</time><p class="trds-zaman__baslik">İnceleme başladı</p><p class="trds-zaman__metin">Başvurunuz Emeklilik Hizmetleri Genel Müdürlüğüne iletildi.</p></li>
  <li class="trds-zaman__oge"><time class="trds-zaman__tarih" datetime="2026-09-02">2 Eylül 2026</time><p class="trds-zaman__baslik">Belgeler alındı</p><p class="trds-zaman__metin">3 belge yüklendi.</p></li>
  <li class="trds-zaman__oge"><time class="trds-zaman__tarih" datetime="2026-09-01">1 Eylül 2026</time><p class="trds-zaman__baslik">Başvuru oluşturuldu</p></li>
</ol>
```

**React** (`import { ZamanCizelgesi } from '@tr-ds/react'`):

```jsx
<ZamanCizelgesi ogeler={[{"tarih":"4 Eylül 2026","iso":"2026-09-04","baslik":"İnceleme başladı","etkin":true}]} />
```

**Vue** (`import { ZamanCizelgesi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsZamanCizelgesi :ogeler='[{"tarih":"4 Eylül 2026","iso":"2026-09-04","baslik":"İnceleme başladı","etkin":true}]' />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

## Türkiye’ye özgü form

Türk kimlik, adres ve finans biçimlerini alan parçalar.

### Adres (Address) — `adres`

Group: Türkiye’ye özgü form · unique to Türkiye. Status: CSS stable, JS bağlı listeler, React stable, Vue stable. WCAG: 1.3.1, 3.3.2, 4.1.3. Also in: UK.

İl, ilçe ve mahalle alanlarını birbirine bağlar, sonra açık adresi alır.

When to use:
- Tebligat adresi veya ikametgâh adresi isterken kullanın.
- Adres kayıt sistemine bağlanabiliyorsanız bağlanın. Bağlanamıyorsanız serbest metin verin.

Accessibility:
- İlçe listesi il seçilmeden devre dışı kalmaz. Boş kalır ve durum duyurulur.
- Liste yüklenirken `aria-busy="true"` kullanılır.
- Sokak adı olmayan adresler için serbest metin alanı her zaman bulunur.
- Posta kodu zorunlu değildir. Çok sayıda adreste bilinmez.

Why it exists: Türk adresi il, ilçe, mahalle, cadde veya sokak, bina no ve daire no olarak yapılır. Bu yapı hiçbir yabancı adres bileşenine uymaz.

Data attributes used by the markup: `data-adres`.

**Bağlı listeler**

```html
<fieldset class="trds-alan-grubu" data-trds="adres">
  <legend class="trds-baslik-legend">Tebligat adresiniz</legend>
  <div class="trds-alan">
    <label class="trds-etiket" for="a-il">İl</label>
    <select class="trds-secim" id="a-il" name="il" data-adres="il"><option value="">Seçiniz</option></select>
  </div>
  <div class="trds-alan">
    <label class="trds-etiket" for="a-ilce">İlçe</label>
    <select class="trds-secim" id="a-ilce" name="ilce" data-adres="ilce"><option value="">Önce il seçiniz</option></select>
  </div>
  <div class="trds-alan">
    <label class="trds-etiket" for="a-acik">Açık adres</label>
    <p class="trds-yardim" id="a-acik-yardim">Mahalle, cadde veya sokak, bina no ve daire no.</p>
    <textarea class="trds-metin-alani" id="a-acik" name="acik_adres" rows="3" aria-describedby="a-acik-yardim"></textarea>
  </div>
  <div class="trds-alan">
    <label class="trds-etiket" for="a-posta">Posta kodu <span class="trds-etiket__istege-bagli">(isteğe bağlı)</span></label>
    <input class="trds-girdi trds-girdi--5" id="a-posta" name="posta_kodu" type="text" inputmode="numeric" maxlength="5" autocomplete="postal-code">
  </div>
</fieldset>
```

**React** (`import { AdresGirisi } from '@tr-ds/react'`):

```jsx
<AdresGirisi etiket="Tebligat adresiniz" />
```

**Vue** (`import { AdresGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsAdresGirisi etiket="Tebligat adresiniz" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### IBAN girişi (IBAN input) — `iban-girisi`

Group: Türkiye’ye özgü form · unique to Türkiye. Status: CSS stable, JS doğrulama, React stable, Vue stable. WCAG: 1.3.5, 3.3.1, 3.3.3.

TR ile başlayan 26 karakterlik IBAN alır, dörderli gösterir ve mod 97 sağlamasını yapar.

When to use:
- İade, ödeme veya maaş hesabı isterken kullanın.
- Kullanıcının bankasını da gösterin. Banka kodu IBAN içinde durur.

Accessibility:
- TR öneki alanda hazır durur ve silinemez. İmleç önekin önüne geçmez. Yapıştırılan bir IBAN’daki TR yinelenmez.
- Boşluklu yapıştırma kabul edilir ve temizlenir.
- Gösterim dörderli gruplanır ama değer boşluksuz gönderilir.
- Yazı tipi tek aralıklıdır, böylece rakamlar hizalanır.
- Sağlama hatası "IBAN yanlış" değil "IBAN’ı kontrol edin, bir hane hatalı görünüyor" der.

Why it exists: IBAN hatası para kaybına yol açar. Mod 97 sağlaması bu hatanın çoğunu form gönderilmeden yakalar. Bugün bu doğrulama her kurumda ayrı yazılıyor.

Data attributes used by the markup: `data-trds-hata`, `data-trds-sayac`.

**Temel**

```html
<div class="trds-alan" data-trds="iban">
  <label class="trds-etiket" for="iban">IBAN numaranız</label>
  <p class="trds-yardim" id="iban-yardim">TR hazır yazılıdır. 24 rakam yazın. Boşluklu yapıştırabilirsiniz.</p>
  <p class="trds-hata" data-trds-hata hidden></p>
  <input class="trds-girdi trds-girdi--iban" id="iban" name="iban" type="text"
         autocomplete="off" spellcheck="false" maxlength="32" aria-describedby="iban-yardim iban-sayac">
  <p class="trds-sayac" id="iban-sayac" data-trds-sayac aria-live="off"></p>
</div>
```

**React** (`import { IbanGirisi } from '@tr-ds/react'`):

```jsx
<IbanGirisi etiket="IBAN numaranız" />
```

**Vue** (`import { IbanGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsIbanGirisi etiket="IBAN numaranız" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Plaka girişi (Licence plate input) — `plaka-girisi`

Group: Türkiye’ye özgü form · unique to Türkiye. Status: CSS stable, JS biçimlendirme, React stable, Vue stable. WCAG: 1.3.5, 3.3.1.

Türk araç plakasını alır, büyük harfe çevirir ve biçimini denetler.

When to use:
- Trafik cezası sorgusu, muayene randevusu veya araç işlemlerinde kullanın.

Accessibility:
- Büyük harfe çevirme Türkçe kuralına göre yapılır. "i" harfi "İ" olur.
- Boşluklu ve boşluksuz yazım kabul edilir.
- Alan görsel olarak plakaya benzetilir ama bu bir süs değildir, tanımayı kolaylaştırır.

Why it exists: Türkçe büyük harf kuralı burada gerçek bir hata kaynağıdır. `toUpperCase()` ile "34 iz 1234" plakası "34 IZ 1234" olur ve yanlış plaka sorgulanır. TRDS bu dönüşümü Türkçe kuralına göre yapar.

Data attributes used by the markup: `data-trds-hata`.

**Temel**

```html
<div class="trds-alan" data-trds="plaka">
  <label class="trds-etiket" for="plaka">Araç plakası</label>
  <p class="trds-yardim" id="plaka-yardim">Örnek: 34 ABC 123</p>
  <p class="trds-hata" data-trds-hata hidden></p>
  <input class="trds-girdi trds-girdi--plaka" id="plaka" name="plaka" type="text" autocomplete="off" spellcheck="false" aria-describedby="plaka-yardim">
</div>
```

**React** (`import { PlakaGirisi } from '@tr-ds/react'`):

```jsx
<PlakaGirisi etiket="Araç plakası" />
```

**Vue** (`import { PlakaGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsPlakaGirisi etiket="Araç plakası" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### T.C. kimlik numarası girişi (National identity number input) — `kimlik-no`

Group: Türkiye’ye özgü form · unique to Türkiye. Status: CSS stable, JS doğrulama, React stable, Vue stable. WCAG: 1.3.5, 3.3.1, 3.3.3.

11 haneli T.C. kimlik numarasını alır, biçimini denetler ve sağlama toplamını doğrular.

When to use:
- Kimlik doğrulaması yasal olarak gerekiyorsa kullanın. Gerekmiyorsa istemeyin.
- Yabancı kimlik numarası da kabul edilecekse `data-ykn="acik"` ekleyin.
- Numarayı asla adres satırında veya sorgu dizesinde taşımayın.

Accessibility:
- `inputmode="numeric"` ile telefonda sayı klavyesi açılır.
- `autocomplete` kapalıdır. Tarayıcı bu değeri saklamamalıdır.
- Parola alanı değildir. Değeri yıldızla gizlemeyin. Kullanıcı yazdığını görür.
- Yazarken altta bir sayaç ilerler: "7 / 11 hane". Son iki hanede duyurulur.
- Alandan çıkınca hane sayısı ve sağlama toplamı denetlenir. Hata ne olduğunu söyler: "11 hane olmalıdır. 7 hane girdiniz."
- `data-zorunlu` ile boş bırakılan alan da hata alır. Aksi hâlde boş alan sessizdir ve zorunluluğu sunucu denetler.

Why it exists: Kimlik numarasının hane sayısını ve sağlama toplamını denetler. Bu kontrol, numaranın gerçek bir kişiye ait olduğunu doğrulamaz.

Data attributes used by the markup: `data-zorunlu`, `data-trds-hata`, `data-trds-sayac`, `data-ykn`.

**Temel**

```html
<div class="trds-alan" data-trds="kimlik-no" data-zorunlu>
  <label class="trds-etiket" for="tckn">T.C. kimlik numaranız</label>
  <p class="trds-yardim" id="tckn-yardim">Nüfus cüzdanınızın ön yüzünde yazan 11 haneli numara.</p>
  <p class="trds-hata" data-trds-hata hidden></p>
  <input class="trds-girdi trds-girdi--11" id="tckn" name="tckn" type="text"
         inputmode="numeric" maxlength="11" autocomplete="off"
         aria-describedby="tckn-yardim tckn-sayac">
  <p class="trds-sayac" id="tckn-sayac" data-trds-sayac aria-live="off"></p>
</div>
```

**Yabancı kimlik numarası da kabul eder**

```html
<div class="trds-alan" data-trds="kimlik-no" data-ykn="acik">
  <label class="trds-etiket" for="kn">Kimlik numaranız</label>
  <p class="trds-yardim" id="kn-yardim">T.C. vatandaşı iseniz 11 haneli T.C. kimlik numaranızı, değilseniz 99 ile başlayan yabancı kimlik numaranızı yazın.</p>
  <p class="trds-hata" data-trds-hata hidden></p>
  <input class="trds-girdi trds-girdi--11" id="kn" name="kn" type="text" inputmode="numeric" maxlength="11" autocomplete="off" aria-describedby="kn-yardim kn-sayac">
  <p class="trds-sayac" id="kn-sayac" data-trds-sayac aria-live="off"></p>
</div>
```

**React** (`import { KimlikNoGirisi } from '@tr-ds/react'`):

```jsx
<KimlikNoGirisi etiket="T.C. kimlik numaranız" />
```

**Vue** (`import { KimlikNoGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKimlikNoGirisi etiket="T.C. kimlik numaranız" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Tarih girişi (Date input) — `tarih-girisi`

Group: Türkiye’ye özgü form · unique to Türkiye. Status: CSS stable, JS doğrulama, React stable, Vue stable. WCAG: 1.3.1, 1.3.5, 3.3.1, 3.3.2. Also in: UK, KR, DK, DE.

Gün, ay ve yıl için üç ayrı alan verir. Türkçe gg.aa.yyyy biçimini kullanır.

When to use:
- Doğum tarihi gibi kullanıcının ezbere bildiği bir tarih için kullanın.
- Randevu seçimi gibi takvim gerektiren bir tarih için takvim bileşenini kullanın.
- Doğum tarihi için takvim açmayın. 60 yıl geri gitmek yavaştır.

Accessibility:
- Üç alan tek bir `fieldset` içinde durur ve `legend` soruyu taşır.
- Her alanın kendi görünür etiketi vardır: Gün, Ay, Yıl.
- Hata tek bir mesajda toplanır ve odak ilk hatalı alana gider.
- Ay alanı sayı kabul eder. Ay adlarını Türkçe küçük harfle yazın: ocak, şubat.

Why it exists: Biçim farkı gerçek bir hata kaynağıdır. 03.04.2026 Türkiye’de 3 Nisan, Amerika’da 4 Mart okunur. Üç ayrı alan bu belirsizliği kaldırır.

Data attributes used by the markup: `data-trds-hata`.

**Doğum tarihi**

```html
<fieldset class="trds-alan-grubu" data-trds="tarih">
  <legend class="trds-baslik-legend">Doğum tarihiniz nedir?</legend>
  <p class="trds-yardim" id="dt-yardim">Örnek: 27 03 1997</p>
  <div class="trds-tarih" role="group" aria-describedby="dt-yardim">
    <div class="trds-tarih__parca">
      <label class="trds-etiket trds-etiket--kucuk" for="dt-gun">Gün</label>
      <input class="trds-girdi trds-girdi--2" id="dt-gun" name="gun" type="text" inputmode="numeric" maxlength="2">
    </div>
    <div class="trds-tarih__parca">
      <label class="trds-etiket trds-etiket--kucuk" for="dt-ay">Ay</label>
      <input class="trds-girdi trds-girdi--2" id="dt-ay" name="ay" type="text" inputmode="numeric" maxlength="2">
    </div>
    <div class="trds-tarih__parca">
      <label class="trds-etiket trds-etiket--kucuk" for="dt-yil">Yıl</label>
      <input class="trds-girdi trds-girdi--4" id="dt-yil" name="yil" type="text" inputmode="numeric" maxlength="4">
    </div>
  </div>
  <p class="trds-hata" data-trds-hata hidden></p>
</fieldset>
```

**React** (`import { TarihGirisi } from '@tr-ds/react'`):

```jsx
<TarihGirisi etiket="Doğum tarihiniz" />
```

**Vue** (`import { TarihGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsTarihGirisi etiket="Doğum tarihiniz" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Telefon girişi (Phone input) — `telefon-girisi`

Group: Türkiye’ye özgü form · unique to Türkiye. Status: CSS stable, JS biçimlendirme, React stable, Vue stable. WCAG: 1.3.5, 3.3.1, 3.3.2. Also in: UK.

+90 ülke kodunu sabit tutar ve 10 haneli numarayı alır.

When to use:
- Cep telefonu veya sabit hat numarası isterken kullanın.
- Doğrulama kodu gönderilecekse cep telefonu olduğunu etikette belirtin.

Accessibility:
- Ülke kodu bir etikettir, girdi değildir. Kullanıcı silemez.
- Başta yazılan 0 sessizce kaldırılır. Kullanıcı 0 ile yazmaya alışkındır.
- Numara yazılırken 3-3-2-2 öbeklerine ayrılır. On birinci hane yazılamaz.
- Boşluk ve tire kabul edilir ve temizlenir. Yapıştırılan numarayı reddetmeyin.
- `autocomplete="tel-national"` kullanılır.

Why it exists: Türk kullanıcı numarasını 0 ile yazar. Çoğu form bunu hata sayar. Bu bileşen 0 ile yazılan, boşluklu ve +90 ile yazılan biçimlerin hepsini kabul eder.

Data attributes used by the markup: `data-cep`, `data-zorunlu`, `data-trds-hata`, `data-trds-sayac`.

**Cep telefonu**

```html
<div class="trds-alan" data-trds="telefon" data-cep="zorunlu" data-zorunlu>
  <label class="trds-etiket" for="tel">Cep telefonu numaranız</label>
  <p class="trds-yardim" id="tel-yardim">Doğrulama kodu bu numaraya gönderilecek.</p>
  <p class="trds-hata" data-trds-hata hidden></p>
  <div class="trds-telefon">
    <span class="trds-telefon__kod" aria-hidden="true">+90</span>
    <input class="trds-girdi trds-girdi--10" id="tel" name="tel" type="tel"
           inputmode="tel" autocomplete="tel-national" maxlength="13" aria-describedby="tel-yardim tel-sayac">
  </div>
  <p class="trds-sayac" id="tel-sayac" data-trds-sayac aria-live="off"></p>
</div>
```

**React** (`import { TelefonGirisi } from '@tr-ds/react'`):

```jsx
<TelefonGirisi etiket="Telefon numaranız" />
```

**Vue** (`import { TelefonGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsTelefonGirisi etiket="Telefon numaranız" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Vergi kimlik numarası girişi (Tax number input) — `vergi-no`

Group: Türkiye’ye özgü form · unique to Türkiye. Status: CSS stable, JS doğrulama, React stable, Vue stable. WCAG: 1.3.5, 3.3.1.

10 haneli vergi kimlik numarasını alır ve sağlama toplamını doğrular.

When to use:
- Kurumsal başvurularda kullanın.
- Gerçek kişi için T.C. kimlik numarası alanını kullanın.

Accessibility:
- `inputmode="numeric"` kullanılır.
- Hata metni hangi haneye bakılacağını söylemez, yalnız numarayı kontrol etmeyi ister.

Why it exists: Kurumsal hizmetlerin tamamı bu numarayı ister. Sağlama algoritması tek bir yerde durmalıdır.

Data attributes used by the markup: `data-trds-hata`, `data-trds-sayac`.

**Temel**

```html
<div class="trds-alan" data-trds="vergi-no">
  <label class="trds-etiket" for="vkn">Vergi kimlik numarası</label>
  <p class="trds-hata" data-trds-hata hidden></p>
  <input class="trds-girdi trds-girdi--10" id="vkn" name="vkn" type="text" inputmode="numeric" maxlength="10" autocomplete="off" aria-describedby="vkn-sayac">
  <p class="trds-sayac" id="vkn-sayac" data-trds-sayac aria-live="off"></p>
</div>
```

**React** (`import { VergiNoGirisi } from '@tr-ds/react'`):

```jsx
<VergiNoGirisi etiket="Vergi kimlik numarası" />
```

**Vue** (`import { VergiNoGirisi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsVergiNoGirisi etiket="Vergi kimlik numarası" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

## Türkiye’ye özgü hizmet

Türk mevzuatından ve altyapısından doğan parçalar.

### e-Devlet ile giriş (e-Devlet sign in) — `edevlet-giris`

Group: Türkiye’ye özgü hizmet · unique to Türkiye. Status: CSS stable, JS yok, React stable, Vue stable. WCAG: 2.4.4, 3.2.4. Also in: FR.

Kullanıcıyı e-Devlet Kapısı kimlik doğrulamasına gönderen tek ve değişmez düğme.

When to use:
- Kimlik doğrulaması gereken her hizmette birincil giriş yolu olarak kullanın.
- Görünümünü değiştirmeyin. Her sitede aynı görünmesi güven işaretidir.
- Bu bileşen `@tr-ds/identity` paketindedir ve kısıtlı lisans taşır.

Accessibility:
- Bir `<a>` öğesidir, çünkü kullanıcıyı başka bir alan adına götürür.
- Metin her zaman "e-Devlet ile giriş yap" olur.
- Yanında alternatif giriş yolları listelenir. e-Devlet tek yol olmamalıdır.

Why it exists: e-Devlet ile giriş seçeneğini tutarlı bir işaret ve etiketle gösterir. Kimlik doğrulama için ayrı bir hizmet entegrasyonu gerekir.

**Temel**

```html
<div class="trds-edevlet">
  <a class="trds-edevlet__dugme" href="#">
    <span class="trds-edevlet__isaret" aria-hidden="true"><img src="/trds/e-devlet-isaret.png" alt=""></span>
    <span>e-Devlet ile giriş yap</span>
  </a>
  <p class="trds-edevlet__yardim">e-Devlet şifreniz yoksa <a class="trds-link" href="#">PTT şubelerinden alabilirsiniz</a>.</p>
</div>
```

**React** (`import { EdevletGiris } from '@tr-ds/react'`):

```jsx
<EdevletGiris href="#" />
```

**Vue** (`import { EdevletGiris } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsEdevletGiris href="#" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### Erişilebilirlik menüsü (Accessibility menu) — `erisim-menusu`

Group: Türkiye’ye özgü hizmet · unique to Türkiye. Status: CSS stable, JS ayar saklama, React stable, Vue stable. WCAG: 1.4.4, 1.4.6, 1.4.12. Also in: FR, KR.

Kullanıcının yazı boyutunu, karşıtlığı ve temayı değiştirmesini sağlar.

When to use:
- Her sayfada başlık çubuğunun içinde kullanın.
- Ayarları kullanıcının tarayıcısında saklayın. Sunucuya göndermeyin.

Accessibility:
- Yazı boyutu üç kademedir ve sayfayı bozmadan yüzde 200 büyür.
- Yüksek karşıtlık kipi 7 üzerinde kontrast sağlar.
- Ayar `localStorage` içinde saklanır. Erişilemezse sayfa yine çalışır.
- Panel üç yolla kapanır: kapat düğmesi, Esc tuşu veya dışarı tıklama.
- Bu menü erişilebilirlik yükümlülüğünün yerine geçmez. Sayfa menü kapalıyken de uyumludur.

Why it exists: Yazı boyutu ve tema tercihlerini tek menüde toplar. Bu tercihler, sayfanın temel erişilebilirlik özelliklerinin yerine geçmez.

Data attributes used by the markup: `data-yazi`, `data-tema`.

**Temel**

```html
<div class="trds-erisim" data-trds="erisim-menusu">
  <button class="trds-erisim__dugme" type="button" aria-expanded="false" aria-controls="erisim-panel">Görünüm ayarları</button>
  <div class="trds-erisim__panel" id="erisim-panel" hidden>
    <button class="trds-erisim__kapat" type="button" aria-label="Menüyü kapat"><svg class="trds-simge" aria-hidden="true"><use href="#trds-close"/></svg></button>
    <fieldset class="trds-erisim__grup">
      <legend>Yazı boyutu</legend>
      <button class="trds-erisim__secenek" type="button" data-yazi="normal" aria-pressed="true">Normal</button>
      <button class="trds-erisim__secenek" type="button" data-yazi="buyuk" aria-pressed="false">Büyük</button>
      <button class="trds-erisim__secenek" type="button" data-yazi="cok-buyuk" aria-pressed="false">Çok büyük</button>
    </fieldset>
    <fieldset class="trds-erisim__grup">
      <legend>Tema</legend>
      <button class="trds-erisim__secenek" type="button" data-tema="acik" aria-pressed="true">Açık</button>
      <button class="trds-erisim__secenek" type="button" data-tema="koyu" aria-pressed="false">Koyu</button>
      <button class="trds-erisim__secenek" type="button" data-tema="yuksek" aria-pressed="false">Yüksek karşıtlık</button>
    </fieldset>
  </div>
</div>
```

**React** (`import { ErisimMenusu } from '@tr-ds/react'`):

```jsx
<ErisimMenusu />
```

**Vue** (`import { ErisimMenusu } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsErisimMenusu />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.

### KVKK açık rıza (Data protection consent) — `kvkk-onay`

Group: Türkiye’ye özgü hizmet · unique to Türkiye. Status: CSS stable, JS gönderim denetimi, React stable, Vue stable. WCAG: 1.3.1, 3.3.1, 3.3.2. Also in: FR.

6698 sayılı kanun kapsamında açık rıza alır ve aydınlatma metnine bağlantı verir.

When to use:
- Kişisel veri işlenen her formda kullanın.
- Kutu önceden işaretli gelmez. Açık rıza işaretlenmiş sayılamaz.
- Rıza vermeyen kullanıcı için alternatif bir yol gösterin.

Accessibility:
- Aydınlatma metni ayrı bir sayfada durur ve bağlantı yeni sekmede açılır.
- Rıza metni sade Türkçe ile yazılır. Hukuk dili özet metinde kullanılmaz.
- Zorunlu ve isteğe bağlı rızalar ayrı kutulardır. Tek kutuda toplanmaz.
- Zorunlu onay boşken form gönderilmez. Hata metni kutunun üstünde görünür ve odak onay kutusuna gider.

Why it exists: Aydınlatma metni bağlantısını ve kullanıcının onay seçeneğini aynı alanda gösterir. Onay seçeneği başlangıçta işaretli değildir.

**Temel**

```html
<div class="trds-kvkk">
  <h2 class="trds-kvkk__baslik">Kişisel verilerinizin işlenmesi</h2>
  <p class="trds-govde">Başvurunuzu değerlendirmek için kimlik ve iletişim bilgilerinizi işliyoruz. Ayrıntılar <a class="trds-link" href="#" target="_blank" rel="noopener">aydınlatma metninde</a> yazılıdır (yeni sekmede açılır).</p>
  <div class="trds-secenek">
    <input class="trds-onay" id="kvkk-zorunlu" name="kvkk" type="checkbox" required>
    <label class="trds-secenek-etiket" for="kvkk-zorunlu">Başvurumun değerlendirilmesi için verilerimin işlenmesine izin veriyorum.</label>
  </div>
  <div class="trds-secenek">
    <input class="trds-onay" id="kvkk-istege" name="kvkk_bilgilendirme" type="checkbox">
    <label class="trds-secenek-etiket" for="kvkk-istege">Yeni hizmetler hakkında bilgilendirme almak istiyorum <span class="trds-etiket__istege-bagli">(isteğe bağlı)</span>.</label>
  </div>
</div>
```

**Gönderim denetimi**

```html
<form action="#" method="post" novalidate>
  <div class="trds-kvkk" data-trds="kvkk">
    <h2 class="trds-kvkk__baslik">Kişisel verilerinizin işlenmesi</h2>
    <p class="trds-govde">Başvurunuzu değerlendirmek için kimlik ve iletişim bilgilerinizi işliyoruz. Ayrıntılar <a class="trds-link" href="#" target="_blank" rel="noopener">aydınlatma metninde</a> yazılıdır (yeni sekmede açılır).</p>
    <p class="trds-hata" id="kvkk-g-hata" hidden></p>
    <div class="trds-secenek">
      <input class="trds-onay" id="kvkk-g-zorunlu" name="kvkk" type="checkbox" required>
      <label class="trds-secenek-etiket" for="kvkk-g-zorunlu">Başvurumun değerlendirilmesi için verilerimin işlenmesine izin veriyorum.</label>
    </div>
    <div class="trds-secenek">
      <input class="trds-onay" id="kvkk-g-istege" name="kvkk_bilgilendirme" type="checkbox">
      <label class="trds-secenek-etiket" for="kvkk-g-istege">Yeni hizmetler hakkında bilgilendirme almak istiyorum <span class="trds-etiket__istege-bagli">(isteğe bağlı)</span>.</label>
    </div>
  </div>
  <button class="trds-button" type="submit">Başvuruyu gönder</button>
</form>
```

**Hata durumu**

```html
<div class="trds-kvkk trds-kvkk--hata">
  <h2 class="trds-kvkk__baslik">Kişisel verilerinizin işlenmesi</h2>
  <p class="trds-govde">Başvurunuzu değerlendirmek için kimlik ve iletişim bilgilerinizi işliyoruz. Ayrıntılar <a class="trds-link" href="#" target="_blank" rel="noopener">aydınlatma metninde</a> yazılıdır (yeni sekmede açılır).</p>
  <p class="trds-hata" id="kvkk-h-hata"><span class="trds-gorsel-gizli">Hata:</span> Devam etmek için verilerinizin işlenmesine izin vermeniz gerekir.</p>
  <div class="trds-secenek">
    <input class="trds-onay" id="kvkk-h-zorunlu" name="kvkk" type="checkbox" required aria-invalid="true" aria-describedby="kvkk-h-hata">
    <label class="trds-secenek-etiket" for="kvkk-h-zorunlu">Başvurumun değerlendirilmesi için verilerimin işlenmesine izin veriyorum.</label>
  </div>
  <div class="trds-secenek">
    <input class="trds-onay" id="kvkk-h-istege" name="kvkk_bilgilendirme" type="checkbox">
    <label class="trds-secenek-etiket" for="kvkk-h-istege">Yeni hizmetler hakkında bilgilendirme almak istiyorum <span class="trds-etiket__istege-bagli">(isteğe bağlı)</span>.</label>
  </div>
</div>
```

**React** (`import { KvkkOnayi } from '@tr-ds/react'`):

```jsx
<KvkkOnayi aydinlatmaHref="#" />
```

**Vue** (`import { KvkkOnayi } from '@tr-ds/vue'` or `app.use(Trds)`):

```html
<TrdsKvkkOnayi aydinlatmaHref="#" />
```

Props shown are the example props; every prop maps to the HTML above. Extra attributes pass through to the root or the input.


## Unique to Türkiye (13)

- Adres: İl, ilçe ve mahalle alanlarını birbirine bağlar, sonra açık adresi alır.
- e-Devlet ile giriş: Kullanıcıyı e-Devlet Kapısı kimlik doğrulamasına gönderen tek ve değişmez düğme.
- Erişilebilirlik menüsü: Kullanıcının yazı boyutunu, karşıtlığı ve temayı değiştirmesini sağlar.
- IBAN girişi: TR ile başlayan 26 karakterlik IBAN alır, dörderli gösterir ve mod 97 sağlamasını yapar.
- Kurum logosu: Kurumun kendi logosu, tek bir bağlantıda ve sabit yükseklikte. Logosu olmayan kurum için bayrak ve ad kilidi.
- Kurum tanıtıcısı: Sayfanın sonunda hizmeti işleten kurumu ve bağlı olduğu bakanlığı adlandırır.
- KVKK açık rıza: 6698 sayılı kanun kapsamında açık rıza alır ve aydınlatma metnine bağlantı verir.
- Plaka girişi: Türk araç plakasını alır, büyük harfe çevirir ve biçimini denetler.
- Resmî site afişi: Sayfanın en üstünde durur ve kullanıcıya bu sitenin resmî bir devlet sitesi olduğunu söyler.
- T.C. kimlik numarası girişi: 11 haneli T.C. kimlik numarasını alır, biçimini denetler ve sağlama toplamını doğrular.
- Tarih girişi: Gün, ay ve yıl için üç ayrı alan verir. Türkçe gg.aa.yyyy biçimini kullanır.
- Telefon girişi: +90 ülke kodunu sabit tutar ve 10 haneli numarayı alır.
- Vergi kimlik numarası girişi: 10 haneli vergi kimlik numarasını alır ve sağlama toplamını doğrular.
